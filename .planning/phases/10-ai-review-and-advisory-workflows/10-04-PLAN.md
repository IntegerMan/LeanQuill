---
phase: 10-ai-review-and-advisory-workflows
plan: 04
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - src/aiReviewWorkflow.ts
  - src/aiReviewSessionStore.ts
  - src/extension.ts
  - src/openQuestionStore.ts
  - package.json
  - test/aiReviewWorkflow.test.ts
  - test/aiReviewSessionStore.test.ts
  - test/storyChatExtensionCommands.test.ts
autonomous: true
requirements:
  - AIR-01
must_haves:
  truths:
    - "Author can start chapter review from the chapter/outline tree context menu using command label `Run chapter review`."
    - "Author can start the same chapter review workflow from Command Palette command `LeanQuill: Run chapter review`."
    - "Each chapter review asks `Choose how to run this review` with `One enabled persona…` and `All enabled personas` before creating stubs."
    - "Run start creates a session issue stub and chat log stub with the same `session_id` before direct LM or chat fallback."
    - "Chapter review finalizes the session issue file with review findings and updates the chat log with summary/counters."
    - "Author can promote a selected session finding into a normal typed issue under `.leanquill/issues/{type}/` per D-02."
    - "Chapter review produces durable audit paths under `.leanquill/issues/sessions/` and `.leanquill/chats/` and never touches manuscript writes."
  artifacts:
    - path: "src/aiReviewWorkflow.ts"
      provides: "Chapter review finalization and promotion orchestration"
      contains: "finalizeChapterReviewRun"
    - path: "src/aiReviewSessionStore.ts"
      provides: "Finding promotion helper source parsing"
      contains: "promoteAiReviewFindingToIssue"
    - path: "src/extension.ts"
      provides: "Chapter review command, persona QuickPick, direct-LM/chat fallback adapter, result save/promotion commands"
      contains: "leanquill.reviewChapter"
    - path: "package.json"
      provides: "Activation event, command contribution, command palette entry, chapter tree context entry"
      contains: "LeanQuill: Run chapter review"
    - path: "test/storyChatExtensionCommands.test.ts"
      provides: "Static wiring coverage for chapter review command and UI labels"
  key_links:
    - from: "package.json"
      to: "src/extension.ts"
      via: "command id `leanquill.reviewChapter`"
      pattern: "leanquill.reviewChapter"
    - from: "src/extension.ts"
      to: "src/aiReviewWorkflow.ts"
      via: "`prepareChapterReviewRun` before LM/chat"
      pattern: "prepareChapterReviewRun"
    - from: "src/aiReviewWorkflow.ts"
      to: "src/aiReviewSessionStore.ts"
      via: "`finalizeChapterReviewRun` writes findings via `finalizeAiReviewSession`"
      pattern: "finalizeAiReviewSession"
    - from: "src/aiReviewSessionStore.ts"
      to: "src/openQuestionStore.ts"
      via: "selected finding promotion creates normal issue markdown"
      pattern: "createOpenQuestion"
---

<objective>
Wire the AIR-01 chapter review workflow into the extension UI and command surface.

Purpose: Phase 10's primary UX is author-triggered chapter review from the chapter tree, with command/chat parity for power users and auditable stubs before model work.
Output: `leanquill.reviewChapter` command, package contributions, persona QuickPick, and chat/LM fallback behavior.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
@docs/ai-integration.md
@docs/manuscript-safety.md
@src/extension.ts
@src/aiReviewWorkflow.ts
@src/aiReviewSessionStore.ts
@src/openQuestionStore.ts
@src/personaStore.ts
@src/chapterTree.ts
@src/outlineWebviewPanel.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add chapter review command and persona QuickPick</name>
  <files>src/extension.ts, test/storyChatExtensionCommands.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/extension.ts
    - C:/Dev/LQ/src/aiReviewWorkflow.ts
    - C:/Dev/LQ/src/personaStore.ts
    - C:/Dev/LQ/test/storyChatExtensionCommands.test.ts
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: extension source contains `leanquill.reviewChapter`, `prepareChapterReviewRun`, and `getEnabledPersonasForProject`.
    - Test 2: extension source contains QuickPick labels `Choose how to run this review`, `One enabled persona…`, `All enabled personas`, and `Select a persona`.
    - Test 3: extension source contains warning copy `No enabled personas in this project.`
  </behavior>
  <acceptance_criteria>
    - `rg -n "leanquill\\.reviewChapter|prepareChapterReviewRun|getEnabledPersonasForProject" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns at least 5 lines
    - `rg -n "Choose how to run this review|One enabled persona…|All enabled personas|Select a persona|No enabled personas in this project" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns at least 5 lines
    - `rg -n "Session file and chat log started for this review" C:/Dev/LQ/src/extension.ts` returns one line
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    In `src/extension.ts`, import `prepareChapterReviewRun`, `buildAiWorkflowFallbackQuery`, and `type PersonaScopeChoice` from `./aiReviewWorkflow`. Add helper `async function pickPersonaScopeForAiReview(personas: Array<{ id: string; name: string; type: string }>): Promise<PersonaScopeChoice | undefined>` inside `activate`. It must first show QuickPick with title `Choose how to run this review` and items `{ label: "One enabled persona…", value: "one" }` and `{ label: "All enabled personas", value: "all" }`. If `one`, show a second QuickPick titled `Select a persona`, with persona `label` set to `name`, `description` set to `id`, and `detail` set to `type`. Return `{ kind: "one", personaId }` or `{ kind: "all" }`. If no personas exist after `getEnabledPersonasForProject(rootPath)`, show warning message exactly `No enabled personas in this project. Enable personas in project.yaml or add persona files under .leanquill/personas/, then try again.` and do not create stubs.

    Register command `leanquill.reviewChapter`. Resolve `chapterRef` from argument `{ chapterPath?: string }`, from active editor path under `manuscript/`, or from `showInputBox` prompt `Chapter path (e.g. manuscript/ch01.md)`. If unresolved, show warning `LeanQuill: select a chapter in the tree or open a chapter file, then run Run chapter review again.`. After persona selection, call `prepareChapterReviewRun(rootPath, safeFileSystem, { chapterRef, personas: personaResult.personas, choice })`. Immediately show information message `Session file and chat log started for this review.`. Then build a fallback query using `buildAiWorkflowFallbackQuery(run)` and open chat with the existing `workbench.action.chat.newChat` then `workbench.action.chat.open` pattern. If `chat.open` fails, show `No chat model available`.

    Do not write manuscript files. Do not implement `master-issues.md`. Direct `vscode.lm.selectChatModels` can be added only if it keeps the same stub-first behavior and catches failures to use the chat fallback; otherwise use the existing chat-open fallback for this plan.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>`leanquill.reviewChapter` resolves a chapter, asks persona scope, creates audit stubs, and opens an advisory chat fallback without manuscript writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Package chapter review activation, palette, and tree context</name>
  <files>package.json, test/storyChatExtensionCommands.test.ts</files>
  <read_first>
    - C:/Dev/LQ/package.json
    - C:/Dev/LQ/test/storyChatExtensionCommands.test.ts
    - C:/Dev/LQ/src/outlineWebviewPanel.ts
    - C:/Dev/LQ/src/chapterTree.ts
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: package JSON includes `onCommand:leanquill.reviewChapter`.
    - Test 2: command contribution has title `LeanQuill: Run chapter review`.
    - Test 3: command palette menu includes `leanquill.reviewChapter` when `leanquill.workspaceLeanQuillReady`.
    - Test 4: webview/context or view/item/context contribution exposes title `Run chapter review` for the chapter/outline tree surface.
  </behavior>
  <acceptance_criteria>
    - `rg -n "onCommand:leanquill\\.reviewChapter|LeanQuill: Run chapter review|leanquill\\.reviewChapter" C:/Dev/LQ/package.json C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns at least 5 lines
    - `rg -n "Run chapter review" C:/Dev/LQ/package.json` returns at least one line
    - `rg -n "webview/context|view/item/context|leanquill\\.outlineTree|chapter" C:/Dev/LQ/package.json` returns at least 4 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    In `package.json`, add activation event `onCommand:leanquill.reviewChapter`. Add command contribution `{ "command": "leanquill.reviewChapter", "title": "LeanQuill: Run chapter review", "category": "LeanQuill", "icon": "$(comment-discussion)" }`. Add `commandPalette` menu entry for `leanquill.reviewChapter` with `when: "leanquill.workspaceLeanQuillReady"`.

    Add a primary tree/context entry consistent with the current outline webview surface: under `menus.webview/context`, add `{ "command": "leanquill.reviewChapter", "when": "webviewId == 'leanquill.outlineTree' && leanquill.workspaceLeanQuillReady", "group": "leanquill@10" }`. If implementation discovers a registered real TreeView chapter surface before editing, add an additional `view/item/context` entry for `viewItem == chapter`; keep the webview/context entry because `leanquill.outlineTree` is currently registered as a webview view. Do not remove existing `leanquill.chatAboutChapter`.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Chapter review is discoverable in Command Palette and chapter/outline context menu with UI-SPEC labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize review output and promote selected findings</name>
  <files>src/aiReviewWorkflow.ts, src/aiReviewSessionStore.ts, src/extension.ts, src/openQuestionStore.ts, package.json, test/aiReviewWorkflow.test.ts, test/aiReviewSessionStore.test.ts, test/storyChatExtensionCommands.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/aiReviewWorkflow.ts
    - C:/Dev/LQ/src/aiReviewSessionStore.ts
    - C:/Dev/LQ/src/storyChatLogStore.ts
    - C:/Dev/LQ/src/openQuestionStore.ts
    - C:/Dev/LQ/src/extension.ts
    - C:/Dev/LQ/package.json
    - C:/Dev/LQ/test/aiReviewWorkflow.test.ts
    - C:/Dev/LQ/test/aiReviewSessionStore.test.ts
    - C:/Dev/LQ/test/storyChatExtensionCommands.test.ts
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
    - C:/Dev/LQ/Imported/data-contracts/issue-schema.md
  </read_first>
  <behavior>
    - Test 1: `finalizeChapterReviewRun` calls `finalizeAiReviewSession` with two findings and rewrites the matching chat log with `issuesGenerated: 2`, `summary`, and `sessionIssueFile`.
    - Test 2: `promoteAiReviewFindingToIssue` creates `.leanquill/issues/{type}/{slug}.md` with `source` set to the session issue file path, `agent_profile` set to the finding persona, and `chapter_ref`/`span_hint` copied from the finding.
    - Test 3: extension command wiring includes `leanquill.saveChapterReviewResult` and `leanquill.promoteSessionFindingToIssue`.
  </behavior>
  <acceptance_criteria>
    - `rg -n "finalizeChapterReviewRun|finalizeAiReviewSession|issuesGenerated|sessionIssueFile" C:/Dev/LQ/src/aiReviewWorkflow.ts C:/Dev/LQ/test/aiReviewWorkflow.test.ts` returns at least 6 lines
    - `rg -n "promoteAiReviewFindingToIssue|createOpenQuestion|saveOpenQuestion|source" C:/Dev/LQ/src/aiReviewSessionStore.ts C:/Dev/LQ/test/aiReviewSessionStore.test.ts` returns at least 6 lines
    - `rg -n "leanquill\\.saveChapterReviewResult|LeanQuill: Save chapter review result|leanquill\\.promoteSessionFindingToIssue|LeanQuill: Promote session finding to issue" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/package.json C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns at least 6 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    Add `finalizeChapterReviewRun(rootPath, safeFs, input)` to `src/aiReviewWorkflow.ts`. The input must include `{ sessionId, chapterRef, personaIds, sessionIssueFile, summary, findings, endedAt?: string }`, where `findings` use the `AiReviewSessionFinding` shape from `src/aiReviewSessionStore.ts`. The function must call `finalizeAiReviewSession(rootPath, safeFs, { sessionId, sessionType: "chapter-review", chapterRef, personaIds, generatedAt: endedAt ?? new Date().toISOString(), issueCount: findings.length, summary, findings })`, then call `saveStoryChatLogSummary` for `.leanquill/chats/{sessionId}.md` with `sessionType: "chapter-review"`, `issuesGenerated: findings.length`, `issuesResolved: 0`, `sessionIssueFile`, `summary`, `personaIds`, and `chaptersInContext: [chapterRef]`. This directly fixes AIR-01: stubs are not enough; completed review output must be persisted.

    Add `promoteAiReviewFindingToIssue(rootPath, safeFs, input)` to `src/aiReviewSessionStore.ts` or a narrow adjacent helper if imports would cycle. The input must include `{ sessionIssueFile, findingId, issueType, titleOverride?: string }`. It must parse the session file, find the selected finding, call `createOpenQuestion(safeFs, rootPath, { title, issueType, association })`, then call `saveOpenQuestion` with body copied from the finding and metadata preserving AI provenance: `source` must serialize as the session issue file path, `agent_profile` as the finding `agentProfile`, `confidence`, `verify_manually`, `chapter_ref`, and `span_hint`. If `serializeOpenQuestionFile` cannot currently accept source/agent fields, extend `OpenQuestionRecord` with optional AI provenance fields and keep author-created issue defaults unchanged. Do not list raw `sessions/*.md` as triage rows.

    In `src/extension.ts`, add command `leanquill.saveChapterReviewResult` for chat fallback/manual result capture. It should accept selected JSON or an input box containing `{ sessionId, chapterRef, personaIds, sessionIssueFile, summary, findings }`, validate parse errors with a warning, call `finalizeChapterReviewRun`, and show `LeanQuill: chapter review session saved.`. Add command `leanquill.promoteSessionFindingToIssue` that prompts for session issue file path, finding id, and issue type/title as needed, calls `promoteAiReviewFindingToIssue`, opens the created issue markdown, and refreshes issue surfaces. Add package activation/command contribution for both commands; use titles `LeanQuill: Save chapter review result` and `LeanQuill: Promote session finding to issue` per UI-SPEC. This implements D-02 and keeps `master-issues.md` out of scope per D-17.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Chapter review produces finalized session issue output plus updated chat log, and selected findings can be promoted into normal typed triage issues.</done>
</task>

</tasks>

<verification>
Run `cd C:/Dev/LQ && npm run build:test && npm test`. Manual later: right-click the outline/chapter tree, choose `Run chapter review`, choose one/all persona, verify both stubs are created, save/finalize review results, and promote one finding into a typed issue under `.leanquill/issues/{type}/`.
</verification>

<success_criteria>
- AIR-01 is executable from primary tree UX and command palette.
- Persona choice is always author-driven.
- Review sessions persist final findings and chat-log counters.
- Selected findings can be promoted into normal triage issues per D-02.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-review-and-advisory-workflows/10-04-SUMMARY.md`
</output>
