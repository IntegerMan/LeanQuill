---
phase: 10-ai-review-and-advisory-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/aiReviewWorkflow.ts
  - test/aiReviewWorkflow.test.ts
autonomous: true
requirements:
  - AIR-01
  - AIR-02
must_haves:
  truths:
    - "Chapter review and story intelligence workflows share one persona-selection and stub-before-model orchestration layer."
    - "Every run asks the author to choose `one` enabled persona or `all` enabled personas before creating session/chat stubs."
    - "If no model is available or direct LM fails, the workflow still leaves audit stubs and returns a chat-open fallback query instead of calling external providers."
  artifacts:
    - path: "src/aiReviewWorkflow.ts"
      provides: "Pure workflow orchestration helpers and VS Code adapter interfaces for AI advisory runs"
      exports: ["pickPersonaScope", "prepareChapterReviewRun", "prepareStoryIntelligenceRun", "buildAiWorkflowFallbackQuery"]
    - path: "test/aiReviewWorkflow.test.ts"
      provides: "Coverage for persona scope, stubs-before-invocation, fallback payloads, and no external provider assumptions"
  key_links:
    - from: "src/aiReviewWorkflow.ts"
      to: "src/aiReviewSessionStore.ts"
      via: "session stub path returned before invocation"
      pattern: "saveAiReviewSessionStub"
    - from: "src/aiReviewWorkflow.ts"
      to: "src/storyChatLogStore.ts"
      via: "chat log stub path returned before invocation"
      pattern: "saveStoryChatLogSummary"
---

<objective>
Build the shared AI workflow orchestration layer used by chapter review and post-write story intelligence.

Purpose: The extension should not duplicate persona selection, audit stub creation, or LM/chat fallback behavior across AIR-01 and AIR-02.
Output: `aiReviewWorkflow.ts` with deterministic helpers and tests that later extension wiring can call.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
@src/aiReviewSessionStore.ts
@src/storyChatLogStore.ts
@src/personaStore.ts
@src/harnessChatDraft.ts
@src/safeFileSystem.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persona scope and advisory run preparation</name>
  <files>src/aiReviewWorkflow.ts, test/aiReviewWorkflow.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/aiReviewWorkflow.ts
    - C:/Dev/LQ/test/aiReviewWorkflow.test.ts
    - C:/Dev/LQ/src/personaStore.ts
    - C:/Dev/LQ/src/aiReviewSessionStore.ts
    - C:/Dev/LQ/src/storyChatLogStore.ts
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `selectPersonaIdsForScope([{id:"casual-reader"}, {id:"copy-editor"}], {kind:"all"})` returns `["casual-reader","copy-editor"]`.
    - Test 2: `selectPersonaIdsForScope(..., {kind:"one", personaId:"copy-editor"})` returns `["copy-editor"]`.
    - Test 3: `prepareChapterReviewRun` returns `sessionType: "chapter-review"`, `chapterRef`, `personaIds`, `sessionIssueFile`, `chatLogFile`, and `chaptersInContext: [chapterRef]`.
    - Test 4: `prepareStoryIntelligenceRun` returns `sessionType: "story-intelligence-update"` and `storyUpdatesMade: false` in the chat stub.
  </behavior>
  <acceptance_criteria>
    - `rg -n "export type PersonaScopeChoice|selectPersonaIdsForScope|prepareChapterReviewRun|prepareStoryIntelligenceRun" C:/Dev/LQ/src/aiReviewWorkflow.ts` returns at least 4 lines
    - `rg -n "chapter-review|story-intelligence-update|sessionIssueFile|chatLogFile|personaIds" C:/Dev/LQ/test/aiReviewWorkflow.test.ts` returns at least 5 lines
    - `rg -n "saveAiReviewSessionStub|saveStoryChatLogSummary" C:/Dev/LQ/src/aiReviewWorkflow.ts` returns at least 2 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    Create `src/aiReviewWorkflow.ts`. Export `type PersonaScopeChoice = { kind: "all" } | { kind: "one"; personaId: string }`. Export `selectPersonaIdsForScope(personas, choice)` where `personas` is an array containing at least `{ id: string }`. For `all`, return all ids in input order. For `one`, return exactly the matching id or throw `Persona not enabled: {personaId}`.

    Export `prepareChapterReviewRun(rootPath, safeFs, input)` with input `{ chapterRef: string; personas: Array<{ id: string }>; choice: PersonaScopeChoice; now?: Date }`. It must compute `sessionId = createAiWorkflowSessionId("chapter-review", now)`, call `saveAiReviewSessionStub` first, then call `saveStoryChatLogSummary` with `sessionType: "chapter-review"`, `launchedFrom: "chapter-review"`, `chapterRef`, `chaptersInContext: [chapterRef]`, `personaIds`, `issuesGenerated: 0`, `issuesResolved: 0`, `sessionIssueFile`, `storyUpdatesMade: false`, and `summary: "Session started."`. Return `{ sessionId, sessionType: "chapter-review", personaIds, chapterRef, sessionIssueFile, chatLogFile }`.

    Export `prepareStoryIntelligenceRun(rootPath, safeFs, input)` with input `{ chapterRef: string; personas: Array<{ id: string }>; choice: PersonaScopeChoice; now?: Date }`. It must use `sessionType: "story-intelligence-update"`, `launchedFrom: "story-intelligence-update"`, `chaptersInContext: [chapterRef]`, and the same stub-before-run order. Do not perform metadata writes in this helper.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Shared run preparation creates both durable stubs with the same session id and selected persona ids before any model/chat step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fallback query and invocation result contracts</name>
  <files>src/aiReviewWorkflow.ts, test/aiReviewWorkflow.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/aiReviewWorkflow.ts
    - C:/Dev/LQ/test/aiReviewWorkflow.test.ts
    - C:/Dev/LQ/src/harnessChatDraft.ts
    - C:/Dev/LQ/docs/ai-integration.md
    - C:/Dev/LQ/docs/manuscript-safety.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `buildAiWorkflowFallbackQuery` for chapter review includes `Session issue file: .leanquill/issues/sessions/`, `Chat log file: .leanquill/chats/`, `Manuscript files are read-only`, and `Do not write manuscript files`.
    - Test 2: story-intelligence fallback includes `MetadataAction`, `requiresApproval`, and `authorApproval`.
    - Test 3: exported result type has `mode: "direct-lm" | "chat-fallback"` without adding external provider URLs or API keys.
  </behavior>
  <acceptance_criteria>
    - `rg -n "buildAiWorkflowFallbackQuery|AiWorkflowInvocationResult|chat-fallback|direct-lm" C:/Dev/LQ/src/aiReviewWorkflow.ts` returns at least 4 lines
    - `rg -n "Session issue file|Chat log file|Manuscript files are read-only|MetadataAction|requiresApproval|authorApproval" C:/Dev/LQ/test/aiReviewWorkflow.test.ts` returns at least 6 lines
    - `rg -n "openai|anthropic|apiKey|api_key|fetch\\(" C:/Dev/LQ/src/aiReviewWorkflow.ts` returns 0 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    In `src/aiReviewWorkflow.ts`, export `type AiWorkflowInvocationResult = { mode: "direct-lm"; sessionId: string; summary: string } | { mode: "chat-fallback"; sessionId: string; query: string }`. Export `buildAiWorkflowFallbackQuery(run)` where `run` accepts the return shape from `prepareChapterReviewRun` or `prepareStoryIntelligenceRun`.

    The fallback query must include concrete lines:
    - `LeanQuill advisory workflow: {sessionType}`
    - `Session id: {sessionId}`
    - `Session issue file: {sessionIssueFile}`
    - `Chat log file: {chatLogFile}`
    - `Chapter in context: {chapterRef}`
    - `Persona ids: {comma-separated personaIds}`
    - `Manuscript files are read-only. Do not write manuscript files.`

    For `story-intelligence-update`, add: `If proposing story-note updates, output MetadataAction JSON with risk "requiresApproval" and authorApproval. Do not apply note changes directly.` Keep the function pure and do not import `vscode` in this file. Do not add external HTTP calls or API key handling.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Fallback query contract is deterministic, safety-forward, and independent of external providers.</done>
</task>

</tasks>

<verification>
Run `cd C:/Dev/LQ && npm run build:test && npm test`. Static acceptance checks must prove no external provider integration and that run preparation creates audit stubs before model/chat invocation.
</verification>

<success_criteria>
- Chapter review and story intelligence share one tested preparation layer.
- Persona scope is explicit and author-driven.
- No-model fallback preserves audit paths and manuscript safety instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-review-and-advisory-workflows/10-02-SUMMARY.md`
</output>
