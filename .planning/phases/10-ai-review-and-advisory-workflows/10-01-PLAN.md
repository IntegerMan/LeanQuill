---
phase: 10-ai-review-and-advisory-workflows
plan: 01
type: execute
wave: 1
depends_on:
  - "10-00"
files_modified:
  - src/aiReviewSessionStore.ts
  - src/storyChatLogStore.ts
  - test/aiReviewSessionStore.test.ts
  - test/storyChatLogStore.test.ts
autonomous: true
requirements:
  - AIR-01
  - AIR-02
  - AIR-03
must_haves:
  truths:
    - "AI workflow run start can persist `.leanquill/issues/sessions/{session_id}.md` and `.leanquill/chats/{session_id}.md` stubs with the same `session_id` before model/chat work."
    - "Session issue files are stored under `.leanquill/issues/sessions/` and are not parsed by `listOpenQuestions`."
    - "Chat logs serialize schema-aligned fields for `chapter-review`, `story-intelligence-update`, and `issue-planning` while preserving backward-compatible reads of existing story-chat logs."
  artifacts:
    - path: "src/aiReviewSessionStore.ts"
      provides: "Session issue artifact serialization, stub/finalization helpers, finding parse/promotion source helpers"
      contains: "LEANQUILL_AI_REVIEW_SESSIONS_DIR"
    - path: "src/storyChatLogStore.ts"
      provides: "Schema-aligned chat log summary fields and backward-compatible parser"
      contains: "session_type"
    - path: "test/aiReviewSessionStore.test.ts"
      provides: "Round-trip tests for session stubs, finalization, finding parse, and sessions directory"
    - path: "test/storyChatLogStore.test.ts"
      provides: "Chat log schema field and backward-compatibility coverage"
  key_links:
    - from: "src/aiReviewSessionStore.ts"
      to: "src/storyChatLogStore.ts"
      via: "shared sessionId supplied by workflows"
      pattern: "sessionId"
    - from: "src/openQuestionStore.ts"
      to: "src/aiReviewSessionStore.ts"
      via: "session files stay under skipped `sessions` folder; promotion later creates normal issues"
      pattern: "sessions"
---

<objective>
Create the durable audit artifact foundation for Phase 10 AI workflows.

Purpose: AIR-01/AIR-02/AIR-03 require reproducible local artifacts before advisory model work starts, and D-04/D-14 require session issue files and chat logs to share the same `session_id`.
Output: New AI review session store plus schema-aligned chat log serialization/parser extensions.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-VALIDATION.md
@Imported/data-contracts/issue-schema.md
@Imported/data-contracts/chat-log-schema.md
@src/openQuestionStore.ts
@src/storyChatLogStore.ts
@src/safeFileSystem.ts
@test/storyChatLogStore.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add session issue artifact store</name>
  <files>src/aiReviewSessionStore.ts, test/aiReviewSessionStore.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/aiReviewSessionStore.ts
    - C:/Dev/LQ/test/aiReviewSessionStore.test.ts
    - C:/Dev/LQ/src/openQuestionStore.ts
    - C:/Dev/LQ/src/safeFileSystem.ts
    - C:/Dev/LQ/Imported/data-contracts/issue-schema.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `createAiWorkflowSessionId("chapter-review", new Date(2026, 3, 25, 9, 7, 5))` returns `2026-04-25-090705-chapter-review`.
    - Test 2: `saveAiReviewSessionStub` writes `.leanquill/issues/sessions/{sessionId}.md` with `session_id`, `session_type`, `chapter_ref`, `persona_ids`, `generated_at`, `issue_count: 0`, and body text `Session started.`
    - Test 3: `finalizeAiReviewSession` rewrites the same file with `issue_count: 2` and two `### Finding` sections.
    - Test 4: `parseAiReviewSessionFile` round-trips `id`, `type`, `title`, `body`, `spanHint`, `agentProfile`, `confidence`, and `verifyManually`.
  </behavior>
  <acceptance_criteria>
    - `rg -n "export const LEANQUILL_AI_REVIEW_SESSIONS_DIR" C:/Dev/LQ/src/aiReviewSessionStore.ts` returns one line containing `.leanquill/issues/sessions`
    - `rg -n "createAiWorkflowSessionId|saveAiReviewSessionStub|finalizeAiReviewSession|parseAiReviewSessionFile" C:/Dev/LQ/src/aiReviewSessionStore.ts` returns at least 4 lines
    - `rg -n "Session started\\.|issue_count: 0|### Finding|session_type" C:/Dev/LQ/test/aiReviewSessionStore.test.ts` returns at least 4 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    Create `src/aiReviewSessionStore.ts`. Export `LEANQUILL_AI_REVIEW_SESSIONS_DIR = ".leanquill/issues/sessions"`. Export `type AiWorkflowSessionType = "chapter-review" | "story-intelligence-update" | "issue-planning"`. Export `interface AiReviewSessionFinding` with concrete fields: `id`, `type`, `status`, `priority`, `title`, `createdAt`, `source`, `chapterRef`, `spanHint`, `agentProfile`, `evidenceLinks`, `confidence`, `verifyManually`, `intentional`, `intentionalNote`, `dismissedReason`, `body`. Export `interface AiReviewSessionSummary` with `sessionId`, `sessionType`, `chapterRef`, `personaIds`, `generatedAt`, `issueCount`, `summary`, `findings`.

    Implement `createAiWorkflowSessionId(sessionType, now = new Date())` using the existing local padded date style from `createStoryChatSessionId`, but suffix with the provided session type. Implement `serializeAiReviewSession(summary)` as markdown with YAML frontmatter keys `session_id`, `session_type`, `chapter_ref`, `persona_ids` as JSON string, `generated_at`, `issue_count`, followed by summary text and `### Finding {id}` sections with fenced YAML frontmatter and body. Use `.leanquill/issues/sessions/{sessionId}.md`, never `.leanquill/issues/master-issues.md` per D-17.

    Implement `saveAiReviewSessionStub(rootPath, safeFs, input)` that writes a file immediately with `issueCount: 0`, `summary: "Session started."`, and empty `findings`. Implement `finalizeAiReviewSession(rootPath, safeFs, summary)` that writes the final serialized content to the same session path. Implement `parseAiReviewSessionFile(fileName, markdown)` for the narrow serializer output only. Do not make this parser responsible for future `master-issues.md` rollup formats.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Session issue store exists, writes stubs/final files under `.leanquill/issues/sessions/`, round-trips findings, and leaves normal issue listing semantics unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend chat log schema fields without breaking old logs</name>
  <files>src/storyChatLogStore.ts, test/storyChatLogStore.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/storyChatLogStore.ts
    - C:/Dev/LQ/test/storyChatLogStore.test.ts
    - C:/Dev/LQ/Imported/data-contracts/chat-log-schema.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: serialization can produce `session_type: chapter-review`, `persona_ids`, `issues_generated`, `issues_resolved`, and `session_issue_file`.
    - Test 2: serialization can produce `session_type: story-intelligence-update`, `story_updates_made: true`, and non-empty `story_update_notes`.
    - Test 3: reading an existing Phase 17-style log with only `launched_from`, `memory_entry_ids`, and `metadata_action_ids` still returns a usable `StoryChatLogSummary`.
  </behavior>
  <acceptance_criteria>
    - `rg -n "sessionType\\?|personaIds\\?|issuesGenerated\\?|issuesResolved\\?|sessionIssueFile\\?|storyUpdateNotes\\?" C:/Dev/LQ/src/storyChatLogStore.ts` returns at least 6 lines
    - `rg -n "session_type: \\$\\{|persona_ids|issues_generated|session_issue_file|story_update_notes" C:/Dev/LQ/src/storyChatLogStore.ts` returns at least 5 lines
    - `rg -n "chapter-review|story-intelligence-update|backward" C:/Dev/LQ/test/storyChatLogStore.test.ts` returns at least 3 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    Extend `StoryChatLogSummary` in `src/storyChatLogStore.ts` with optional fields `sessionType?: "story-chat" | "chapter-review" | "story-intelligence-update" | "issue-planning" | string`, `personaIds?: string[]`, `issuesGenerated?: number`, `issuesResolved?: number`, `sessionIssueFile?: string`, `storyUpdatesMade?: boolean`, and `storyUpdateNotes?: string`. Preserve required existing fields so existing callers compile unchanged.

    Update `serializeStoryChatLogSummary` to write `session_type: ${summary.sessionType ?? "story-chat"}`, `persona_ids` as a JSON string array, `issues_generated`, `issues_resolved`, `session_issue_file`, `story_updates_made`, and `story_update_notes`. Keep existing `launched_from`, `memory_entry_ids`, and `metadata_action_ids` keys for backward compatibility.

    Update `readStoryChatLogSummaryFromDisk` to parse both old and new fields. If `persona_ids`, `memory_entry_ids`, or `metadata_action_ids` are absent or malformed, return empty arrays. If `session_type` is absent, default to `story-chat`. Do not require transcript body; D-16 says transcript capture is optional and summary-first.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Chat log store writes Phase 10 schema-aligned audit fields and continues to parse pre-Phase-10 chat logs.</done>
</task>

</tasks>

<verification>
Run `cd C:/Dev/LQ && npm run build:test && npm test`. Static acceptance greps prove D-04, D-05, D-14, D-16, and D-17 constraints are represented in deterministic store code.
</verification>

<success_criteria>
- `.leanquill/issues/sessions/` stubs/finalizations can be created with a stable `session_id`.
- `.leanquill/chats/` logs can carry schema-aligned fields for all Phase 10 workflow types.
- No plan or code path introduces `master-issues.md` automation in Phase 10.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-review-and-advisory-workflows/10-01-SUMMARY.md`
</output>
