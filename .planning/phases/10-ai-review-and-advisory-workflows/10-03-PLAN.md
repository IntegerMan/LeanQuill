---
phase: 10-ai-review-and-advisory-workflows
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/storyChatContext.ts
  - src/extension.ts
  - src/openQuestionsHtml.ts
  - src/openQuestionsPanel.ts
  - src/planningPanel.ts
  - package.json
  - test/storyChatContext.test.ts
  - test/openQuestionsHtml.test.ts
autonomous: true
requirements:
  - ISSUE-05
  - AIR-03
must_haves:
  truths:
    - "Issue rows expose a `Chat about this` action in both Planning Issues and bottom-panel Issues hosts."
    - "`leanquill.chatAboutIssue` includes the issue markdown plus full chapter for `chapter` and `selection` associations."
    - "`selection` issue chat preserves `spanHint` as an anchor and does not introduce a partial chapter slice contract."
    - "Non-manuscript issue chat includes issue markdown plus linked character/place/thread/research target file when applicable; book issues rely on the base bundle."
    - "Issue chat remains advisory-only and includes active story memory through the existing story chat context bundle."
  artifacts:
    - path: "src/storyChatContext.ts"
      provides: "Association-aware issue chat path helper"
      exports: ["pathsForIssueChat"]
    - path: "src/openQuestionsHtml.ts"
      provides: "Shared `Chat about this` row/context action and postMessage protocol"
      contains: "openQuestion:chatAboutThis"
    - path: "src/extension.ts"
      provides: "Command wiring for scoped issue chat"
      contains: "leanquill.chatAboutIssue"
  key_links:
    - from: "src/openQuestionsHtml.ts"
      to: "src/openQuestionsPanel.ts"
      via: "postMessage `openQuestion:chatAboutThis`"
      pattern: "openQuestion:chatAboutThis"
    - from: "src/extension.ts"
      to: "src/storyChatContext.ts"
      via: "`pathsForIssueChat(issue, config)` passed to `openStoryChatWithContext`"
      pattern: "pathsForIssueChat"
---

<objective>
Implement ISSUE-05 and AIR-03 issue-focused advisory chat.

Purpose: The author needs a one-action path from any issue to scoped AI discussion, and the injected context must be complete enough to advise without writing manuscript prose.
Output: Association-aware issue chat context plus shared Issues UI action in both webview hosts.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
@docs/manuscript-safety.md
@src/storyChatContext.ts
@src/extension.ts
@src/openQuestionsHtml.ts
@src/openQuestionsPanel.ts
@src/planningPanel.ts
@src/openQuestionStore.ts
@src/types.ts
@src/projectConfig.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Association-aware issue chat context paths</name>
  <files>src/storyChatContext.ts, src/extension.ts, test/storyChatContext.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/storyChatContext.ts
    - C:/Dev/LQ/src/extension.ts
    - C:/Dev/LQ/test/storyChatContext.test.ts
    - C:/Dev/LQ/src/openQuestionStore.ts
    - C:/Dev/LQ/src/types.ts
    - C:/Dev/LQ/src/projectConfig.ts
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: chapter issue paths equal `[ ".leanquill/issues/question/q1.md", "manuscript/ch01.md" ]`.
    - Test 2: selection issue paths include full `chapterRef` and the summary contains `Selection span: selected phrase`.
    - Test 3: character/place/thread/research issue paths include configured folder target file names.
    - Test 4: book issue paths include only the issue markdown beyond the standard base bundle.
  </behavior>
  <acceptance_criteria>
    - `rg -n "export function pathsForIssueChat|association.kind === \"chapter\"|association.kind === \"selection\"|folders.characters|folders.settings|folders.threads|folders.research" C:/Dev/LQ/src/storyChatContext.ts` returns at least 7 lines
    - `rg -n "pathsForIssueChat" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatContext.test.ts` returns at least 3 lines
    - `rg -n "manuscript/ch01\\.md|notes/characters/hero\\.md|notes/settings/dock\\.md|notes/threads/main\\.md|research/leanquill/r\\.md" C:/Dev/LQ/test/storyChatContext.test.ts` returns at least 5 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    In `src/storyChatContext.ts`, import `ProjectConfig` type and `OpenQuestionRecord` type. Export `function pathsForIssueChat(issue: OpenQuestionRecord, config: ProjectConfig): string[]`. The first path must always be `.leanquill/issues/${issue.fileName}`. For `chapter` and `selection`, append `issue.association.chapterRef`. For `character`, append `${config.folders.characters.replace(/\/+$/, "")}/${issue.association.fileName}`. For `place`, append `${config.folders.settings.replace(/\/+$/, "")}/${issue.association.fileName}`. For `thread`, append `${config.folders.threads.replace(/\/+$/, "")}/${issue.association.fileName}`. For `research`, append `${config.folders.research.replace(/\/+$/, "")}/${issue.association.fileName}`. For `book`, append no extra paths. Return normalized forward-slash paths and dedupe while preserving first occurrence order.

    In `src/extension.ts`, update `leanquill.chatAboutIssue` to load `const cfg = await readProjectConfigWithDefaults(rootPath)` before computing included paths. Replace the current `const included = [\`.leanquill/issues/${issue.fileName}\`]` with `const included = pathsForIssueChat(issue, cfg)`. For target details, set `target.chapterRef` and `target.spanHint` when `issue.association.kind === "selection"`, and set `target.chapterRef` when `kind === "chapter"`. Keep `manuscriptScopeForIssueAssociation` returning `"chapter"` for both chapter and selection so full chapter paths are retained. Do not add a partial-chapter slice contract.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Issue chat context includes required issue/chapter/entity paths and preserves selection span anchors while remaining advisory-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared Issues UI `Chat about this` action</name>
  <files>src/openQuestionsHtml.ts, src/openQuestionsPanel.ts, src/planningPanel.ts, package.json, test/openQuestionsHtml.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/openQuestionsHtml.ts
    - C:/Dev/LQ/src/openQuestionsPanel.ts
    - C:/Dev/LQ/src/planningPanel.ts
    - C:/Dev/LQ/package.json
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
    - C:/Dev/LQ/.planning/phases/08-issue-capture-triage-and-editor-signals/08-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: rendered issue HTML contains button text `Chat about this`.
    - Test 2: row context menu includes a `data-ctx="chat-about-this"` item.
    - Test 3: client script posts `openQuestion:chatAboutThis` with `{ id }`.
  </behavior>
  <acceptance_criteria>
    - `rg -n "openQuestion:chatAboutThis|Chat about this|chat-about-this" C:/Dev/LQ/src/openQuestionsHtml.ts C:/Dev/LQ/test/openQuestionsHtml.test.ts` returns at least 6 lines
    - `rg -n "leanquill\\.chatAboutIssue" C:/Dev/LQ/src/openQuestionsPanel.ts C:/Dev/LQ/src/planningPanel.ts` returns at least 2 lines
    - `rg -n "\"command\": \"leanquill.chatAboutIssue\"|\"title\": \"Chat about this\"" C:/Dev/LQ/package.json` returns at least 2 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    In `src/openQuestionsHtml.ts`, extend the documented postMessage protocol comment with `openQuestion:chatAboutThis — { type, host, id }`. Add a secondary row action button in the Description column after the preview line: `<button type="button" class="oq-inline-action" data-action="chat-about-this" data-question-id="{id}">Chat about this</button>`. Add CSS for `.oq-inline-action` using font-size `12px`, margin-top `4px`, color `var(--vscode-textLink-foreground)`, transparent background, no border, and underline on hover. In the custom context menu, add a non-danger item labeled `Chat about this` with `data-ctx="chat-about-this"` between `Open source` and `Edit status…`. In the click handlers, post `openQuestion:chatAboutThis` with the issue id for both the inline button and the context menu item.

    In `src/openQuestionsPanel.ts`, handle message type `openQuestion:chatAboutThis` by executing `this.vscodeApi.commands.executeCommand("leanquill.chatAboutIssue", { id: String(msg.id ?? "") })`. In `src/planningPanel.ts`, add the same message branch in `_handleMessage`. Update `package.json` command contribution title for `leanquill.chatAboutIssue` to exactly `Chat about this` to match UI-SPEC copy; keep the command id unchanged.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Both issue webview hosts expose `Chat about this` and route to the existing scoped issue chat command.</done>
</task>

</tasks>

<verification>
Run `cd C:/Dev/LQ && npm run build:test && npm test`. Manual later: right-click an issue row in Planning Issues and bottom panel; both must open `leanquill.chatAboutIssue` with the selected issue id.
</verification>

<success_criteria>
- ISSUE-05 is satisfied through row/context-menu entry points.
- AIR-03 is satisfied through issue-specific context including issue markdown, relevant chapter or linked target, and active story memory.
- No code path writes manuscript files as part of issue chat.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-review-and-advisory-workflows/10-03-SUMMARY.md`
</output>
