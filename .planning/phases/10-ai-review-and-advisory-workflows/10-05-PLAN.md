---
phase: 10-ai-review-and-advisory-workflows
plan: 05
type: execute
wave: 4
depends_on:
  - "10-02"
  - "10-04"
files_modified:
  - src/extension.ts
  - src/aiReviewWorkflow.ts
  - src/metadataActionContract.ts
  - src/metadataActionApplier.ts
  - src/metadataActionLog.ts
  - package.json
  - test/aiReviewWorkflow.test.ts
  - test/storyChatExtensionCommands.test.ts
  - test/metadataActionLog.test.ts
  - test/metadataActionContract.test.ts
  - test/metadataActionApplier.test.ts
autonomous: true
requirements:
  - AIR-02
must_haves:
  truths:
    - "Saving a manuscript chapter never starts AI automatically; it only shows a non-blocking prompt with `Run` and `Dismiss`."
    - "The explicit command `LeanQuill: Run story intelligence update for active chapter` runs the same workflow as the save prompt `Run` action."
    - "Story intelligence input scope is the entire active chapter manuscript file."
    - "Story-note updates are represented as `MetadataAction` JSON with `risk: \"requiresApproval\"` and `authorApproval`; direct AI note writes are not introduced."
    - "Approved story-intelligence MetadataActions are applied through `applyMetadataAction`, creating note or memory backlinks while manuscript targets are blocked."
    - "Metadata action JSONL audit remains outcome-only with `applied`, `rejected`, and `blocked`, not `proposed`."
  artifacts:
    - path: "src/extension.ts"
      provides: "Debounced save prompt and explicit story intelligence command"
      contains: "leanquill.runStoryIntelligenceUpdate"
    - path: "src/aiReviewWorkflow.ts"
      provides: "Story intelligence fallback prompt instructions for approved MetadataAction JSON"
      contains: "story-intelligence-update"
    - path: "src/metadataActionLog.ts"
      provides: "Outcome-only metadata action log status contract"
      contains: "applied"
    - path: "src/metadataActionApplier.ts"
      provides: "Approved story-note backlink application and manuscript blocking"
      contains: "applyMetadataAction"
  key_links:
    - from: "src/extension.ts"
      to: "src/aiReviewWorkflow.ts"
      via: "`prepareStoryIntelligenceRun` after user clicks Run or command"
      pattern: "prepareStoryIntelligenceRun"
    - from: "src/aiReviewWorkflow.ts"
      to: "src/metadataActionContract.ts"
      via: "fallback prompt names `MetadataAction`, `requiresApproval`, `authorApproval`"
      pattern: "requiresApproval"
    - from: "src/extension.ts"
      to: "src/metadataActionApplier.ts"
      via: "`leanquill.applyMetadataAction` applies approved story intelligence output"
      pattern: "applyMetadataAction"
---

<objective>
Implement AIR-02 post-write story intelligence workflow with explicit author consent and metadata-action safety.

Purpose: Authors can ask LeanQuill to analyze a saved chapter and propose story-note updates, but AI never auto-runs on save and never directly writes manuscript or notes.
Output: Debounced save prompt, explicit command parity, story-intelligence fallback query, and outcome-only metadata action logging contract.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
@docs/manuscript-safety.md
@src/extension.ts
@src/aiReviewWorkflow.ts
@src/metadataActionContract.ts
@src/metadataActionApplier.ts
@src/metadataActionLog.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Save prompt and explicit story intelligence command</name>
  <files>src/extension.ts, package.json, test/storyChatExtensionCommands.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/extension.ts
    - C:/Dev/LQ/src/aiReviewWorkflow.ts
    - C:/Dev/LQ/package.json
    - C:/Dev/LQ/test/storyChatExtensionCommands.test.ts
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: extension source contains `onDidSaveTextDocument`, `setTimeout`, `Run`, `Dismiss`, and `leanquill.runStoryIntelligenceUpdate`.
    - Test 2: extension source contains prompt body `You saved a chapter. Run a story intelligence pass on the full chapter? Proposed story-note updates require your approval. Manuscript files are never modified.`
    - Test 3: package JSON contains activation and command contribution for `leanquill.runStoryIntelligenceUpdate`.
  </behavior>
  <acceptance_criteria>
    - `rg -n "onDidSaveTextDocument|setTimeout|Run|Dismiss|leanquill\\.runStoryIntelligenceUpdate" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns at least 8 lines
    - `rg -n "You saved a chapter\\. Run a story intelligence pass on the full chapter\\?|Proposed story-note updates require your approval|Manuscript files are never modified" C:/Dev/LQ/src/extension.ts` returns at least 3 lines
    - `rg -n "onCommand:leanquill\\.runStoryIntelligenceUpdate|LeanQuill: Run story intelligence update for active chapter|leanquill\\.runStoryIntelligenceUpdate" C:/Dev/LQ/package.json C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns at least 4 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    In `src/extension.ts`, add command id `leanquill.runStoryIntelligenceUpdate`. The command accepts optional `{ chapterPath?: string }`, otherwise resolves active editor relative path. It must require a path starting with `manuscript/` and ending in `.md`; if absent, show warning exactly `LeanQuill: open a saved manuscript chapter, then run Run story intelligence update again.`. It must call the same persona picker helper created for chapter review, then call `prepareStoryIntelligenceRun(rootPath, safeFileSystem, { chapterRef, personas: personaResult.personas, choice })`, then open chat with `buildAiWorkflowFallbackQuery(run)` using the same chat-open fallback pattern as chapter review.

    Add debounced save prompt after workspace watchers are established. Use `let storyIntelligenceSaveTimer: ReturnType<typeof setTimeout> | undefined;` and delay `1500` ms. Subscribe to `vscode.workspace.onDidSaveTextDocument`. For saved documents under the workspace whose normalized relative path starts with `manuscript/` and ends with `.md`, reset the timer and show `vscode.window.showInformationMessage("You saved a chapter. Run a story intelligence pass on the full chapter? Proposed story-note updates require your approval. Manuscript files are never modified.", "Run", "Dismiss")`. If choice is `"Run"`, execute command `leanquill.runStoryIntelligenceUpdate` with `{ chapterPath: rel }`. If `"Dismiss"` or undefined, do nothing. Do not call `prepareStoryIntelligenceRun`, `vscode.lm`, or `workbench.action.chat.open` directly from the save event before the user chooses Run.

    In `package.json`, add activation event `onCommand:leanquill.runStoryIntelligenceUpdate`, command contribution title `LeanQuill: Run story intelligence update for active chapter`, and command palette menu entry gated by `leanquill.workspaceLeanQuillReady`.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Save prompt is consent-only, explicit command runs identical story-intelligence workflow, and both paths use whole active chapter context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Story-intelligence metadata-action prompt and outcome-only log</name>
  <files>src/aiReviewWorkflow.ts, src/metadataActionLog.ts, test/aiReviewWorkflow.test.ts, test/metadataActionLog.test.ts, test/metadataActionContract.test.ts, test/metadataActionApplier.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/aiReviewWorkflow.ts
    - C:/Dev/LQ/src/metadataActionLog.ts
    - C:/Dev/LQ/src/metadataActionContract.ts
    - C:/Dev/LQ/src/metadataActionApplier.ts
    - C:/Dev/LQ/test/aiReviewWorkflow.test.ts
    - C:/Dev/LQ/test/metadataActionLog.test.ts
    - C:/Dev/LQ/test/metadataActionContract.test.ts
    - C:/Dev/LQ/test/metadataActionApplier.test.ts
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: story intelligence fallback query includes `MetadataAction`, `risk "requiresApproval"`, `authorApproval`, and `Do not apply note changes directly.`
    - Test 2: `MetadataActionLogStatus` excludes `"proposed"` or tests prove no code appends `"status":"proposed"`.
    - Test 3: applying valid and blocked metadata actions still logs only `applied`, `rejected`, and `blocked`.
  </behavior>
  <acceptance_criteria>
    - `rg -n "risk \"requiresApproval\"|authorApproval|Do not apply note changes directly|MetadataAction" C:/Dev/LQ/src/aiReviewWorkflow.ts C:/Dev/LQ/test/aiReviewWorkflow.test.ts` returns at least 4 lines
    - `rg -n "\"proposed\"" C:/Dev/LQ/src/metadataActionLog.ts C:/Dev/LQ/test/metadataActionLog.test.ts C:/Dev/LQ/test/metadataActionApplier.test.ts` returns 0 lines
    - `rg -n "\"applied\" \\| \"rejected\" \\| \"blocked\"|status === \"applied\"|status === \"blocked\"" C:/Dev/LQ/src/metadataActionLog.ts C:/Dev/LQ/test/metadataActionApplier.test.ts` returns at least 3 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    In `src/aiReviewWorkflow.ts`, ensure the story-intelligence branch of `buildAiWorkflowFallbackQuery` includes exactly these instructional strings: `Output MetadataAction JSON`, `risk "requiresApproval"`, `authorApproval`, and `Do not apply note changes directly.` It must also say `Target story-note updates only to configured LeanQuill metadata roots such as notes/characters, notes/settings, notes/threads, research/leanquill, .leanquill/themes.yaml, or .leanquill/memory.` and `Never target manuscript/`.

    In `src/metadataActionLog.ts`, narrow `MetadataActionLogStatus` to `"applied" | "rejected" | "blocked"` unless another already-shipped test requires `"proposed"`. If narrowing creates compile errors, fix call sites by removing proposed-only expectations; do not add a proposed append path. Update tests so `rg -n "\"proposed\""` over the files listed in acceptance returns zero lines. Preserve existing `appendMetadataActionLogEntry` and `readMetadataActionLog` behavior for outcome statuses.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Story intelligence prompts for approved MetadataAction JSON only, and metadata action JSONL remains outcome-oriented with no proposed stream.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply approved story-note backlinks through MetadataAction</name>
  <files>src/extension.ts, src/metadataActionContract.ts, src/metadataActionApplier.ts, test/metadataActionContract.test.ts, test/metadataActionApplier.test.ts, test/storyChatExtensionCommands.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/extension.ts
    - C:/Dev/LQ/src/metadataActionContract.ts
    - C:/Dev/LQ/src/metadataActionApplier.ts
    - C:/Dev/LQ/src/metadataActionLog.ts
    - C:/Dev/LQ/src/characterStore.ts
    - C:/Dev/LQ/src/placeStore.ts
    - C:/Dev/LQ/src/threadStore.ts
    - C:/Dev/LQ/src/storyMemoryStore.ts
    - C:/Dev/LQ/test/metadataActionContract.test.ts
    - C:/Dev/LQ/test/metadataActionApplier.test.ts
    - C:/Dev/LQ/test/storyChatExtensionCommands.test.ts
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-RESEARCH.md
    - C:/Dev/LQ/.planning/phases/09-ai-safety-rails-and-persona-baseline/09-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: an approved `append` MetadataAction targeting `notes/characters/hero.md` field path `["customFields","chapter_backlinks"]` appends a line containing `manuscript/ch03.md` and returns `status: "applied"`.
    - Test 2: an approved `createMemory` MetadataAction with `sourceChatId` from a `story-intelligence-update` session creates a `.leanquill/memory/*.md` backlink record and returns `status: "applied"`.
    - Test 3: an approved action targeting `manuscript/ch03.md` returns `status: "blocked"` and appends a JSONL line with `"status":"blocked"`.
    - Test 4: low-risk story-note updates without `authorApproval` are rejected or blocked; story-note writes require `requiresApproval` per D-12.
  </behavior>
  <acceptance_criteria>
    - `rg -n "chapter_backlinks|manuscript/ch03\\.md|story-intelligence-update" C:/Dev/LQ/test/metadataActionApplier.test.ts C:/Dev/LQ/test/metadataActionContract.test.ts` returns at least 5 lines
    - `rg -n "applyMetadataAction|LeanQuill blocked this metadata action|Applied LeanQuill metadata action" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns at least 4 lines
    - `rg -n "\"status\":\"applied\"|\"status\":\"blocked\"|readMetadataActionLog" C:/Dev/LQ/test/metadataActionApplier.test.ts` returns at least 3 lines
    - `rg -n "\"proposed\"" C:/Dev/LQ/src/metadataActionLog.ts C:/Dev/LQ/test/metadataActionLog.test.ts C:/Dev/LQ/test/metadataActionApplier.test.ts` returns 0 lines
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    Extend tests around the existing `leanquill.applyMetadataAction` path to prove AIR-02 note updates actually happen after approval. In `test/metadataActionApplier.test.ts`, create a workspace with `notes/characters/hero.md`, valid `.leanquill/project.yaml`, and `SafeFileSystem` allowances. Apply this concrete action:
    `{ "schemaVersion":"1", "actionId":"siu-char-backlink-1", "sourceChatId":"2026-04-25-120000-story-intelligence-update", "operation":"append", "targetPath":"notes/characters/hero.md", "fieldPath":["customFields","chapter_backlinks"], "oldValue":"", "newValue":"- manuscript/ch03.md: appeared in the dock scene", "rationale":"Adds a source chapter backlink from story intelligence.", "risk":"requiresApproval", "authorApproval":{"approvedAt":"2026-04-25T12:10:00.000Z","method":"extension-command"} }`.
    Assert `status === "applied"` and the serialized character file contains `manuscript/ch03.md`.

    Add a second approved `createMemory` action whose `newValue` includes `{ "topic":"Chapter 3 entity updates", "body":"- manuscript/ch03.md: dock scene introduced the witness.", "association":{"kind":"chapter","chapterRef":"manuscript/ch03.md"} }`; assert a `.leanquill/memory/*.md` record exists and the log has `"status":"applied"`.

    Add/keep blocking tests for `targetPath: "manuscript/ch03.md"` and low-risk story-note writes without `authorApproval`; assert blocked/rejected status and JSONL outcome entries only. If extension static tests do not already cover `leanquill.applyMetadataAction`, add assertions that `src/extension.ts` still routes selected/pasted JSON to `applyMetadataAction` and shows `Applied LeanQuill metadata action.` / `LeanQuill blocked this metadata action...` messages. Do not add any direct note mutation path outside `applyMetadataAction`.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Approved story-intelligence MetadataActions update allowed note/memory backlinks, manuscript targets are blocked, and JSONL contains only outcome statuses.</done>
</task>

</tasks>

<verification>
Run `cd C:/Dev/LQ && npm run build:test && npm test`. Manual later: save a manuscript chapter, verify only a Run/Dismiss prompt appears; click Run, select persona scope, confirm `.leanquill/issues/sessions/` plus `.leanquill/chats/` stubs, then apply an approved story-note MetadataAction and verify note/memory backlinks are written while manuscript targets are blocked.
</verification>

<success_criteria>
- AIR-02 is available from save prompt and explicit command.
- Save events do not auto-run AI.
- Story-note changes are routed through approved `MetadataAction` JSON, applied through existing applier code, and outcome-audited only.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-review-and-advisory-workflows/10-05-SUMMARY.md`
</output>
