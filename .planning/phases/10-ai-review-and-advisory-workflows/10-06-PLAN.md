---
phase: 10-ai-review-and-advisory-workflows
plan: 06
type: execute
wave: 5
depends_on:
  - "10-03"
  - "10-04"
  - "10-05"
files_modified: []
autonomous: false
requirements:
  - AIR-01
  - AIR-02
  - AIR-03
  - ISSUE-05
must_haves:
  truths:
    - "Full automated suite and extension bundle build pass after Phase 10 implementation."
    - "Chapter review can be launched from tree/context and command palette, asks persona scope, creates matching session/chat stubs, finalizes findings, and updates chat-log counters."
    - "A selected session finding can be promoted into a normal typed triage issue under `.leanquill/issues/{type}/`."
    - "Story intelligence save prompt offers only Run/Dismiss and does not auto-run AI until Run is chosen."
    - "Approved story-intelligence MetadataActions write note/memory backlinks and manuscript targets are blocked."
    - "Issue `Chat about this` is available in both Issues webview hosts and injects issue plus full chapter/target context."
    - "No AI workflow modifies manuscript files; metadata actions targeting manuscript paths are blocked and logged as outcomes."
  artifacts:
    - path: "dist/extension.js"
      provides: "Built extension bundle for Extension Development Host verification"
    - path: "dist-test/"
      provides: "Bundled node:test files"
    - path: ".leanquill/issues/sessions/"
      provides: "Runtime session issue audit artifacts created during manual verification"
    - path: ".leanquill/chats/"
      provides: "Runtime chat log audit artifacts created during manual verification"
    - path: ".leanquill/issues/{type}/"
      provides: "Promoted typed triage issue created from selected session finding"
  key_links:
    - from: "package.json"
      to: "src/extension.ts"
      via: "registered Phase 10 commands activate handlers"
      pattern: "leanquill.reviewChapter|leanquill.runStoryIntelligenceUpdate|leanquill.chatAboutIssue"
    - from: "src/extension.ts"
      to: "src/metadataActionApplier.ts"
      via: "story intelligence outputs MetadataAction JSON applied by existing approval path"
      pattern: "applyMetadataAction"
    - from: "src/aiReviewWorkflow.ts"
      to: "src/aiReviewSessionStore.ts"
      via: "review result finalization calls `finalizeAiReviewSession`"
      pattern: "finalizeChapterReviewRun"
---

<objective>
Verify Phase 10 end-to-end with automated and Extension Development Host checks.

Purpose: Deterministic tests cover stores and helpers, but VS Code commands, QuickPick prompts, save notifications, chat opening, and context-menu UX need a final human checkpoint.
Output: Green automated gate plus human verification of AIR-01, AIR-02, AIR-03, and ISSUE-05.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-review-and-advisory-workflows/10-CONTEXT.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-VALIDATION.md
@.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
@docs/manuscript-safety.md
@package.json
@src/extension.ts
@src/aiReviewWorkflow.ts
@src/aiReviewSessionStore.ts
@src/storyChatContext.ts
@src/storyChatLogStore.ts
@src/metadataActionApplier.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated Phase 10 gate</name>
  <files>package.json, src/extension.ts, src/aiReviewWorkflow.ts, src/aiReviewSessionStore.ts, src/storyChatContext.ts, src/storyChatLogStore.ts, src/metadataActionApplier.ts</files>
  <read_first>
    - C:/Dev/LQ/package.json
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-VALIDATION.md
    - C:/Dev/LQ/src/extension.ts
    - C:/Dev/LQ/src/aiReviewWorkflow.ts
    - C:/Dev/LQ/src/aiReviewSessionStore.ts
    - C:/Dev/LQ/src/storyChatContext.ts
    - C:/Dev/LQ/src/storyChatLogStore.ts
    - C:/Dev/LQ/src/metadataActionApplier.ts
  </read_first>
  <acceptance_criteria>
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
    - `cd C:/Dev/LQ && npm run build` exits 0
    - `rg -n "leanquill\\.reviewChapter|leanquill\\.runStoryIntelligenceUpdate|leanquill\\.chatAboutIssue" C:/Dev/LQ/package.json C:/Dev/LQ/src/extension.ts` returns at least 6 lines
    - `rg -n "saveAiReviewSessionStub|finalizeAiReviewSession|saveStoryChatLogSummary|sessionIssueFile|chatLogFile|issuesGenerated" C:/Dev/LQ/src/aiReviewWorkflow.ts C:/Dev/LQ/src/aiReviewSessionStore.ts C:/Dev/LQ/src/storyChatLogStore.ts` returns at least 8 lines
    - `rg -n "promoteAiReviewFindingToIssue|leanquill\\.promoteSessionFindingToIssue|LeanQuill: Promote session finding to issue" C:/Dev/LQ/src C:/Dev/LQ/package.json C:/Dev/LQ/test` returns at least 5 lines
    - `rg -n "pathsForIssueChat|openQuestion:chatAboutThis|Chat about this" C:/Dev/LQ/src C:/Dev/LQ/test` returns at least 6 lines
    - `rg -n "onDidSaveTextDocument|Run|Dismiss|story intelligence pass" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns at least 5 lines
    - `rg -n "chapter_backlinks|story-intelligence-update|manuscript/ch03\\.md|applyMetadataAction" C:/Dev/LQ/test/metadataActionApplier.test.ts C:/Dev/LQ/test/metadataActionContract.test.ts C:/Dev/LQ/src/extension.ts` returns at least 6 lines
    - `rg -n "\"proposed\"" C:/Dev/LQ/src/metadataActionLog.ts C:/Dev/LQ/test/metadataActionLog.test.ts C:/Dev/LQ/test/metadataActionApplier.test.ts` returns 0 lines
  </acceptance_criteria>
  <action>
    Run the automated gate exactly:
    1. `cd C:/Dev/LQ && npm run build:test && npm test`
    2. `cd C:/Dev/LQ && npm run build`
    3. Run each `rg` acceptance check above.

    If any command fails, fix the owning implementation plan files before proceeding to the human checkpoint. Do not skip `npm run build:test`; `npm test` uses prebuilt `dist-test/` output.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test && npm run build</automated>
  </verify>
  <done>Automated tests, build, and static wiring checks pass for Phase 10.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual Extension Development Host verification</name>
  <files>package.json, src/extension.ts, src/aiReviewWorkflow.ts, src/storyChatContext.ts, src/metadataActionApplier.ts</files>
  <read_first>
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-UI-SPEC.md
    - C:/Dev/LQ/.planning/phases/10-ai-review-and-advisory-workflows/10-VALIDATION.md
    - C:/Dev/LQ/package.json
    - C:/Dev/LQ/src/extension.ts
    - C:/Dev/LQ/src/aiReviewWorkflow.ts
    - C:/Dev/LQ/src/storyChatContext.ts
    - C:/Dev/LQ/src/metadataActionApplier.ts
  </read_first>
  <acceptance_criteria>
    - Human reports command/context label `Run chapter review` is visible from the chapter/outline tree context surface.
    - Human reports command `LeanQuill: Run chapter review` asks `Choose how to run this review`.
    - Human reports choosing a persona creates one `.leanquill/issues/sessions/*.md` file and one `.leanquill/chats/*.md` file with matching `session_id`.
    - Human reports saving/finalizing review output updates the session issue with findings and updates the matching chat log with `issues_generated`.
    - Human reports promoting a selected finding creates a typed issue under `.leanquill/issues/{type}/`.
    - Human reports issue row action/context menu `Chat about this` appears in Planning Issues and bottom-panel Issues.
    - Human reports issue chat draft includes `Launch source: issue`, `Included paths:`, the issue markdown path, and the associated chapter or linked target path.
    - Human reports saving a manuscript `.md` file shows one non-blocking prompt with buttons `Run` and `Dismiss`.
    - Human reports clicking `Dismiss` does not create a new session issue or chat log file.
    - Human reports clicking `Run` asks persona scope and creates matching story-intelligence session/chat stubs.
    - Human reports applying an approved story-note MetadataAction writes a backlink to an allowed note or memory file.
    - Human reports a metadata action targeting `manuscript/ch01.md` is blocked and `.leanquill/metadata-actions.jsonl` contains `"status":"blocked"` but no `"status":"proposed"`.
  </acceptance_criteria>
  <action>
    Pause for human Extension Development Host verification after Task 1 is green. Present the `<how-to-verify>` steps exactly. Do not mark the plan complete until the user reports `approved` or describes a failed step. If the user describes a failed step, route back to the plan that owns the failed behavior and re-run Task 1 after fixing it.
  </action>
  <what-built>
    Phase 10 AI review/advisory workflows: chapter review, finalized review output, session finding promotion, issue-focused chat, story intelligence prompt/command, durable session/chat audit artifacts, and metadata action safety.
  </what-built>
  <how-to-verify>
    1. Launch the extension with VS Code Run and Debug -> Run LeanQuill Extension.
    2. Open a LeanQuill-ready book workspace in the Extension Development Host with at least one enabled persona.
    3. Right-click the chapter/outline tree surface and choose `Run chapter review`, or run `LeanQuill: Run chapter review` from the Command Palette.
    4. Expected: QuickPick title `Choose how to run this review` appears with `One enabled persona…` and `All enabled personas`.
    5. Choose one persona. Expected: notification `Session file and chat log started for this review.`, one `.leanquill/issues/sessions/*.md`, and one `.leanquill/chats/*.md` share the same `session_id`.
    6. Finalize the review using the direct result path or `LeanQuill: Save chapter review result` with at least one finding. Expected: the session file contains `### Finding`, `issue_count` is greater than 0, and the matching chat log contains `issues_generated`.
    7. Run `LeanQuill: Promote session finding to issue` for that finding. Expected: a normal issue file appears under `.leanquill/issues/{type}/` and is visible in the triage list; the raw session file is not listed as a normal issue.
    8. Open the Planning Issues tab and bottom-panel Issues view. Expected: issue rows/context menu include `Chat about this`.
    9. Choose `Chat about this` on a chapter or selection issue. Expected: chat draft includes `Launch source: issue`, the issue markdown under `.leanquill/issues/`, and the full associated `manuscript/*.md` path.
    10. Save an open manuscript chapter `.md`. Expected: one prompt says `You saved a chapter. Run a story intelligence pass on the full chapter? Proposed story-note updates require your approval. Manuscript files are never modified.` with buttons `Run` and `Dismiss`.
    11. Click `Dismiss`. Expected: no new `.leanquill/issues/sessions/*.md` or `.leanquill/chats/*.md` file appears.
    12. Save again and click `Run`. Expected: persona QuickPick appears, then matching `story-intelligence-update` session/chat stubs are created.
    13. Run `LeanQuill: Apply Metadata Action` with an approved action targeting an allowed note/memory path and containing a backlink to `manuscript/ch03.md`. Expected: the note/memory file is updated and `.leanquill/metadata-actions.jsonl` contains `"status":"applied"`.
    14. Run `LeanQuill: Apply Metadata Action` with any valid-shaped action targeting `manuscript/ch01.md`. Expected: blocked notification and `.leanquill/metadata-actions.jsonl` contains `"status":"blocked"` and no `"status":"proposed"`.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe the exact failed step and observed behavior.</resume-signal>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test && npm run build</automated>
  </verify>
  <done>Human verifies AIR-01, AIR-02, AIR-03, and ISSUE-05 in Extension Development Host with durable artifacts and manuscript safety intact.</done>
</task>

</tasks>

<verification>
Automated gate: `npm run build:test && npm test && npm run build`. Manual gate: Extension Development Host checklist in Task 2.
</verification>

<success_criteria>
- All Phase 10 roadmap success criteria are demonstrable.
- AI workflows are advisory-only, auditable, and local-first.
- Manuscript files remain read-only for AI workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-review-and-advisory-workflows/10-06-SUMMARY.md`
</output>
