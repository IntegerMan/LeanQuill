---
phase: 14-open-questions
plan: 04
type: execute
wave: 4
depends_on:
  - "14-03"
files_modified:
  - src/planningPanelHtml.ts
  - src/planningPanel.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
user_setup: []
must_haves:
  truths:
    - "Planning Workspace exposes an **Open questions** tab (D-01) using shared `openQuestionsHtml` output."
    - "Planning webview handles `openQuestion:*` messages: list load, edit, save, status change, delete, navigate request."
  artifacts:
    - path: "src/planningPanelHtml.ts"
      contains: "openQuestions"
    - path: "src/planningPanel.ts"
      contains: "openQuestion"
  key_links:
    - from: "src/planningPanel.ts"
      to: "src/openQuestionStore.ts"
      via: "list/save/delete"
      pattern: "listOpenQuestions|saveOpenQuestion"
---

<objective>
Integrate Open Questions into the Planning workspace webview as the primary consolidated surface (D-01), delegating UI to the shared HTML module (D-02).

Purpose: Authors manage questions where they already plan characters, places, and threads.
Output: New tab in `renderPlanningHtml` + full handler wiring in `PlanningPanelProvider`.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/planningPanel.ts
@src/planningPanelHtml.ts
@src/openQuestionsHtml.ts
@src/openQuestionStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: planningPanelHtml — tab + renderPlanningHtml contract</name>
  <files>src/planningPanelHtml.ts</files>
  <read_first>
    - src/planningPanelHtml.ts (TAB_IDS, renderPlanningHtml signature ~598+)
    - src/openQuestionsHtml.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "openQuestions" src/planningPanelHtml.ts` shows tab id in `TAB_IDS` array after `threads` (tab order: … → Threads → Open questions).
    - `grep -n "Open questions" src/planningPanelHtml.ts` matches 14-UI-SPEC tab label (sentence case in button text).
    - `grep -n "renderOpenQuestionsHtml" src/planningPanelHtml.ts` wires shared renderer with `host: 'planning'`.
    - `renderPlanningHtml` signature includes new args: `openQuestions` rows array + `selectedOpenQuestionId: string | undefined` (exact names as implemented — document in grep-able export).
  </acceptance_criteria>
  <action>
    Extend `TAB_IDS` / `TAB_LABELS` with `openQuestions` → label **Open questions**. In `renderPlanningHtml`, add parameters for serialized open-question rows and selection. Tab panel branch calls `renderOpenQuestionsHtml(..., 'planning', ...)`. Pass questions sorted by `updatedAt` or `created_at` descending if available. Ensure master–detail receives `selectedOpenQuestionId` for initial selection state.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Planning HTML compiles; tab renders shared open-questions panel body.</done>
</task>

<task type="auto">
  <name>Task 2: planningPanel.ts — data load + openQuestion:* message handlers</name>
  <files>src/planningPanel.ts</files>
  <read_first>
    - src/planningPanel.ts (_handleMessage, character/places debounce patterns)
    - src/openQuestionStore.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "openQuestion:" src/planningPanel.ts` shows handler switch cases matching protocol from `openQuestionsHtml.ts`.
    - `grep -n "_selectedOpenQuestion" src/planningPanel.ts` OR equivalent selection state fields.
    - `grep -n "debounce" src/planningPanel.ts` with open-question save path (pattern match for debounced field saves).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <action>
    Import store functions. Add private state: selected open-question id, pending dirty fields, debounce timer (mirror `_charDebounceTimer` pattern). On `_renderPanel`, load `listOpenQuestions` + pass to `renderPlanningHtml`. Handle messages: `openQuestion:select` sets selection and re-renders; `openQuestion:fieldChange` updates pending; `openQuestion:save` flushes to `saveOpenQuestion`; `openQuestion:delete` calls `deleteOpenQuestion` then refresh; `openQuestion:navigate` delegates to extension command `leanquill.openQuestionTarget` (registered in 14-06) via `vscode.postMessage` is wrong direction — **host** must execute navigation: use injected callback or `vscodeApi.commands.executeCommand` from `PlanningPanelProvider` (pass `vscodeApi` from ctor). For 14-04, stub navigate handler that calls `this.vscodeApi.commands.executeCommand("leanquill.openQuestionTarget", { questionId })` if command not yet registered, no-op catch until 14-06 registers it. On successful mutations, call `this._renderPanel()` and emit refresh to panel host via shared service if introduced in 14-05/06 (optional event emitter). Support `openQuestion:new-question` with optional `prefill` payload from 14-06 context commands.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Planning workspace open-questions tab is interactive against store for CRUD within webview limits.</done>
</task>

</tasks>

<verification>
Automated: `npm run build`. Full `npm test` at wave end.
</verification>

<success_criteria>
D-01 satisfied: consolidated list + edit in Planning workspace using shared visuals (D-02) from plan 14-03.
</success_criteria>

<output>
After completion, create `.planning/phases/14-open-questions/14-04-SUMMARY.md`
</output>
