---
phase: 14-open-questions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/openQuestionStore.ts
  - test/openQuestionStore.test.ts
  - test/openQuestionChapterCounts.test.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
user_setup: []
must_haves:
  truths:
    - "Automated test sources exist for open-question parse/serialize, associations, allowed statuses, and per-chapter open counts."
    - "`npm run build:test` compiles new test entrypoints into dist-test/."
    - "Wave 0 tests encode ISSUE-01-shaped records (chapter_ref, span_hint, book/entity association keys) and ISSUE-02 partial status rules (only open / deferred / resolved); they compile against `src/openQuestionStore.ts` and go GREEN when 14-02 replaces the stub with real behavior."
  artifacts:
    - path: "src/openQuestionStore.ts"
      provides: "Compile stub with exported serializers + counting helpers (replaced in 14-02)"
      contains: "STUB: replaced by plan 14-02"
    - path: "test/openQuestionStore.test.ts"
      provides: "ISSUE-01/02 partial unit tests (RED until plan 14-02 implements store)"
      min_lines: 40
    - path: "test/openQuestionChapterCounts.test.ts"
      provides: "D-03 counting + path normalization tests"
      min_lines: 30
  key_links:
    - from: "test/openQuestionStore.test.ts"
      to: "src/openQuestionStore.ts"
      via: "imports stub module (14-02 swaps stub body for real impl)"
      pattern: "openQuestionStore"
---

<objective>
Add Wave 0 unit test files for open-question persistence and chapter-linked open counts so later plans can run `node --test` against real behavior.

Purpose: Satisfy Nyquist / 14-VALIDATION.md gaps before production code lands.
Output: Two new `test/*.test.ts` files that compile via `build:test`.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-open-questions/14-CONTEXT.md
@.planning/phases/14-open-questions/14-RESEARCH.md
@.planning/phases/14-open-questions/14-VALIDATION.md
@Imported/data-contracts/issue-schema.md
@test/threadStore.test.ts
@test/characterStore.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Minimal openQuestionStore stub (compile surface for Wave 0)</name>
  <files>src/openQuestionStore.ts</files>
  <read_first>
    - .planning/phases/14-open-questions/14-RESEARCH.md
    - Imported/data-contracts/issue-schema.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "STUB: replaced by plan 14-02" src/openQuestionStore.ts` returns at least one line.
    - `grep -n "parseOpenQuestionFile" src/openQuestionStore.ts` shows an exported `parseOpenQuestionFile`.
    - `grep -n "serializeOpenQuestionFile" src/openQuestionStore.ts` shows an exported `serializeOpenQuestionFile`.
    - `grep -nE "countOpenQuestionsByChapter|mergeOpenQuestionCountsIntoChapterStatusIndex" src/openQuestionStore.ts` matches at least one exported counting helper name used by `openQuestionChapterCounts.test.ts` (use the same chosen name in Task 2 tests).
  </acceptance_criteria>
  <action>
    Add `src/openQuestionStore.ts` as a **compile stub** (plan 14-02 replaces with full ISSUE-01/02 implementation). Export minimal types and functions that Wave 0 tests will import: `parseOpenQuestionFile`, `serializeOpenQuestionFile`, and one counting helper (`countOpenQuestionsByChapter` **or** `mergeOpenQuestionCountsIntoChapterStatusIndex` — pick one name and use it consistently in Task 2). Stub bodies may `throw new Error("not implemented")` or return empty neutral values; Wave 0 only requires **TypeScript compile** via `build:test` after tests are added. Top of file: comment line exactly `// STUB: replaced by plan 14-02` (grep target).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Stub file exists; all stub acceptance greps pass; `npx tsc --noEmit` succeeds (typechecks `src/openQuestionStore.ts` via tsconfig `include`).</done>
</task>

<task type="auto">
  <name>Task 1: openQuestionStore.test.ts (ISSUE-01 partial, ISSUE-02 partial)</name>
  <files>test/openQuestionStore.test.ts</files>
  <read_first>
    - Imported/data-contracts/issue-schema.md
    - test/threadStore.test.ts
    - .planning/phases/14-open-questions/14-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "author-note" test/openQuestionStore.test.ts` returns at least one line.
    - `grep -n "chapter_ref" test/openQuestionStore.test.ts` returns at least one line.
    - `grep -nE "open|deferred|resolved" test/openQuestionStore.test.ts` matches only Phase-14 statuses (no `dismissed` tests).
    - `grep -n "span_hint" test/openQuestionStore.test.ts` returns at least one line for selection anchoring (D-05).
    - `grep -nE "lq_assoc|character|place|thread|book" test/openQuestionStore.test.ts` documents extension association keys planned for Phase 8 merge (per 14-RESEARCH.md).
  </acceptance_criteria>
  <action>
    Create `test/openQuestionStore.test.ts` importing `../src/openQuestionStore` (stub from Task 0; 14-02 replaces stub implementation). Cover per D-05: minimal storage uses `type: author-note`, contract `chapter_ref` as repo-relative forward slashes (`manuscript/ch01.md` or `project-wide`), optional `span_hint` as quoted fragment per issue-schema; document in test comments that stale/ambiguous resolution is host/UI (14-06). Tests for round-trip serialize/parse of: book-wide (`chapter_ref: project-wide` + `lq_book_wide: true` or equivalent single flag agreed in 14-02), character (`lq_character_file`), place (`lq_place_file`), thread (`lq_thread_file`), chapter-only, chapter+selection. ISSUE-02 partial: only `open`, `deferred`, `resolved` — assert invalid statuses rejected or normalized in store API tests. Tests **may fail** at runtime until 14-02 implements the module; this plan only requires **successful TypeScript compile** via `build:test`.
  </action>
  <verify>
    <automated>npm run build:test</automated>
  </verify>
  <done>`npm run build:test` succeeds; `test/openQuestionStore.test.ts` exists and satisfies acceptance grep bullets.</done>
</task>

<task type="auto">
  <name>Task 2: openQuestionChapterCounts.test.ts (D-03)</name>
  <files>test/openQuestionChapterCounts.test.ts</files>
  <read_first>
    - src/chapterTree.ts
    - .planning/phases/14-open-questions/14-UI-SPEC.md
    - .planning/phases/14-open-questions/14-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "countOpen" test/openQuestionChapterCounts.test.ts` OR `grep -n "openQuestion" test/openQuestionChapterCounts.test.ts` matches helper under test.
    - `grep -n "manuscript/" test/openQuestionChapterCounts.test.ts` confirms normalized chapter paths use forward slashes.
    - `grep -nE "book|entity|character|thread|place" test/openQuestionChapterCounts.test.ts` includes assertion that entity-only / book-wide **open** questions do **not** increment a manuscript chapter path key (D-03 / 14-UI-SPEC).
  </acceptance_criteria>
  <action>
    Add `test/openQuestionChapterCounts.test.ts` targeting the **pure counting helper** exported from `src/openQuestionStore.ts` (e.g. `countOpenQuestionsByChapterRef` / `mergeOpenQuestionCountsIntoChapterStatusIndex`) once 14-02 defines the name. Until then, write tests against the expected function signatures described in 14-RESEARCH.md (import from `../src/openQuestionStore`). Cases: multiple open questions on same `chapter_ref`; `resolved`/`deferred` excluded from open count; Windows-style path input normalized to forward slashes; `project-wide` book questions excluded from per-chapter map. Compile-only gate for this plan is `npm run build:test`.
  </action>
  <verify>
    <automated>npm run build:test</automated>
  </verify>
  <done>Second test file exists; `npm run build:test` succeeds.</done>
</task>

</tasks>

<verification>
Run `npm run build` after Task 0; run `npm run build:test` after Tasks 1–2; full `npm test` is wave-level after plan 14-02 greens store tests.
</verification>

<success_criteria>
Wave 0 test entrypoints exist and compile; ISSUE-01/02 and D-03 behaviors are specified in tests for implementation in 14-02.
</success_criteria>

<output>
After completion, create `.planning/phases/14-open-questions/14-01-SUMMARY.md`
</output>
