---
phase: 14-open-questions
plan: 05
type: execute
wave: 5
depends_on:
  - "14-04"
files_modified:
  - src/openQuestionsPanel.ts
  - package.json
  - src/extension.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
user_setup: []
must_haves:
  truths:
    - "Bottom panel hosts Open Questions via `WebviewViewProvider` in `viewsContainers.panel` (D-02)."
    - "`retainContextWhenHidden: true` is set on the webview view options (14-UI-SPEC)."
  artifacts:
    - path: "src/openQuestionsPanel.ts"
      provides: "OpenQuestionsPanelViewProvider class"
      min_lines: 60
    - path: "package.json"
      contains: "leanquillOpenQuestions"
  key_links:
    - from: "src/openQuestionsPanel.ts"
      to: "src/openQuestionsHtml.ts"
      via: "renderOpenQuestionsHtml(..., 'panel', ...)"
      pattern: "renderOpenQuestionsHtml"
---

<objective>
Register the panel-region Open Questions webview view and minimal extension wiring so authors can dock the feature beside Problems/Output/Terminal (D-02).

Purpose: Second host consumes the same HTML builder and message protocol as the Planning tab.
Output: `openQuestionsPanel.ts`, `package.json` contributions, `extension.ts` registration.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-open-questions/14-RESEARCH.md
@.planning/phases/14-open-questions/14-UI-SPEC.md
@package.json
@src/extension.ts
@src/openQuestionsHtml.ts
@src/openQuestionStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: package.json panel container + webview view + activation</name>
  <files>package.json</files>
  <read_first>
    - package.json (contributes.views, viewsContainers, activationEvents)
    - .planning/phases/14-open-questions/14-RESEARCH.md (sample JSON)
  </read_first>
  <acceptance_criteria>
    - `grep -n "viewsContainers" package.json` includes `"panel"` array with id `leanquillOpenQuestions` and title `LeanQuill` (or product-consistent title per discretion).
    - `grep -n "leanquill.openQuestionsPanel" package.json` appears under `views` for that container with `"type": "webview"` and name **Open questions**.
    - `grep -n "onView:leanquill.openQuestionsPanel" package.json` in `activationEvents`.
  </acceptance_criteria>
  <action>
    Add `viewsContainers.panel` entry and nested `views` for webview id `leanquill.openQuestionsPanel`. Reuse `media/leanquill.svg` for container icon. Add activationEvent `onView:leanquill.openQuestionsPanel`. Do not add Phase-8 deferred menus yet (those land in 14-06).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Contributions validate; extension still bundles.</done>
</task>

<task type="auto">
  <name>Task 2: OpenQuestionsPanelViewProvider + registerWebviewViewProvider</name>
  <files>src/openQuestionsPanel.ts, src/extension.ts</files>
  <read_first>
    - src/extension.ts (registerWebviewViewProvider for outline/context)
    - src/openQuestionsHtml.ts
    - src/openQuestionStore.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "registerWebviewViewProvider" src/extension.ts` includes `leanquill.openQuestionsPanel`.
    - `grep -n "retainContextWhenHidden" src/openQuestionsPanel.ts` OR in register options second argument.
    - `grep -n "leanquill.openQuestionsPanel" src/openQuestionsPanel.ts` defines `static viewId`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <action>
    Implement `OpenQuestionsPanelViewProvider` implementing `WebviewViewProvider`: on `resolveWebviewView`, set options `enableScripts: true`, `localResourceRoots: [extensionUri]`, register `onDidReceiveMessage` with the **same** `openQuestion:*` handler logic as planning panel (extract shared `handleOpenQuestionMessage` in a new small module `src/openQuestionWebviewHandlers.ts` if needed to avoid duplication — planner discretion, D-02 anti-drift). Render via `renderOpenQuestionsHtml(..., 'panel', ...)`. Constructor receives `(vscodeApi, extensionUri, rootPath, safeFs, getPlanningPanel?: () => PlanningPanelProvider | undefined)` to optionally forward refresh; minimal 14-05: panel loads/saves via store directly without planning panel coupling. Expose `refresh(): Promise<void>` public method for extension to call after mutations from commands in 14-06.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Panel webview resolves HTML; provider registered in activation.</done>
</task>

</tasks>

<verification>
`npm run build` after tasks; manual dock test deferred to UAT.
</verification>

<success_criteria>
D-02 second surface exists in the panel region with shared renderer and matching webview options.
</success_criteria>

<output>
After completion, create `.planning/phases/14-open-questions/14-05-SUMMARY.md`
</output>
