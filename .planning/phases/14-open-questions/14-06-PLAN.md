---
phase: 14-open-questions
plan: 06
type: execute
wave: 6
depends_on:
  - "14-05"
files_modified:
  - package.json
  - src/extension.ts
  - src/planningPanel.ts
  - src/planningPanelHtml.ts
  - src/outlineWebviewPanel.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
user_setup: []
must_haves:
  truths:
    - "Context-first commands exist for chapter (outline row), manuscript selection, book-wide, character/place/thread targets (D-04) — **mandatory** planning-webview row context actions for character / place / thread (no palette-only fallback for those associations)."
    - "Outline webview shows open **question** counts on manuscript-linked rows (D-03 — current repo’s chapter navigation surface; not `chapterTree.ts` which is unused in package.json)."
    - "`leanquill.openQuestionTarget` navigates book-wide, character, place, thread, and manuscript (chapter / selection) targets per roadmap success criterion 3: branch on `lq_assoc_kind` (or equivalent persisted discriminator); open entity markdown under `ProjectConfig` folder paths; open manuscript with `span_hint` selection when uniquely resolvable; otherwise degrade (open file / chapter, stale flag in DTO / refresh)."
  artifacts:
    - path: "src/extension.ts"
      contains: "newOpenQuestion"
    - path: "src/outlineWebviewPanel.ts"
      contains: "issue"
  key_links:
    - from: "src/extension.ts"
      to: ".leanquill/open-questions/"
      via: "FileSystemWatcher"
      pattern: "open-questions"
---

<objective>
Wire context menus, commands, file watchers, outline issue counts, and cross-webview refresh so open questions are authorable from context (D-04), visible on chapter rows (D-03), and navigable with graceful stale handling (D-05).

Purpose: Completes roadmap success criteria 1–4 for Phase 14 without Phase-8 deferred scope.
Output: Fully integrated extension behavior plus outline UI augmentation.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/extension.ts
@src/outlineWebviewPanel.ts
@src/openQuestionStore.ts
@src/planningPanel.ts
@src/planningPanelHtml.ts
@src/openQuestionsPanel.ts
@.planning/phases/14-open-questions/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commands + menus + watchers + coordinated refresh</name>
  <files>package.json, src/extension.ts, src/planningPanel.ts, src/planningPanelHtml.ts</files>
  <read_first>
    - src/extension.ts (newCharacterCommand / context menu patterns)
    - package.json (commands + menus sections)
    - src/planningPanel.ts
    - src/planningPanelHtml.ts
    - src/openQuestionsPanel.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "leanquill.newOpenQuestion" package.json` lists at least palette-visible command for book-wide creation.
    - `grep -n "leanquill.newOpenQuestionFromSelection" package.json` OR similarly named command for editor selection (exact id grep-able).
    - `grep -n "openQuestionsPanel" package.json` menus tie context actions to outline / editor when clauses use `leanquill.workspaceLeanQuillReady`.
    - `grep -n "open-questions" src/extension.ts` shows `FileSystemWatcher` (or `createFileSystemWatcher`) on `.leanquill/open-questions/**` calling `planningPanel.refresh()` and `openQuestionsPanel.refresh()`.
    - **D-04 entity commands:** `grep -n "newOpenQuestionFromCharacter" package.json`; `grep -n "newOpenQuestionFromPlace" package.json`; `grep -n "newOpenQuestionFromThread" package.json` each return ≥1 line (command contribution).
    - **Planning webview row context (mandatory):** `grep -n "openQuestionRowContext" src/planningPanelHtml.ts` AND `grep -n "openQuestionRowContext" src/planningPanel.ts` each return ≥1 line (HTML posts/menu ids + handler dispatch to the three entity commands above).
  </acceptance_criteria>
  <action>
    Register commands (exact ids — implement as specified): `leanquill.newOpenQuestion` (book-wide, `chapter_ref: project-wide`, `lq_assoc_kind: book`), `leanquill.newOpenQuestionFromChapter` (argument: `{ chapterPath: string }` from outline context), `leanquill.newOpenQuestionFromSelection` (active editor: verify URI under `manuscript/` relative to workspace; capture selected text → `span_hint`, `chapter_ref` = relative path; error message if no selection). **Per D-04 (mandatory, no escape hatch):** register `leanquill.newOpenQuestionFromCharacter`, `leanquill.newOpenQuestionFromPlace`, `leanquill.newOpenQuestionFromThread` each accepting `{ fileName: string }` (basename or repo-relative per existing entity stores); wire **planning webview** list rows for characters, places, and threads so row context invokes the matching command with the row’s entity file key — include the literal marker string `openQuestionRowContext` in both `planningPanelHtml.ts` and `planningPanel.ts` (message type, `data-*` attribute, or constant shared by handler). After each creation, open Planning workspace to Open questions tab with new id selected (call `planningPanel.showOpenQuestion(id)` or add method). Register `leanquill.openQuestionTarget` in Task 1 if required for activation order; **full** navigation branches for all `lq_assoc_kind` values are implemented in Task 3. Outline: extend `handleContextAction` / `contextAction` pipeline for `newOpenQuestion` on chapter nodes with `fileName` as `chapter_ref`. Package.json: `commandPalette` + **`editor/context`** for selection command when `editorTextFocus` and resource under manuscript folder; **`webview/context`** for outline if required. Wire watchers to refresh planning + panel hosts + outline provider after filesystem changes.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Commands, mandatory entity row context wiring, and refresh compile; `npm run build` passes (full `npm test` deferred to Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Outline webview — open question counts on chapter rows (D-03)</name>
  <files>src/outlineWebviewPanel.ts, src/extension.ts</files>
  <read_first>
    - src/outlineWebviewPanel.ts (`renderNodeTree`, `refresh`)
    - src/types.ts (`OutlineNode`)
    - src/openQuestionStore.ts (counting helper from 14-02)
  </read_first>
  <acceptance_criteria>
    - `grep -n "openQuestion" src/outlineWebviewPanel.ts` OR `grep -n "questionCount" src/outlineWebviewPanel.ts` shows count injection into row HTML for leaf manuscript nodes.
    - `grep -n "listOpenQuestions" src/outlineWebviewPanel.ts` OR counts passed from `refresh()` caller in extension.
    - Row template includes muted count text consistent with `chapterTree.issueCountText` semantics (`N issues` vs `N questions` — use **issues** label only if matching existing chapter UX; 14-UI-SPEC allows question wording in open-questions UI, not necessarily outline — prefer `issues` count text to match `src/chapterTree.ts` `issueCountText` for D-03 consistency).
  </acceptance_criteria>
  <action>
    Extend `renderOutlineWebviewHtml` signature to accept `chapterOpenIssueCounts: Record<string, number>` (normalized `manuscript/...` keys). In `OutlineWebviewProvider.refresh`, call `listOpenQuestions`, compute map via exported helper from `openQuestionStore.ts`, pass into HTML renderer. For each outline node with `fileName` matching a manuscript chapter path key, append count badge next to status text when count &gt; 0. Ensure orphan rows (`orphan-item`) get counts too when file path matches. After question mutations (from 14-06 task 1 watcher), `outlineWebviewProvider.refresh()` must be called — thread `onMutate` or explicit callback.
  </action>
  <verify>
    <automated>npm run build && npm run build:test && node --test dist-test/outlineStore.test.js</automated>
  </verify>
  <done>Outline rows show per-chapter open question counts aligned with D-03; targeted outline store tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Navigation helper + grep verification hooks</name>
  <files>src/extension.ts, src/planningPanel.ts</files>
  <read_first>
    - src/openQuestionStore.ts
    - src/projectConfig.ts
    - Imported/data-contracts/issue-schema.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "showTextDocument" src/extension.ts` appears inside open-question navigation path.
    - `grep -n "TextEditorRevealType" src/extension.ts` OR selection range construction for `span_hint` navigation.
    - `grep -n "openQuestionTarget" src/extension.ts` implements command body.
    - **Roadmap SC3 — discriminator:** `grep -n "lq_assoc_kind" src/extension.ts` appears in the navigation resolver path (same function or a `resolveOpenQuestionTarget` helper colocated in `extension.ts`).
    - **Entity basenames → paths:** `grep -n "lq_character_file" src/extension.ts`; `grep -n "lq_place_file" src/extension.ts`; `grep -n "lq_thread_file" src/extension.ts` each return ≥1 line (read frontmatter / map to paths).
    - **ProjectConfig folders:** `grep -n "folders.characters" src/extension.ts`; `grep -n "folders.threads" src/extension.ts`; `grep -n "folders.settings" src/extension.ts` each return ≥1 line (places use `settings` per `placeStore.ts` — if navigation uses a different helper, still grep must show all three folder roots referenced for the three entity kinds).
    - **Book-wide:** `grep -n "project-wide" src/extension.ts` appears in the `book` association branch (or comment `openQuestionTarget: book` on adjacent line).
    - **Manuscript selection:** `grep -n "span_hint" src/extension.ts` appears in chapter/selection navigation.
    - **Planning list row reveal:** `grep -n "revealOpenQuestionRow" src/planningPanel.ts` returns ≥1 line (public method or exported symbol invoked from `openQuestionTarget` for `book` and after entity opens as needed).
  </acceptance_criteria>
  <action>
    Implement `leanquill.openQuestionTarget` in `extension.ts` (complete if Task 1 only registered it): load question by id; read `lq_assoc_kind` (and `lq_character_file` / `lq_place_file` / `lq_thread_file` / `chapter_ref` / `span_hint` per 14-02). **Branches:** `book` → show Planning workspace, open-questions tab, call `planningPanel.revealOpenQuestionRow(questionId)` (add method if missing). `character` | `place` | `thread` → resolve absolute path using `getProjectConfig` + `folders.characters` / `folders.settings` / `folders.threads` + basename from `lq_*_file`, then `showTextDocument`. `chapter` | `selection` with manuscript `chapter_ref` → open manuscript file; if `span_hint` present, search for substring — unique match: set selection + `revealRange(InCenter)`; zero matches: open file, set `staleAnchor` / `navStale` on DTO and `refresh()`; multiple matches: first occurrence + ambiguous flag (D-05). Keep manuscript read-only (no writes).
  </action>
  <verify>
    <automated>npm run build:test && npm test</automated>
  </verify>
  <done>Navigation command covers book, character, place, thread, and manuscript targets; full test suite passes.</done>
</task>

</tasks>

<verification>
Phase gate: `npm run build:test && npm test` per 14-VALIDATION.md (run once after Task 3; Tasks 1–2 use narrower automated commands above).
</verification>

<success_criteria>
Phase 14 roadmap success criteria: create with associations, consolidated list in both hosts, **navigate to book-wide, character, place, thread, and manuscript targets** (success criterion 3), status updates with immediate refresh — all covered by implementation tasks; manual UAT per 14-VALIDATION.md.
</success_criteria>

<output>
After completion, create `.planning/phases/14-open-questions/14-06-SUMMARY.md`
</output>
