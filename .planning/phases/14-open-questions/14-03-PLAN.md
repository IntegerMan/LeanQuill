---
phase: 14-open-questions
plan: 03
type: execute
wave: 3
depends_on:
  - "14-02"
files_modified:
  - src/openQuestionsHtml.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
user_setup: []
must_haves:
  truths:
    - "Shared HTML/CSS/JS string builder can render master–detail open-questions UI for both hosts with host parameter."
    - "Copy, spacing, typography, and status labels match 14-UI-SPEC.md (Phase 14 subset)."
  artifacts:
    - path: "src/openQuestionsHtml.ts"
      provides: "renderOpenQuestionsHtml(questions, selectedId, host, nonce, cspSource)"
      min_lines: 80
  key_links:
    - from: "src/openQuestionsHtml.ts"
      to: "openQuestion:* message protocol"
      via: "inline script postMessage"
      pattern: "openQuestion"
---

<objective>
Create the shared webview markup builder for Open Questions per 14-UI-SPEC.md so Planning tab and panel host stay visually identical (D-02).

Purpose: Single source for list/detail UI; prevents drift called out in 14-RESEARCH.md.
Output: `src/openQuestionsHtml.ts` exporting a render function and documented message types.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-open-questions/14-UI-SPEC.md
@src/planningPanelHtml.ts
@src/htmlUtils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared layout, tokens, and empty states per UI-SPEC</name>
  <files>src/openQuestionsHtml.ts</files>
  <read_first>
    - .planning/phases/14-open-questions/14-UI-SPEC.md
    - src/planningPanelHtml.ts (CSP + body styles pattern, first ~200 lines)
  </read_first>
  <acceptance_criteria>
    - `grep -n "No open questions yet" src/openQuestionsHtml.ts` matches empty-title copy from UI-SPEC.
    - `grep -n "Create a question from the chapter tree" src/openQuestionsHtml.ts` matches empty body hint.
    - `grep -n "This link may be out of date" src/openQuestionsHtml.ts` matches stale-anchor copy.
    - `grep -n "40%" src/openQuestionsHtml.ts` OR flex split comment reflecting 40/60 master–detail default.
    - `grep -n "520" src/openQuestionsHtml.ts` for responsive stack breakpoint.
  </acceptance_criteria>
  <action>
    Implement `renderOpenQuestionsHtml` with parameters `(questions: SerializableOpenQuestionRow[], selectedId: string | undefined, host: 'planning' | 'panel', nonce: string, cspSource: string)` where `SerializableOpenQuestionRow` is a minimal DTO (id, title, preview, status, associationChip). Use VS Code theme CSS variables per 14-UI-SPEC (surface, border, warning for stale). Master–detail: 40/60 split wide, stack below 520px. Toolbar strip: **New question** button `data-action="new-question"` and **Refresh** `data-action="refresh"`. List rows: `data-question-id`, keyboard focusable attributes as feasible in static HTML. Detail: title input `name="title"`, body textarea `name="body"`, status `<select>` with only `open|deferred|resolved`, association read-only summary + optional stale warning region `data-stale="1"`. Use CSP mirroring `planningPanelHtml.ts` (`script-src nonce-...`). Do **not** surface dismissed/filter UI (Phase 8 deferred).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Module exports render function; `npm run build` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Client script: openQuestion:* postMessage protocol</name>
  <files>src/openQuestionsHtml.ts</files>
  <read_first>
    - src/planningPanelHtml.ts (search for postMessage / vscode.getState patterns in characters/places tabs)
  </read_first>
  <acceptance_criteria>
    - `grep -n "openQuestion:" src/openQuestionsHtml.ts` lists at least `openQuestion:select`, `openQuestion:fieldChange`, `openQuestion:save`, `openQuestion:delete`, `openQuestion:navigate` (exact names as implemented — grep must find `openQuestion:` prefix).
    - `grep -n "acquireVsCodeApi" src/openQuestionsHtml.ts` confirms vscode messaging bootstrap.
  </acceptance_criteria>
  <action>
    Embed nonce-gated script that uses `acquireVsCodeApi()`. Wire list click → `openQuestion:select`. Debounce title/body/status changes → `openQuestion:fieldChange` with `{ id, field, value }`. Primary save button → `openQuestion:save`. Optional delete → `openQuestion:delete`. Navigate from detail → `openQuestion:navigate` when user activates “Go to target” control. Include `host` in each message payload `{ host: 'planning' | 'panel' }` so extension routes to the correct webview instance (D-02).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Protocol strings documented in file header comment + grep acceptance satisfied.</done>
</task>

</tasks>

<verification>
Manual UI parity lives in 14-VALIDATION.md; automated gate is `npm run build`.
</verification>

<success_criteria>
Shared renderer exists; D-02 structural parity requirements from 14-UI-SPEC are encoded in one module.
</success_criteria>

<output>
After completion, create `.planning/phases/14-open-questions/14-03-SUMMARY.md`
</output>
