---
phase: 14-open-questions
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - src/types.ts
  - src/openQuestionStore.ts
  - src/chapterStatus.ts
  - test/chapterStatus.test.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
user_setup: []
must_haves:
  truths:
    - "Open questions persist as one markdown file per record under `.leanquill/open-questions/` with issue-schema-aligned frontmatter plus documented `lq_*` association keys."
    - "Only statuses open / deferred / resolved are allowed for Phase 14 records (ISSUE-02 partial)."
    - "Chapter status writes do not reset `openIssueCount` to zero (D-03 pitfall fix from 14-RESEARCH.md)."
  artifacts:
    - path: "src/openQuestionStore.ts"
      provides: "CRUD, list, parse/serialize, counting helpers"
      min_lines: 120
    - path: "src/chapterStatus.ts"
      provides: "Preserves openIssueCount on status-only writes"
      contains: "openIssueCount"
  key_links:
    - from: "src/openQuestionStore.ts"
      to: ".leanquill/open-questions/"
      via: "SafeFileSystem writes"
      pattern: "open-questions"
---

<objective>
Implement the local-first open-question store and fix chapter status persistence so open-issue counts are not clobbered, matching ISSUE-01/02 partial scope and D-05 anchor representation.

Purpose: Establish the single source of truth Phase 8 will extend.
Output: `openQuestionStore.ts`, types, and green unit tests from 14-01.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@Imported/data-contracts/issue-schema.md
@src/safeFileSystem.ts
@src/threadStore.ts
@src/chapterStatus.ts
@src/yamlUtils.ts

<interfaces>
From `src/safeFileSystem.ts` (conceptual): use existing `SafeFileSystem` for all writes under `.leanquill/`.

From `src/types.ts` (extend with):
- `OpenQuestionStatus = "open" | "deferred" | "resolved"`
- `OpenQuestionAssociation` discriminated union: `book` | `{ kind: "character"; fileName: string }` | `{ kind: "place"; fileName: string }` | `{ kind: "thread"; fileName: string }` | `{ kind: "chapter"; chapterRef: string }` | `{ kind: "selection"; chapterRef: string; spanHint: string }`
- `OpenQuestionRecord` with `id`, `title`, `body`, `status`, `createdAt`, association, optional `staleHint` for UI (computed, not persisted).

**D-05 (locked, documented here):** Persist `chapter_ref` + optional `span_hint` per `issue-schema.md`; add `lq_assoc_kind: book | character | place | thread | chapter | selection` and `lq_character_file` / `lq_place_file` / `lq_thread_file` (basename under respective folders) for entity links. Phase 8 merges these keys into the unified issue model without migration rewrite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + openQuestionStore CRUD and YAML/markdown serialization</name>
  <files>src/types.ts, src/openQuestionStore.ts</files>
  <read_first>
    - src/threadStore.ts
    - src/yamlUtils.ts
    - Imported/data-contracts/issue-schema.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "open-questions" src/openQuestionStore.ts` shows directory constant `.leanquill/open-questions`.
    - `grep -n "author-note" src/openQuestionStore.ts` shows default `type`.
    - `grep -n "lq_assoc_kind" src/openQuestionStore.ts` returns at least one line.
    - `grep -n "normalize" src/openQuestionStore.ts` shows path normalization to forward slashes.
    - `npm run build` succeeds (extension bundle).
  </acceptance_criteria>
  <action>
    Add types per `<interfaces>`. Implement `src/openQuestionStore.ts`: `listOpenQuestions`, `getOpenQuestion`, `createOpenQuestion` (generates id + ISO `created_at`), `saveOpenQuestion`, `deleteOpenQuestion` using `SafeFileSystem`. Default frontmatter fields aligned to issue-schema (`type`, `status`, `title`, `created_at`, `source: author`, `chapter_ref`, `span_hint`, `agent_profile: author`, empty arrays/strings for unused contract keys). Store body as markdown below frontmatter. Enforce Phase-14 statuses only (`open`, `deferred`, `resolved`). Normalize `chapter_ref` on read/write. Export pure helpers used by tests: `parseOpenQuestionFile`, `serializeOpenQuestionFile`, and association round-trip mappers.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Store module exists; extension build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Counting helpers + writeChapterStatusEntry preservation + tests green</name>
  <files>src/openQuestionStore.ts, src/chapterStatus.ts, test/chapterStatus.test.ts</files>
  <read_first>
    - src/chapterStatus.ts
    - test/openQuestionStore.test.ts
    - test/openQuestionChapterCounts.test.ts
    - test/chapterStatus.test.ts
  </read_first>
  <behavior>
    - `writeChapterStatusEntry` preserves prior `openIssueCount` when updating status/title (change test that currently expects `0` to expect preserved value).
    - `countOpenQuestionsByChapter` (or equivalent) returns correct open counts per normalized `chapter_ref` for `status === open` only.
    - Book-wide and entity-only questions do not add to chapter path counts.
  </behavior>
  <acceptance_criteria>
    - `grep -n "current.openIssueCount" src/chapterStatus.ts` OR logical equivalent preserving count.
    - `npm run build:test && node --test dist-test/openQuestionStore.test.js dist-test/openQuestionChapterCounts.test.js dist-test/chapterStatus.test.js`
  </acceptance_criteria>
  <action>
    Implement `mergeOpenQuestionCountsIntoChapterStatusIndex(index, questions): ChapterStatusIndex` **or** `applyOpenQuestionCountsToEntries` as needed, in `openQuestionStore.ts` (or colocate in `chapterStatus.ts` if cleaner — prefer single export surface from `openQuestionStore.ts` for questions). Fix `writeChapterStatusEntry` (lines ~160–166) to set `openIssueCount: current.openIssueCount` instead of `0` per 14-RESEARCH Pitfall 1. Update `test/chapterStatus.test.ts` "writes and reloads" case to: pre-seed index with `openIssueCount: 2`, call `writeChapterStatusEntry`, assert count still `2`. Run store + count tests to GREEN.
  </action>
  <verify>
    <automated>npm run build:test && node --test dist-test/openQuestionStore.test.js dist-test/openQuestionChapterCounts.test.js dist-test/chapterStatus.test.js</automated>
  </verify>
  <done>All three test files pass; chapter status write no longer clobbers counts.</done>
</task>

</tasks>

<verification>
`npm run build:test && npm test` recommended after this plan (wave merge).
</verification>

<success_criteria>
ISSUE-01 partial data shape and ISSUE-02 partial statuses are enforced in code; D-03 counting and pitfall fix are test-backed.
</success_criteria>

<output>
After completion, create `.planning/phases/14-open-questions/14-02-SUMMARY.md`
</output>
