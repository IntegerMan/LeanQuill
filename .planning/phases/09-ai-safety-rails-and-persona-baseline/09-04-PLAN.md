---
phase: 09-ai-safety-rails-and-persona-baseline
plan: 04
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
  - "09-03"
files_modified:
  - src/safeFileSystem.ts
  - src/metadataActionApplier.ts
  - src/initialize.ts
  - src/extension.ts
  - src/storyChatContext.ts
  - test/safeFileSystem.test.ts
  - test/metadataActionContract.test.ts
  - test/metadataActionApplier.test.ts
  - docs/manuscript-safety.md
autonomous: true
requirements:
  - PER-01
user_setup: []
must_haves:
  truths:
    - "`applyMetadataAction` uses a `SafeFileSystem` instance that cannot write `manuscript/Book.txt` even if caller passes a fully configured author `SafeFileSystem` (D-09 defense-in-depth for AI metadata apply path)."
    - "`validateMetadataAction` continues to block `manuscript/ch01.md` and `manuscript/Book.txt` (D-08/D-09); tests assert blocked cases."
    - "On workspace activation with valid `project.yaml`, persona resolution warnings/errors surface via `vscode.window.showWarningMessage` when non-empty and detailed lines go to the existing `LeanQuill` output channel (D-15)."
  artifacts:
    - path: "src/safeFileSystem.ts"
      provides: "Optional constructor flag denying Book.txt writes"
      contains: "Book.txt"
    - path: "src/metadataActionApplier.ts"
      provides: "Metadata apply uses constrained SafeFileSystem"
      contains: "SafeFileSystem"
    - path: "docs/manuscript-safety.md"
      provides: "Writable table includes `notes/settings` default row"
      contains: "settings"
  key_links:
    - from: "src/metadataActionApplier.ts"
      to: "src/safeFileSystem.ts"
      via: "new SafeFileSystem(rootPath, options) + allowPath mirrors"
      pattern: "deny|Book|purpose|metadata"
---

<objective>
Harden the AI metadata apply path against manuscript writes (D-08–D-11), wire persona resolution UX on activation (D-15), expose active personas read-only to story chat context (downstream API), and align safety docs with configured folder allowances.

Purpose: Roadmap success criterion 3 + PER-01 operational integration in the running extension.
Output: SafeFileSystem option, applier-internal FS construction, extension + story chat wiring, tests, doc row fix.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-safety-rails-and-persona-baseline/09-CONTEXT.md (D-08–D-11, D-15)
@.planning/phases/09-ai-safety-rails-and-persona-baseline/09-RESEARCH.md § SafeFileSystem and metadata
@src/safeFileSystem.ts
@src/metadataActionApplier.ts
@src/metadataActionContract.ts
@src/extension.ts
@src/storyChatContext.ts
@docs/manuscript-safety.md
@src/personaStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SafeFileSystem deny Book.txt option + applier wiring</name>
  <files>src/safeFileSystem.ts,src/metadataActionApplier.ts,test/safeFileSystem.test.ts,test/metadataActionApplier.test.ts</files>
  <read_first>
    - src/safeFileSystem.ts
    - src/metadataActionApplier.ts
    - src/extension.ts
    - test/safeFileSystem.test.ts
    - test/metadataActionApplier.test.ts
  </read_first>
  <acceptance_criteria>
    - `rg -n "constructor\\(.*rootPath" src/safeFileSystem.ts` shows extended signature with optional second argument **or** documented options parameter (e.g. `{ denyManuscriptBookTxt?: boolean }`) defaulting to author behavior unchanged.
    - New test proves `denyManuscriptBookTxt: true` (or equivalent) rejects `manuscript/Book.txt` write: `test/safeFileSystem.test.ts` contains substring `Book.txt` and `Blocked write` or `false` for `canWrite` — assert `canWrite(path.join(dir,"manuscript","Book.txt"), true) === false` when flag set.
    - `rg -n "new SafeFileSystem\\(" src/metadataActionApplier.ts` shows construction of apply-scoped instance inside `applyMetadataAction` before any `writeFile` / store mutation paths.
    - `npm run build:test && npm test` exits 0.
  </acceptance_criteria>
  <action>
    Extend `SafeFileSystem` constructor in `src/safeFileSystem.ts` with optional options object `{ denyManuscriptBookTxt?: boolean }` (exact name as implementer chooses — document in file header comment referencing D-09). When `denyManuscriptBookTxt === true`, `canWrite` must return **false** for relative path `manuscript${sep}Book.txt` even though author mode allows it. Implement `function configureProjectWritableRoots(safeFs: SafeFileSystem, rootPath: string, config: ProjectConfig): void` in `src/metadataActionApplier.ts` (or `src/projectWritableRoots.ts` if cleaner) that duplicates the **safe** allowPath logic from `extension.ts` L217–253: research folder with manuscript guard + default fallback, characters/threads/settings with manuscript guard + defaults; always allow `.leanquill/**` via implicit SafeFileSystem base rules. Inside `applyMetadataAction`, after loading `config`, create `const opFs = new SafeFileSystem(rootPath, { denyManuscriptBookTxt: true }); configureProjectWritableRoots(opFs, rootPath, config);` and use `opFs` for **all** filesystem writes and `canWrite` checks that gate mutations and log append compatibility (`appendMetadataActionLogEntry` must receive `opFs` or keep using author fs only if `canWrite` on `.leanquill/metadata-actions.jsonl` still passes — prefer single `opFs` for consistency). Replace uses of the passed-in `safeFs` parameter for writes with `opFs`; keep parameter in signature for backward compatibility but document it is ignored for writes. Update applier tests if they assert internal calls; extend `safeFileSystem.test.ts` for new flag behavior.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; npm test</automated>
  </verify>
  <done>Metadata apply path cannot write Book.txt under constrained fs; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Contract tests for Book.txt + grep audit note</name>
  <files>test/metadataActionContract.test.ts</files>
  <read_first>
    - src/metadataActionContract.ts
    - test/metadataActionContract.test.ts
    - .planning/phases/09-ai-safety-rails-and-persona-baseline/09-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `test/metadataActionContract.test.ts` includes a test whose name or body mentions `Book.txt` and asserts `validateMetadataAction` rejects low-risk memory op targeting `manuscript/Book.txt` (`ok === false`).
    - `rg "manuscript/Book\\.txt" test/metadataActionContract.test.ts` returns ≥1 line.
    - `npm run build:test && npm test` exits 0.
  </acceptance_criteria>
  <action>
    Add `test` case using existing `baseAction` helper with `targetPath: "manuscript/Book.txt"` and `operation: "createMemory"` (or another op) expecting `ok === false` and `blockedReasons` or `errors` non-empty. Add comment block at bottom of test file documenting manual grep audit from 09-VALIDATION.md: `rg "fs\\.writeFile\\(" src` and `rg "appendFile\\(" src` — executor / reviewer verifies manuscript writes only occur in `initialize.ts` / `leanpubScaffold.ts` / `bookTxtSync.ts` (list may be adjusted to match actual repo after grep); no behavior change required if audit clean.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; npm test</automated>
  </verify>
  <done>Book.txt blocked at contract layer explicitly tested.</done>
</task>

<task type="auto">
  <name>Task 3: Activation persona warnings + story chat bundle + docs</name>
  <files>src/extension.ts,src/storyChatContext.ts,docs/manuscript-safety.md</files>
  <read_first>
    - src/extension.ts
    - src/personaStore.ts
    - src/storyChatContext.ts
    - docs/manuscript-safety.md
    - .planning/phases/09-ai-safety-rails-and-persona-baseline/09-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "getEnabledPersonasForProject" src/extension.ts` ≥1 call in activation path after config available.
    - `rg -n "showWarningMessage" src/extension.ts` triggers when `warnings.length + errors.length > 0` from persona resolution (substring `persona` or `Persona` in user-visible string).
    - `rg -n "activePersonas|personas" src/storyChatContext.ts` shows new optional field on `StoryChatContextBundle` or summary builder listing enabled persona `id` + `name` read-only.
    - `docs/manuscript-safety.md` table row mentions **`notes/settings`** path for Place profiles (mirror default `folders.settings` from project schema).
    - `npm run build:test && npm test` exits 0.
  </acceptance_criteria>
  <action>
    In `src/extension.ts` inside `if (config)` block after `ensureLeanquillDefaultPersonas`: optionally **before** persona resolution, if `readProjectYamlRaw(rootPath)` returns text matching `/active_personas:\\s*\\[\\]\\s*$/m` or equivalent empty list, rewrite `project.yaml` via `safeFileSystem.writeFile` to replace that segment with the same three-entry `active_personas` block as `renderProjectYaml` (import shared YAML snippet from `initialize.ts` export **or** duplicate minimal literal — avoid circular imports); only when `parseActivePersonas(raw).length === 0` per PER-01 legacy backfill. Then `await` or `void` IIFE: call `getEnabledPersonasForProject(rootPath)`; if `warnings.length || errors.length`, join first N messages and `log.warn` each line to existing `log` output channel; `vscode.window.showWarningMessage` with single concise string including counts, e.g. `LeanQuill: ${warnings.length} persona warning(s) — see Output › LeanQuill` (D-15). Find `openStoryChat` / `buildStoryChatContextBundle` call chain in `extension.ts` + `storyChatContext.ts`; extend `StoryChatContextBundle` with optional `activePersonas?: Array<{ id: string; name: string; type: string }>` populated from `getEnabledPersonasForProject` result **personas** map (sync field subset). Ensure `buildStoryChatContextSummary` appends a short bullet list of active personas when present (read-only advisory text). Update `docs/manuscript-safety.md` writable-path table to include `notes/settings/**/*.md` (or configured settings folder) matching `metadataActionApplier` `configuredMetadataWritableRoots` defaults.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; npm test</automated>
  </verify>
  <done>D-15 wiring + story chat read-only persona context + doc accuracy; tests green.</done>
</task>

</tasks>

<verification>
Optional F5: break one persona file’s frontmatter while enabled → expect warning toast + output lines (manual per 09-VALIDATION.md).
</verification>

<success_criteria>
Defense-in-depth for metadata apply path (D-08–D-09), visible persona health feedback (D-15), story chat carries persona list, docs match allowlist.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-safety-rails-and-persona-baseline/09-04-SUMMARY.md`
</output>
