---
phase: 09-ai-safety-rails-and-persona-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - src/types.ts
  - src/personaStore.ts
  - test/personaStore.test.ts
autonomous: true
requirements:
  - PER-01
user_setup: []
must_haves:
  truths:
    - "`resolveActivePersonas` skips `enabled: false` entries without reading disk (D-07)."
    - "Missing enabled persona file yields a warning string containing the id (D-06) and omits that persona from the resolved list."
    - "Invalid persona files (missing `---` fence or missing required keys per D-12) are skipped with errors collected; valid personas still returned (D-14)."
  artifacts:
    - path: "src/personaStore.ts"
      provides: "load, validate, resolve, getEnabledPersonasForProject"
      contains: "resolveActivePersonas"
    - path: "test/personaStore.test.ts"
      provides: "PER-01b/c automated coverage"
      contains: "resolveActivePersonas"
  key_links:
    - from: "src/personaStore.ts"
      to: "src/types.ts"
      via: "ActivePersonaEntry + PersonaRecord types"
      pattern: "ActivePersonaEntry"
---

<objective>
Implement persona file IO, hybrid validation (D-12–D-14), and `resolveActivePersonas` / `getEnabledPersonasForProject` API for runtime consumers (extension + story chat in 09-04).

Purpose: PER-01 custom personas + runtime enable/disable semantics; single module for Phase 10/17 reuse.
Output: `personaStore.ts`, extended types if needed, `test/personaStore.test.ts`.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-safety-rails-and-persona-baseline/09-CONTEXT.md (D-06, D-07, D-12–D-14)
@Imported/data-contracts/persona-schema.md
@src/characterStore.ts (frontmatter fence + line parsing pattern)
@src/types.ts
@src/projectConfig.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PersonaRecord types + personaStore skeleton</name>
  <files>src/types.ts,src/personaStore.ts</files>
  <read_first>
    - Imported/data-contracts/persona-schema.md
    - src/types.ts
    - src/characterStore.ts
  </read_first>
  <acceptance_criteria>
    - `rg -n "export interface PersonaRecord|export type PersonaType" src/types.ts` shows persona-related exports.
    - `rg -n "export async function getEnabledPersonasForProject|export function resolveActivePersonas" src/personaStore.ts` matches exported APIs.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <action>
    Extend `src/types.ts` with runtime types mirroring schema: `PersonaType` union matching schema `type` values; nested `PersonaExpertise`, `PersonaContextAccess`, `PersonaFeedback` interfaces with the field names from `persona-schema.md` (use `context_access` / snake_case keys in TS as `context_access` property names matching YAML). `export interface PersonaRecord { ...; rawFrontmatterUnknown?: Record&lt;string, string&gt;; }` or similar for D-13 forward-compat (store unknown top-level scalar lines only — document). Create `src/personaStore.ts` exporting: `loadPersonaFile(rootPath: string, personaId: string): Promise&lt;{ ok: true; markdown: string } | { ok: false; reason: string }&gt;` reading `.leanquill/personas/${personaId}.md` via `fs.readFile` (UTF-8). Stub `validatePersonaMarkdown(markdown: string, fileLabel: string)` returning structured result type until Task 2 fills it — must compile.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Types + module compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hybrid validation + resolveActivePersonas implementation</name>
  <files>src/personaStore.ts,test/personaStore.test.ts</files>
  <read_first>
    - src/personaStore.ts
    - Imported/data-contracts/persona-schema.md
    - .planning/phases/09-ai-safety-rails-and-persona-baseline/09-CONTEXT.md
    - test/threadStore.test.ts
  </read_first>
  <behavior>
    - `resolveActivePersonas(root, [{id:"casual-reader",enabled:false},{id:"avid-genre-fan",enabled:true}])` reads at most `avid-genre-fan.md` (use temp dir fixtures).
    - Enabled missing file → warning includes substring `avid-genre-fan` and `missing` or `not found` (case-insensitive ok in test).
    - Invalid frontmatter (no closing `---`) → skip + error; second valid persona still returned.
    - Unknown top-level key `x_custom: hello` in frontmatter does not fail hard parse (D-13) — persona still valid if core keys present.
  </behavior>
  <acceptance_criteria>
    - File `test/personaStore.test.ts` exists.
    - `npm run build:test && npm test` exits 0.
    - `rg -n "resolveActivePersonas" test/personaStore.test.ts` has ≥3 tests.
  </acceptance_criteria>
  <action>
    Implement `validatePersonaMarkdown` per D-12: hard-fail if no `/^---\\n([\\s\\S]*?)\\n---/` match; require non-empty `id`, `name`, `type` keys in frontmatter; parse nested blocks for `expertise`, `context_access`, `feedback` with required subkeys per `persona-schema.md` (if a nested key missing → hard-fail for that file). Non-critical numeric out-of-range → clamp 0..1 with optional soft warning string. Implement `resolveActivePersonas(rootPath: string, entries: ActivePersonaEntry[]): Promise&lt;{ personas: PersonaRecord[]; warnings: string[]; errors: string[] }&gt;` per D-06/D-07/D-14. Implement `getEnabledPersonasForProject(rootPath: string): Promise&lt;{ personas: PersonaRecord[]; warnings: string[]; errors: string[] }&gt;` by reading `.leanquill/project.yaml` via `readProjectYamlRaw` + `parseActivePersonas` from `projectConfig.ts`. Tests: use `fs.mkdtemp` + write persona files under `.leanquill/personas/`; minimal valid frontmatter bodies; cover behaviors above.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; npm test</automated>
  </verify>
  <done>Validation + resolver behaviors proven by tests.</done>
</task>

</tasks>

<verification>
`rg "personaStore" src/extension.ts` may be 0 until 09-04 — this plan stays library-only.
</verification>

<success_criteria>
Runtime persona resolution with D-06/D-07/D-12–D-14 semantics and automated PER-01b/c coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-safety-rails-and-persona-baseline/09-03-SUMMARY.md`
</output>
