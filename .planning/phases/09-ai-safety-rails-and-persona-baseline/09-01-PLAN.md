---
phase: 09-ai-safety-rails-and-persona-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/projectConfig.ts
  - test/projectConfig.test.ts
autonomous: true
requirements:
  - PER-01
user_setup: []
must_haves:
  truths:
    - "`ActivePersonaEntry` exists in `src/types.ts` and is the canonical `{ id, enabled }` shape per D-04."
    - "`parseActivePersonas` returns deterministic arrays for valid YAML and `[]` for missing/malformed blocks without throwing."
    - "`npm run build:test && npm test` exits 0 after this plan."
  artifacts:
    - path: "src/types.ts"
      provides: "ActivePersonaEntry (+ optional PersonaType alias if needed for later plans)"
      contains: "ActivePersonaEntry"
    - path: "src/projectConfig.ts"
      provides: "parseActivePersonas(content: string)"
      contains: "parseActivePersonas"
    - path: "test/projectConfig.test.ts"
      provides: "PER-01b parsing coverage"
      contains: "parseActivePersonas"
  key_links:
    - from: "test/projectConfig.test.ts"
      to: "src/projectConfig.ts"
      via: "import parseActivePersonas"
      pattern: "parseActivePersonas"
---

<objective>
Add shared types and line-oriented `project.yaml` parsing for `active_personas` so later plans can seed, load, and resolve personas without ad hoc string handling.

Purpose: Satisfies PER-01 config half (enable/disable) and D-04 locked shape before init and personaStore consume it.
Output: Exported `ActivePersonaEntry`, `parseActivePersonas`, and regression tests.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-safety-rails-and-persona-baseline/09-CONTEXT.md (D-04, D-05, D-07)
@.planning/phases/09-ai-safety-rails-and-persona-baseline/09-RESEARCH.md
@Imported/data-contracts/project-config-schema.md
@src/projectConfig.ts
@src/types.ts
@test/projectConfig.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivePersonaEntry type in types.ts</name>
  <files>src/types.ts</files>
  <read_first>
    - Imported/data-contracts/project-config-schema.md
    - src/types.ts
    - .planning/phases/09-ai-safety-rails-and-persona-baseline/09-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "export interface ActivePersonaEntry" src/types.ts` returns at least one line.
    - `rg -n "enabled: boolean" src/types.ts` appears inside `ActivePersonaEntry` block (same file).
    - `rg -n "id: string" src/types.ts` appears inside `ActivePersonaEntry` block.
  </acceptance_criteria>
  <action>
    Append to `src/types.ts` (match neighboring comment style): `export interface ActivePersonaEntry { id: string; enabled: boolean; }` exactly matching D-04 / `project-config-schema.md` `active_personas` list item. No runtime code in this file.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Interface exported; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parseActivePersonas + projectConfig tests</name>
  <files>src/projectConfig.ts,test/projectConfig.test.ts</files>
  <read_first>
    - src/projectConfig.ts
    - src/types.ts
    - test/projectConfig.test.ts
    - Imported/data-contracts/project-config-schema.md
  </read_first>
  <behavior>
    - Given YAML with `active_personas:` followed by three `- id: ...` / `enabled: true|false` entries, `parseActivePersonas` returns three `ActivePersonaEntry` objects with correct ids and booleans.
    - Given YAML with no `active_personas:` key, returns `[]`.
    - Given `active_personas: []` (flow style empty), returns `[]`.
    - CRLF-only files normalize like existing `parseProjectConfig` (same `replace(/\\r\\n/g, "\\n")` pattern).
  </behavior>
  <acceptance_criteria>
    - `rg -n "function parseActivePersonas|export function parseActivePersonas" src/projectConfig.ts` matches exactly one exported function.
    - `rg -n "parseActivePersonas" test/projectConfig.test.ts` has at least 4 test blocks or `test(` calls covering enabled true/false, three defaults ids, and empty/missing.
    - `npm run build:test` exits 0.
    - `npm test` exits 0.
  </acceptance_criteria>
  <action>
    Implement `import type { ActivePersonaEntry } from "./types";` and `export function parseActivePersonas(content: string): ActivePersonaEntry[]` in `src/projectConfig.ts`. Algorithm (line-oriented, no new npm deps): normalize CRLF; locate `^active_personas:\\s*$`; consume subsequent lines while line matches `^\\s` OR starts list items; for each `- id:` line extract scalar id (quoted or bare); next non-list line `enabled:\\s*(true|false)` sets enabled (default `false` if missing for that block — document in code comment); ignore malformed list items without throwing (skip entry). Do **not** change `parseProjectConfig` return type in this plan — keep personas separate to avoid churn (per 09-RESEARCH.md). Add tests in `test/projectConfig.test.ts` **before** implementation passes RED then GREEN.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; npm test</automated>
  </verify>
  <done>All behavior bullets covered by tests; full test suite green.</done>
</task>

</tasks>

<verification>
`npm run build:test &amp;&amp; npm test` passes; `rg "parseActivePersonas" src` lists `projectConfig.ts` only (plus imports elsewhere after later plans).
</verification>

<success_criteria>
PER-01 parsing foundation: `ActivePersonaEntry` + `parseActivePersonas` with automated tests; ready for init seeding and persona resolution plans.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-safety-rails-and-persona-baseline/09-01-SUMMARY.md`
</output>
