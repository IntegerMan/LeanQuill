---
phase: 09-ai-safety-rails-and-persona-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personaDefaults.ts
  - src/initialize.ts
  - src/extension.ts
  - test/initializePersonas.test.ts
autonomous: true
requirements:
  - PER-01
user_setup: []
must_haves:
  truths:
    - "`renderProjectYaml` emits non-empty `active_personas` with exactly `casual-reader`, `avid-genre-fan`, `copy-editor`, all `enabled: true` (D-01, D-02, D-05)."
    - "Fresh `initializeProject` creates three files under `.leanquill/personas/*.md` whose frontmatter `id` matches filename stem."
    - "`ensureLeanquillDefaultPersonas` (or equivalent name) creates missing packaged files with exclusive-create (`wx`) and never overwrites existing persona markdown."
  artifacts:
    - path: "src/personaDefaults.ts"
      provides: "Packaged persona markdown bodies + id list constant"
      contains: "casual-reader"
    - path: "src/initialize.ts"
      provides: "renderProjectYaml personas block + init writes + backfill helper"
      contains: "active_personas"
    - path: "test/initializePersonas.test.ts"
      provides: "PER-01a disk assertions for packaged files"
      contains: "personas"
  key_links:
    - from: "src/initialize.ts"
      to: "src/personaDefaults.ts"
      via: "import packaged markdown"
      pattern: "personaDefaults"
---

<objective>
Ship packaged default persona markdown files and seed `project.yaml` `active_personas` on new init; backfill missing packaged files (and empty persona lists) for legacy workspaces per D-02 and 09-RESEARCH.md migration note.

Purpose: Roadmap success criterion 1 (library on disk + defaults) and criterion 2 (config lists enabled personas).
Output: `personaDefaults.ts`, updated `initialize.ts`, activation hook in `extension.ts`, focused tests.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-safety-rails-and-persona-baseline/09-CONTEXT.md (D-01–D-03, D-05)
@.planning/phases/09-ai-safety-rails-and-persona-baseline/09-RESEARCH.md § Persona packaging
@Imported/data-contracts/persona-schema.md
@src/initialize.ts
@src/safeFileSystem.ts
@src/extension.ts (ensureLeanquillWorkflows call site ~L258)
</context>

<tasks>

<task type="auto">
  <name>Task 1: personaDefaults.ts with three schema-valid bodies</name>
  <files>src/personaDefaults.ts</files>
  <read_first>
    - Imported/data-contracts/persona-schema.md
    - .planning/phases/09-ai-safety-rails-and-persona-baseline/09-CONTEXT.md
    - src/characterStore.ts
  </read_first>
  <acceptance_criteria>
    - `rg -n "casual-reader|avid-genre-fan|copy-editor" src/personaDefaults.ts` matches at least 9 lines total (three ids referenced across constants/templates).
    - `rg -n "context_access:" src/personaDefaults.ts` count is ≥ 3 (one per packaged file content or per template).
    - `rg -n "feedback:" src/personaDefaults.ts` count is ≥ 3.
    - D-03 differentiation is machine-checkable only: `rg -n "genre_familiarity: 0\\.35" src/personaDefaults.ts` emits ≥1 line and `rg -n "genre_familiarity: 0\\.85" src/personaDefaults.ts` emits ≥1 line (fixed literals: 0.85 is greater than 0.35). `rg -c "type: beta-reader" src/personaDefaults.ts` prints count **2** (casual + avid). `rg -n "type: copy-editor" src/personaDefaults.ts` emits ≥1 line. `rg -c "scope: sequential" src/personaDefaults.ts` prints count **1**. `rg -c "include_research_notes: true" src/personaDefaults.ts` prints count **1** (copy-editor only).
  </acceptance_criteria>
  <action>
    Create `src/personaDefaults.ts` exporting: (1) `export const PACKAGED_PERSONA_IDS = ["casual-reader","avid-genre-fan","copy-editor"] as const` (exact order); (2) `export function packagedPersonaMarkdown(id: typeof PACKAGED_PERSONA_IDS[number]): string` returning full file contents: opening `---\\n`, YAML frontmatter per `persona-schema.md` (all required keys: `id`, `name`, `type`, `expertise` nested map, `context_access`, `feedback`, multiline or block `description`), closing `---\\n`, then a short non-empty markdown body line. Numeric expertise fields in 0..1. **Lock these literals for Task 1 acceptance greps (D-03):** `casual-reader` YAML must contain `genre_familiarity: 0.35`; `avid-genre-fan` must contain `genre_familiarity: 0.85`; `copy-editor` must contain `genre_familiarity: 0.55`. Exactly two personas use `type: beta-reader` (casual + avid); `copy-editor` uses `type: copy-editor`. Exactly one packaged body uses `scope: sequential` (copy-editor); only `copy-editor` sets `include_research_notes: true`. Implement reader-stricter copy-editor (e.g. more `copy-edit` / `narrative-quality` in `allowed_types`, `tone: critical`); `casual-reader` widest reader lens with `interest_heatmap` / `reader_question_stream` toggles per schema; `avid-genre-fan` distinct `primary_focus` ordering vs casual. Use exact ids in frontmatter `id:` line matching filename stem requirement.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Module compiles; grep checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: renderProjectYaml + initializeProject write personas</name>
  <files>src/initialize.ts</files>
  <read_first>
    - src/initialize.ts
    - src/personaDefaults.ts
    - src/safeFileSystem.ts
    - Imported/data-contracts/project-config-schema.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "active_personas:" -A6 src/initialize.ts` shows three `- id:` lines for the three packaged ids with `enabled: true`.
    - `rg -n "packagedPersonaMarkdown|personaDefaults" src/initialize.ts` shows import and use in `initializeProject`.
    - `rg -n "\\.leanquill\", \"personas\"" src/initialize.ts` confirms mkdir still precedes file writes.
  </acceptance_criteria>
  <action>
    Update `renderProjectYaml` in `src/initialize.ts`: replace the single line `active_personas: []` with a YAML list literal of three objects `{ id: casual-reader, enabled: true }`, `{ id: avid-genre-fan, enabled: true }`, `{ id: copy-editor, enabled: true }` using the same quoting style as neighboring keys (D-05). In `initializeProject` after `safeFs.mkdir` for `.leanquill/personas`, for each `PACKAGED_PERSONA_IDS` entry call `safeFs.writeFile(path.join(rootPath, ".leanquill", "personas", \`\${id}.md\`), packagedPersonaMarkdown(id))` so fresh init always writes packaged files (D-02). Export new async function `ensureLeanquillDefaultPersonas(rootPath: string, safeFs?: SafeFileSystem)` in the same file: default `safeFs = new SafeFileSystem(rootPath)`; `mkdir` personas dir; for each packaged id, `fs.writeFile(target, content, { flag: "wx", encoding: "utf8" })` mirroring `ensureLeanquillWorkflows` EEXIST swallow pattern — content from `packagedPersonaMarkdown`; paths must stay under `.leanquill/personas/` (D-02, non-destructive for edits). Defer `project.yaml` `active_personas: []` text backfill to plan **09-04** if still needed after activation seeding.
  </action>
  <verify>
    <automated>npm run build:test</automated>
  </verify>
  <done>`initialize.ts` changes compile; `npm run build:test` exits 0 (full `npm test` is Task 3 verify).</done>
</task>

<task type="auto">
  <name>Task 3: Activation hook + initializePersonas tests</name>
  <files>src/extension.ts,test/initializePersonas.test.ts</files>
  <read_first>
    - src/extension.ts
    - src/initialize.ts
    - test/projectConfig.test.ts
  </read_first>
  <acceptance_criteria>
    - `rg -n "ensureLeanquillDefaultPersonas" src/extension.ts` shows `void ...catch` alongside `ensureLeanquillWorkflows` when `config` is non-null (same guarded branch).
    - `test/initializePersonas.test.ts` exists and `rg -n "mkdtemp|personas" test/initializePersonas.test.ts` shows temp-dir test.
    - `npm run build:test && npm test` exits 0.
  </acceptance_criteria>
  <action>
    Import `ensureLeanquillDefaultPersonas` from `./initialize` in `src/extension.ts`. After `void ensureLeanquillWorkflows(rootPath, safeFileSystem)...` (inside the `if (config)` block ~L258), add `void ensureLeanquillDefaultPersonas(rootPath, safeFileSystem).catch(() => { /* non-critical */ });`. Add `test/initializePersonas.test.ts`: create temp workspace with `.leanquill/project.yaml` minimal valid via `validateProjectYamlForSetup` helper from projectConfig tests pattern, mkdir `.leanquill` only, run `ensureLeanquillDefaultPersonas`, assert `fs.access` on three expected paths under `.leanquill/personas/`; second call does not throw and does not change byte length if user pre-created one file with different content (EEXIST path).
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; npm test</automated>
  </verify>
  <done>Activation wiring + tests satisfy acceptance greps; suite green.</done>
</task>

</tasks>

<verification>
Manual optional: run **LeanQuill: Initialize** in Extension Host on empty folder — confirm three persona files (post Phase execution).
</verification>

<success_criteria>
Packaged persona library files and `project.yaml` defaults exist; legacy activation backfills missing packaged files without overwrite.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-safety-rails-and-persona-baseline/09-02-SUMMARY.md`
</output>
