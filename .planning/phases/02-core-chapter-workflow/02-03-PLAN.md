---
phase: 02-core-chapter-workflow
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/chapterContextPane.ts
  - test/chapterContextPane.test.ts
autonomous: true
requirements:
  - CHAP-04
must_haves:
  truths:
    - "Active chapter context is visible in a dedicated sidebar pane."
    - "Pane presents status and open-issue summary with no notes editor in Phase 2."
    - "Pane can retain and re-render last known chapter context when active editor is not a chapter."
  artifacts:
    - path: "src/chapterContextPane.ts"
      provides: "WebviewViewProvider for chapter context rendering and refresh entry points."
  key_links:
    - from: "src/chapterContextPane.ts"
      to: "leanquill.updateChapterStatus"
      via: "primary action hook for status updates"
      pattern: "updateChapterStatus"
    - from: "src/chapterContextPane.ts"
      to: "Content-Security-Policy"
      via: "webview HTML security header"
      pattern: "Content-Security-Policy"
---

<objective>
Deliver the native Chapter Context webview provider aligned with approved UI and interaction contract.

Purpose: Satisfy CHAP-04 by exposing chapter status context that auto-refresh logic can target.
Output: Script-free, CSP-compliant context pane module with explicit refresh/update entry points.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-chapter-workflow/02-CONTEXT.md
@.planning/phases/02-core-chapter-workflow/02-UI-SPEC.md
@.planning/phases/02-core-chapter-workflow/02-RESEARCH.md
@src/types.ts
@src/chapterStatus.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Chapter Context WebviewViewProvider skeleton and state model</name>
  <read_first>
    - .planning/phases/02-core-chapter-workflow/02-RESEARCH.md
    - .planning/phases/02-core-chapter-workflow/02-UI-SPEC.md
    - src/types.ts
  </read_first>
  <files>src/chapterContextPane.ts</files>
  <action>
    Create ChapterContextPaneProvider with resolveWebviewView lifecycle and retained view reference.
    Add explicit methods for setActiveChapterContext and refreshAfterStatusUpdate to support D-10 refresh triggers.
    Preserve last-known chapter state when incoming editor path is non-chapter per D-11.
  </action>
  <acceptance_criteria>
    - grep -n "class ChapterContextPaneProvider" src/chapterContextPane.ts confirms provider class exists.
    - grep -n "setActiveChapter" src/chapterContextPane.ts confirms chapter update entry point.
    - grep -n "last" src/chapterContextPane.ts confirms retained context behavior implementation.
  </acceptance_criteria>
  <behavior>
    - Provider can render before/after chapter activation.
    - Non-chapter activation does not clear last-known chapter content.
  </behavior>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Context pane provider supports stateful rendering and refresh hooks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render approved context pane HTML contract with CSP and no notes section</name>
  <read_first>
    - .planning/phases/02-core-chapter-workflow/02-UI-SPEC.md
    - src/chapterContextPane.ts
  </read_first>
  <files>src/chapterContextPane.ts</files>
  <action>
    Implement static HTML/CSS rendering for chapter title, status block, open issue block, and helper text per UI-SPEC.
    Add strict CSP meta tag and keep scripts disabled, per research/security requirements.
    Configure the webview with enableCommandUris=true and render Update Status as a command URI anchor to trigger leanquill.updateChapterStatus from the script-free pane.
    Omit notes editing and show issue count only (0 in this phase) per D-14 and D-15.
  </action>
  <acceptance_criteria>
    - grep -n "Content-Security-Policy" src/chapterContextPane.ts confirms CSP present.
    - grep -n "default-src 'none'" src/chapterContextPane.ts confirms strict policy.
    - grep -n "enableCommandUris" src/chapterContextPane.ts confirms script-free command URI support.
    - grep -n "command:leanquill.updateChapterStatus" src/chapterContextPane.ts confirms pane action wiring.
    - grep -n "Update Status" src/chapterContextPane.ts confirms primary action copy contract.
    - grep -n "notes" src/chapterContextPane.ts does not introduce editable notes controls for Phase 2.
  </acceptance_criteria>
  <behavior>
    - Pane displays chapter status and issue count with theme-token styling.
    - Pane copy and structure match approved UI contract.
  </behavior>
  <verify>
    <automated>npm run build && grep -n "Content-Security-Policy" src/chapterContextPane.ts</automated>
  </verify>
  <done>Context pane HTML is contract-compliant, secure, and scope-correct for Phase 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add behavioral tests for context pane HTML rendering</name>
  <read_first>
    - test/chapterOrder.test.ts
    - src/chapterContextPane.ts
  </read_first>
  <files>test/chapterContextPane.test.ts</files>
  <action>
    Add tests for pure HTML rendering helpers to verify empty state copy, active chapter status rendering, retained-context helper text, and persistent CSP inclusion.
    Keep tests vscode-free by targeting exported pure render helpers and deterministic input fixtures.
  </action>
  <acceptance_criteria>
    - test/chapterContextPane.test.ts includes assertions for empty-state and active-chapter rendering.
    - grep -n "Content-Security-Policy" test/chapterContextPane.test.ts confirms CSP assertions are present.
    - npm test passes with chapterContextPane tests enabled.
  </acceptance_criteria>
  <behavior>
    - Empty state renders before any chapter activation.
    - Last-known context helper copy renders for non-chapter transitions.
  </behavior>
  <verify>
    <automated>npm run build && npm run build:test && npm test</automated>
  </verify>
  <done>Context pane rendering behavior gains regression coverage independent of manual host checks.</done>
</task>

</tasks>

<verification>
- npm run build
- npm run build:test
- npm test
- grep -n "Content-Security-Policy" src/chapterContextPane.ts
</verification>

<success_criteria>
- Webview provider exists with stable refresh API for integration wiring.
- HTML output follows UI-SPEC and CSP constraints.
- Phase 2 keeps notes out of the pane while preserving status and issue context.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-chapter-workflow/02-03-SUMMARY.md`
</output>
