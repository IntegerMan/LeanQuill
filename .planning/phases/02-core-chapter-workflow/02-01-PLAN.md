---
phase: 02-core-chapter-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/chapterStatus.ts
  - test/chapterStatus.test.ts
autonomous: true
requirements:
  - CHAP-01
  - CHAP-03
must_haves:
  truths:
    - "Chapter records always resolve to a valid lifecycle status."
    - "Chapters without saved state resolve to not-started by default."
    - "Status updates persist to LeanQuill state and can be reloaded."
  artifacts:
    - path: "src/types.ts"
      provides: "Canonical chapter status and index contracts used by tree and context pane."
    - path: "src/chapterStatus.ts"
      provides: "Read/write status index helpers with validation and fallback behavior."
    - path: "test/chapterStatus.test.ts"
      provides: "Automated guardrails for defaulting, invalid status coercion, and persistence."
  key_links:
    - from: "src/chapterStatus.ts"
      to: "src/types.ts"
      via: "shared ChapterStatus and ChapterStatusIndex typings"
      pattern: "import .*ChapterStatus"
    - from: "src/chapterStatus.ts"
      to: ".leanquill/chapter-status-index.json"
      via: "status index read/write functions"
      pattern: "chapter-status-index\.json"
---

<objective>
Establish the chapter status data foundation consumed by all Phase 2 UI surfaces.

Purpose: Ensure status rendering and editing operate on one validated source of truth.
Output: Shared status contracts, persistence helpers, and focused tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-chapter-workflow/02-CONTEXT.md
@.planning/phases/02-core-chapter-workflow/02-RESEARCH.md
@Imported/contracts/chapter-status.schema.json
@src/types.ts
@src/safeFileSystem.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical chapter status contracts</name>
  <read_first>
    - src/types.ts
    - .planning/phases/02-core-chapter-workflow/02-CONTEXT.md
    - Imported/contracts/chapter-status.schema.json
  </read_first>
  <files>src/types.ts</files>
  <action>
    Add exported types for ChapterStatus, ChapterStatusEntry, and ChapterStatusIndex in src/types.ts using the exact status enum from D-01.
    Include openIssueCount on each entry and keep default semantics for missing chapter state aligned to D-03.
    Keep naming stable for downstream imports (tree, context pane, extension wiring).
  </action>
  <acceptance_criteria>
    - grep -n "export type ChapterStatus" src/types.ts returns one canonical union with: planning, not-started, drafting, draft-complete, editing, review-pending, final.
    - grep -n "export interface ChapterStatusIndex" src/types.ts confirms chapters map contract.
  </acceptance_criteria>
  <behavior>
    - Unknown stored status values are considered invalid inputs for parsing.
    - Missing chapter entries resolve to default not-started status.
  </behavior>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Shared status contracts compile and are importable from src/types.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chapter status index read/write with validation</name>
  <read_first>
    - src/safeFileSystem.ts
    - src/initialize.ts
    - .planning/phases/02-core-chapter-workflow/02-RESEARCH.md
  </read_first>
  <files>src/chapterStatus.ts</files>
  <action>
    Create src/chapterStatus.ts with typed helpers to read and write .leanquill/chapter-status-index.json.
    Use fallback-to-not-started behavior for invalid statuses per D-04 and default empty index behavior per D-03.
    Implement writes through SafeFileSystem for mutation paths per D-02 and preserve openIssueCount=0 semantics for this phase per D-14.
  </action>
  <acceptance_criteria>
    - grep -n "chapter-status-index.json" src/chapterStatus.ts confirms fixed storage location under .leanquill.
    - grep -n "not-started" src/chapterStatus.ts confirms invalid/missing fallback handling.
    - grep -n "SafeFileSystem" src/chapterStatus.ts confirms guarded write path usage.
  </acceptance_criteria>
  <behavior>
    - Read path returns a valid ChapterStatusIndex even when file is missing or malformed.
    - Write path updates only the selected chapter entry and persists ISO updatedAt.
  </behavior>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Chapter status persistence is centralized, typed, and constrained to LeanQuill-safe writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add status persistence tests for defaulting and coercion</name>
  <read_first>
    - test/safeFileSystem.test.ts
    - test/chapterOrder.test.ts
    - src/chapterStatus.ts
  </read_first>
  <files>test/chapterStatus.test.ts</files>
  <action>
    Add focused unit tests for empty-index default behavior, invalid-status coercion to not-started (D-04), and status write/read roundtrip.
    Use deterministic temporary directories and assert openIssueCount behavior is stable at 0 in Phase 2 (D-14).
  </action>
  <acceptance_criteria>
    - test/chapterStatus.test.ts exists and includes test names for default, coercion, and persistence.
    - npm test passes with chapterStatus tests included.
  </acceptance_criteria>
  <behavior>
    - Invalid status input yields warning-compatible normalized output.
    - Saved entry reloads with the chosen status and updated timestamp.
  </behavior>
  <verify>
    <automated>npm run build && npm run build:test && npm test</automated>
  </verify>
  <done>Status data layer has regression coverage for core Phase 2 state rules.</done>
</task>

</tasks>

<verification>
Run full compile and test pass after task completion:
- npm run build
- npm run build:test
- npm test
</verification>

<success_criteria>
- Status contracts and persistence compile without type errors.
- Default and invalid status handling are test-verified.
- Foundation is ready for tree/context UI plans without additional data-contract changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-chapter-workflow/02-01-SUMMARY.md`
</output>
