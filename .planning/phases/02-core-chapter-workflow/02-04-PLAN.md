---
phase: 02-core-chapter-workflow
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - package.json
  - src/extension.ts
autonomous: false
requirements:
  - CHAP-01
  - CHAP-02
  - CHAP-03
  - CHAP-04
must_haves:
  truths:
    - "Chapters and Chapter Context views are available in the LeanQuill container when initialized."
    - "Status updates can be triggered from tree context and context pane action path."
    - "Active chapter switches refresh context pane data without clearing on non-chapter files."
  artifacts:
    - path: "package.json"
      provides: "View, command, and menu contributions for chapter workflow."
    - path: "src/extension.ts"
      provides: "Provider registrations, status command flow, and active editor refresh wiring."
  key_links:
    - from: "package.json"
      to: "src/extension.ts"
      via: "matching command/view identifiers"
      pattern: "leanquill\.chapters|leanquill\.chapterContext|leanquill\.updateChapterStatus"
    - from: "src/extension.ts"
      to: "src/chapterTree.ts and src/chapterContextPane.ts"
      via: "registerTreeDataProvider and registerWebviewViewProvider"
      pattern: "registerTreeDataProvider|registerWebviewViewProvider"
---

<objective>
Wire Phase 2 providers and status-edit workflow into the extension manifest and activation path.

Purpose: Turn previously built provider modules into a complete user-visible chapter workflow.
Output: Manifest contributions and extension integration for CHAP-01 through CHAP-04.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-chapter-workflow/02-CONTEXT.md
@.planning/phases/02-core-chapter-workflow/02-UI-SPEC.md
@src/extension.ts
@package.json
@src/chapterTree.ts
@src/chapterContextPane.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest contributions for chapter views, command, and menu wiring</name>
  <read_first>
    - package.json
    - .planning/phases/02-core-chapter-workflow/02-UI-SPEC.md
    - .planning/phases/02-core-chapter-workflow/02-CONTEXT.md
  </read_first>
  <files>package.json</files>
  <action>
    Add contributes.views entries for leanquill.chapters (tree) and leanquill.chapterContext (webview) under the existing leanquill container.
    Add leanquill.updateChapterStatus command and view/item/context menu contribution for chapter rows (D-07).
    Gate view availability with initialization context where appropriate and retain existing initialize command contributions.
  </action>
  <acceptance_criteria>
    - grep -n "leanquill.chapters" package.json confirms chapter tree contribution.
    - grep -n "leanquill.chapterContext" package.json confirms context pane contribution.
    - grep -n "leanquill.updateChapterStatus" package.json confirms command and menu wiring.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Manifest exposes all Phase 2 surfaces and status update command hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Register providers and implement status-update orchestration in activation flow</name>
  <read_first>
    - src/extension.ts
    - src/chapterTree.ts
    - src/chapterContextPane.ts
    - src/chapterStatus.ts
  </read_first>
  <files>src/extension.ts</files>
  <action>
    Register ChapterTreeProvider and ChapterContextPaneProvider, load chapter order plus status index, and connect refresh paths.
    Register leanquill.updateChapterStatus command to show quick pick with the D-01 status list and persist updates via chapterStatus helpers (D-02, D-03, D-04, D-12).
    Trigger context updates on active editor changes (with Windows-safe path normalization) and after status updates per D-10; keep last-known pane data when active file is not a chapter per D-11.
  </action>
  <acceptance_criteria>
    - grep -n "registerTreeDataProvider(\"leanquill.chapters\"" src/extension.ts confirms tree registration.
    - grep -n "registerWebviewViewProvider(\"leanquill.chapterContext\"" src/extension.ts confirms pane registration.
    - grep -n "registerCommand(\"leanquill.updateChapterStatus\"" src/extension.ts confirms command orchestration.
    - grep -n "onDidChangeActiveTextEditor" src/extension.ts confirms auto-refresh hook.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run build:test && npm test</automated>
  </verify>
  <done>Extension activation path delivers the end-to-end chapter workflow with refresh and status update loops.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validate live UX flow in Extension Development Host</name>
  <read_first>
    - .planning/phases/02-core-chapter-workflow/02-UI-SPEC.md
    - src/extension.ts
    - package.json
  </read_first>
  <action>
    Launch the extension host and validate: chapter rows render in project order with status and issue count, single-click preview + double-click pin behavior, context menu status updates, and chapter context auto-refresh on chapter switches.
    Confirm non-chapter file activation keeps last known chapter context displayed (D-11).
  </action>
  <acceptance_criteria>
    - Chapters view and Chapter Context view are visible in LeanQuill container after initialization.
    - Status updates from tree/context path reflect immediately in tree and context pane.
    - Switching to a non-chapter file keeps the last chapter context visible with helper messaging.
  </acceptance_criteria>
  <how-to-verify>
    1. Run extension in debug host (F5) with an initialized LeanPub-style workspace.
    2. Click a chapter once and confirm preview tab opens; double-click to confirm pin.
    3. Right-click chapter row and run Update Chapter Status; choose a different status and confirm immediate UI update.
    4. Open another chapter and then a non-chapter file; confirm context pane updates for chapter and retains last context for non-chapter.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or list the failing behavior.</resume-signal>
  <verify>
    <automated>npm run build && npm run build:test && npm test</automated>
  </verify>
  <done>Live behavior validates CHAP-01 through CHAP-04 end-to-end in the running extension.</done>
</task>

</tasks>

<verification>
- npm run build
- npm run build:test
- npm test
- Manual Extension Host validation for core UX interactions
</verification>

<success_criteria>
- Manifest and activation wiring expose both native surfaces and status-edit command flow.
- Active chapter changes refresh context data as specified.
- End-to-end behavior is verified in both automated checks and live extension host UX pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-chapter-workflow/02-04-SUMMARY.md`
</output>
