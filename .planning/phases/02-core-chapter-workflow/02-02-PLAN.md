---
phase: 02-core-chapter-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/chapterTree.ts
  - test/chapterTree.test.ts
autonomous: true
requirements:
  - CHAP-01
  - CHAP-02
must_haves:
  truths:
    - "Author sees all chapters in project order with status and issue count metadata."
    - "Author can open chapter manuscript files directly from the chapter tree."
    - "Orphan manuscript files appear under Not Included and missing referenced files remain visible as warnings."
  artifacts:
    - path: "src/chapterTree.ts"
      provides: "TreeDataProvider and item mapping for ordered, missing, and Not Included chapter entries."
    - path: "test/chapterTree.test.ts"
      provides: "Automated checks for ordering, grouping, and open-command behavior."
  key_links:
    - from: "src/chapterTree.ts"
      to: "src/chapterStatus.ts"
      via: "status resolution for each tree row"
      pattern: "readChapterStatus|ChapterStatus"
    - from: "src/chapterTree.ts"
      to: "vscode.open"
      via: "TreeItem command for single-click preview"
      pattern: "command:\s*['\"]vscode.open['\"]"
---

<objective>
Build the native chapter tree provider implementing ordering, open behavior, and edge-case visibility rules.

Purpose: Deliver CHAP-01 and CHAP-02 user workflow in the sidebar while preserving LeanPub ordering semantics.
Output: Tree provider module and tests proving list construction and row command behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-chapter-workflow/02-CONTEXT.md
@.planning/phases/02-core-chapter-workflow/02-UI-SPEC.md
@.planning/phases/02-core-chapter-workflow/02-RESEARCH.md
@src/chapterOrder.ts
@src/types.ts
@src/chapterStatus.ts

<interfaces>
From src/chapterOrder.ts:
```typescript
export async function resolveChapterOrder(rootPath: string): Promise<ChapterOrderResult>
```

From src/types.ts (from 02-01 output):
```typescript
export type ChapterStatus = "planning" | "not-started" | "drafting" | "draft-complete" | "editing" | "review-pending" | "final";
export interface ChapterStatusEntry { status: ChapterStatus; openIssueCount: number; }
export interface ChapterStatusIndex { chapters: Record<string, ChapterStatusEntry>; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement chapter tree item model and hierarchy rules</name>
  <read_first>
    - src/chapterOrder.ts
    - src/chapterStatus.ts
    - .planning/phases/02-core-chapter-workflow/02-CONTEXT.md
    - .planning/phases/02-core-chapter-workflow/02-UI-SPEC.md
  </read_first>
  <files>src/chapterTree.ts</files>
  <action>
    Create ChapterTreeProvider and item types that render ordered chapter rows first (D-08), include status and issue count text (D-05), append Not Included group for orphan manuscript files (D-16), and preserve visible missing-file placeholders with warning icon/label (D-13).
    Use status icon mapping from UI-SPEC and phase research; keep issue counts visible as 0 in Phase 2 per D-14.
    Expose a pure data builder (for example buildChapterRows) that computes deterministic row metadata without requiring vscode imports so tests can run through build:test without vscode bundling failures.
  </action>
  <acceptance_criteria>
    - grep -n "Not Included" src/chapterTree.ts confirms explicit group node implementation.
    - grep -n "warning" src/chapterTree.ts confirms missing-file placeholder icon/state.
    - grep -n "openIssueCount" src/chapterTree.ts confirms issue count display path.
    - grep -n "buildChapterRows" src/chapterTree.ts confirms exported pure row-construction helper for vscode-safe tests.
  </acceptance_criteria>
  <behavior>
    - Ordered chapters keep chapter-order sequence.
    - Non-ordered manuscript files render only under Not Included.
  </behavior>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Tree model implements all required chapter row states without extension wiring yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add open behavior and context-menu-ready item metadata</name>
  <read_first>
    - src/chapterTree.ts
    - .planning/phases/02-core-chapter-workflow/02-RESEARCH.md
  </read_first>
  <files>src/chapterTree.ts</files>
  <action>
    Configure chapter leaf items with vscode.open command and preview=true for single-click preview/double-click pin behavior (D-06).
    Set contextValue variants for chapter and chapter-missing to support command menu targeting in integration plan (D-07).
  </action>
  <acceptance_criteria>
    - grep -n "vscode.open" src/chapterTree.ts confirms open command wiring.
    - grep -n "preview: true" src/chapterTree.ts confirms preview tab behavior.
    - grep -n "contextValue" src/chapterTree.ts confirms chapter/chapter-missing targeting.
  </acceptance_criteria>
  <behavior>
    - Existing files open from tree.
    - Missing rows remain non-openable placeholders.
  </behavior>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Tree items expose native editor open behavior and menu context metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add unit tests for ordering, grouping, and row command mapping</name>
  <read_first>
    - test/chapterOrder.test.ts
    - src/chapterTree.ts
  </read_first>
  <files>test/chapterTree.test.ts</files>
  <action>
    Add tests validating ordered rows, Not Included grouping, missing-file placeholders, and chapter item command metadata.
    Use deterministic fixtures for chapter-order and manuscript file sets so assertions are stable across OS path separators.
    Import and exercise the pure row-construction helper from src/chapterTree.ts so tests avoid runtime coupling to vscode module resolution.
  </action>
  <acceptance_criteria>
    - test/chapterTree.test.ts includes assertions for Not Included and missing-file behavior.
    - grep -n "buildChapterRows" test/chapterTree.test.ts confirms tests execute the pure helper path.
    - npm test passes with chapterTree tests enabled.
  </acceptance_criteria>
  <behavior>
    - Generated tree model preserves deterministic output order.
    - Command metadata is present only for openable chapter rows.
  </behavior>
  <verify>
    <automated>npm run build && npm run build:test && npm test</automated>
  </verify>
  <done>Tree provider behavior is regression-tested and ready for extension registration.</done>
</task>

</tasks>

<verification>
- npm run build
- npm run build:test
- npm test
</verification>

<success_criteria>
- Tree provider supports ordered chapters, missing placeholders, and Not Included grouping.
- Chapter rows open editor previews via native command wiring.
- Unit tests cover major row-construction pathways.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-chapter-workflow/02-02-SUMMARY.md`
</output>
