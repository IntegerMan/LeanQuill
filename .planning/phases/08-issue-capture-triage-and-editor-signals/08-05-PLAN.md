---
phase: 08-issue-capture-triage-and-editor-signals
plan: 05
type: execute
wave: 4
depends_on:
  - "02"
  - "03"
  - "04"
files_modified:
  - src/extension.ts
  - src/issueGutterController.ts
  - package.json
  - media/issue-gutter.svg
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-04
  - PLAN-03
user_setup: []
must_haves:
  truths:
    - "On workspace activation, `migrateIssuesLayoutV3IfNeeded` runs before first `listOpenQuestions` (D-03)."
    - "File watcher covers `.leanquill/issues/**/*.md` and refreshes planning/panel + gutter index (D-04 path reservation for sessions/)."
    - "Manuscript editors show gutter decoration for active issues with resolvable `span_hint` in active chapter document (ISSUE-04, D-11)."
    - "Stacked anchors: command `LeanQuill: Issues at cursor` opens QuickPick when multiple matches; single match opens issue file directly (D-12) — document spike result if glyph click unsupported."
    - "Decorations refresh on active editor change + debounced `onDidChangeTextDocument` using `resolveSpanHintInDocument` (D-13)."
  artifacts:
    - path: "src/issueGutterController.ts"
      provides: "DecorationTypes, index build, QuickPick navigation"
    - path: "src/extension.ts"
      provides: "Registration + migrate + watcher + tree refresh fan-out"
  key_links:
    - from: "src/issueGutterController.ts"
      to: "src/openQuestionStore.ts"
      via: "listOpenQuestions filtered by chapter_ref + selection span_hint"
      pattern: "listOpenQuestions"
    - from: "src/issueGutterController.ts"
      to: "src/spanHintResolve.ts"
      via: "range computation per document line"
      pattern: "resolveSpanHintInDocument"
---

<objective>
Wire extension lifecycle: v3 migration gate, issues file watching, sidebar tree refresh fan-out, and manuscript **gutter** decorations with cursor command for D-12 stacking behavior. Satisfy ISSUE-04 + remaining navigation glue for ISSUE-01; PLAN-03 unchanged in gutter but issues list refresh includes research-linked rows.

Purpose: End-to-end Phase 8 editor signals and integration.
Output: `issueGutterController.ts`, `extension.ts` updates, `package.json` command contributions, optional theme-friendly gutter SVG under `media/`.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-issue-capture-triage-and-editor-signals/08-CONTEXT.md
@.planning/phases/08-issue-capture-triage-and-editor-signals/08-RESEARCH.md
@.planning/phases/08-issue-capture-triage-and-editor-signals/08-UI-SPEC.md
@src/extension.ts
@src/openQuestionStore.ts
@src/spanHintResolve.ts
@src/issueMigration.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: extension.ts — migration gate, watcher, refresh fan-out</name>
  <files>src/extension.ts</files>
  <read_first>
    - src/extension.ts
    - src/issueMigration.ts
    - src/openQuestionStore.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "migrateIssuesLayoutV3IfNeeded" src/extension.ts` shows await before panel/planning first load path.
    - `grep -n "\.leanquill/issues" src/extension.ts` shows `RelativePattern` glob `**/.leanquill/issues/**/*.md` (or equivalent) replacing only `open-questions` watcher.
    - `grep -n "characterTreeProvider.refresh\|placeTreeProvider.refresh\|researchTreeProvider.refresh" src/extension.ts` appears inside `refreshOpenQuestionSurfaces` or shared callback fired on issue watcher events.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <action>
    After `rootPath` / `safeFileSystem` ready, `await migrateIssuesLayoutV3IfNeeded(rootPath, safeFileSystem)` once per session. Replace `openQuestionsWatcher` glob with `.leanquill/issues/**/*.md`. On create/change/delete, call existing `refreshOpenQuestionSurfaces()` **and** refresh `characterTreeProvider`, `placeTreeProvider`, `researchTreeProvider`, `outlineWebviewPanel` / chapter tree provider as needed so D-09 counts stay live. Do **not** register AI session paths (D-04). Update any remaining `OPEN_QUESTIONS_DIR` imports used only for watching to issues root from 08-02 exports.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; npm test</automated>
  </verify>
  <done>Migration + watcher + tree refresh integrated; full test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: issueGutterController — decorations, debounce, D-12 command, spike notes</name>
  <files>src/issueGutterController.ts,media/issue-gutter.svg</files>
  <read_first>
    - .planning/phases/08-issue-capture-triage-and-editor-signals/08-RESEARCH.md
    - src/spanHintResolve.ts
    - src/openQuestionStore.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "createTextEditorDecorationType" src/issueGutterController.ts` returns at least one line.
    - `grep -n "setDecorations" src/issueGutterController.ts` returns at least one line.
    - `grep -n "resolveSpanHintInDocument" src/issueGutterController.ts` shows re-resolution on document text.
    - `grep -n "showQuickPick" src/issueGutterController.ts` implements multi-match selection (D-12).
    - `grep -n "ISSUE-04 D-12 spike" src/issueGutterController.ts` file-top comment block documents whether gutter glyph click is supported on `engines.vscode ^1.90` or if command/hover link is primary UX (per 08-RESEARCH Pitfall 3).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <action>
    Add `IssueGutterController` class: construct 2–3 `TextEditorDecorationType`s (default active, stale/muted, optional resolved — hide resolved from gutter per D-06 active focus). **Index:** `listOpenQuestions`, filter records where `association.kind` is `chapter` or `selection`, `chapter_ref` matches active editor relative path, `isActiveForSidebarCount(status)`, and `span_hint` non-empty. Map to `vscode.Range` using `resolveSpanHintInDocument(document.getText(), hint, preferredIndex)`. **Events:** `onDidChangeActiveTextEditor`, `onDidChangeTextDocument` debounced ~150ms. **Tooltip:** `MarkdownString` with issue title; multiple issues same line → show count per 08-UI-SPEC. **D-12:** register command `leanquill.issuesAtCursor` (**title:** `LeanQuill: Issues at cursor`) resolving issues whose resolved range contains selection.active line; if 0, show info message; if 1, `openTextDocument` issue file URI; if &gt;1, `showQuickPick` by title. Optionally add `MarkdownString.appendMarkdown` command link `[Choose issue](command:leanquill.issuesAtCursor)` in hover. Add minimal `media/issue-gutter.svg` (copy or simplify from `@vscode/codicons` `issues.svg` / `warning.svg`) and reference via `context.extensionUri`; set `light`/`dark` if needed for D-11.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Controller file + SVG exist; acceptance greps pass.</done>
</task>

<task type="auto">
  <name>Task 3: package.json + extension registration + PLAN-03 verify hook</name>
  <files>package.json,src/extension.ts</files>
  <read_first>
    - package.json
    - src/issueGutterController.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "leanquill.issuesAtCursor" package.json` shows command contribution with title `LeanQuill: Issues at cursor`.
    - `grep -n "issueGutter\|IssueGutterController" src/extension.ts` shows instantiate + `activate` subscription + `deactivate` dispose.
    - `grep -n "editor/context" package.json` includes menu entry for `leanquill.issuesAtCursor` when `editorTextFocus &amp;&amp; leanquill.workspaceLeanQuillReady`.
    - `grep -n "lq_research_file\|research" test/openQuestionStore.test.ts` still passes — run full `npm test` for PLAN-03 regression.
    - `npm test` exits 0.
  </acceptance_criteria>
  <action>
    Register command + keybinding optional (omit if no sensible default). Wire `IssueGutterController` in `activate()` after migration with `rootPath`, `extensionUri`, `vscode` API. Ensure `deactivate` disposes decoration types. Align `leanquill.newOpenQuestion` command titles in `package.json` with D-14 naming if not already (`LeanQuill: New issue` family) — grep `newOpenQuestion` entries and update **title** strings only where user-visible. Run full test suite.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; npm test</automated>
  </verify>
  <done>Commands registered; tests green; manual UAT note for gutter visibility in 08-VALIDATION.md style.</done>
</task>

</tasks>

<verification>
Manual (ISSUE-04): open manuscript chapter with selection-anchored issue; see gutter icon; run **Issues at cursor** on stacked lines; switch light/dark theme. Record outcome in plan SUMMARY for D-12 spike.
</verification>

<success_criteria>
ISSUE-04 satisfied in extension host with documented D-12 interaction path; workspace migration and watchers align with D-01–D-04; PLAN-03 data still listed in triage after integration.
</success_criteria>

<output>
After completion, create `.planning/phases/08-issue-capture-triage-and-editor-signals/08-05-SUMMARY.md`
</output>
