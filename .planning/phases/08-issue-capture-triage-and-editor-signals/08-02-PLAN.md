---
phase: 08-issue-capture-triage-and-editor-signals
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - Imported/data-contracts/issue-schema.md
  - src/types.ts
  - src/openQuestionStore.ts
  - src/issueMigration.ts
  - src/spanHintResolve.ts
  - src/openQuestionWorkspaceSync.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
  - PLAN-03
user_setup: []
must_haves:
  truths:
    - "Issues persist under `.leanquill/issues/<type-slug>/*.md` with YAML `type` matching folder (D-01)."
    - "One-time v3 migration moves `.leanquill/open-questions/` → `.leanquill/issues/question/` and writes idempotency marker (D-02, D-03)."
    - "`dismissed` status and optional `dismissed_reason` round-trip in frontmatter (D-05, D-07)."
    - "`listOpenQuestions` (or renamed export) recursively loads all author issue markdown across type folders; `lq_research_file` association preserved for PLAN-03 (D-15)."
    - "`resolveSpanHintInDocument` implements bounded best-effort matching for gutter/list consumers (D-13)."
  artifacts:
    - path: ".leanquill/issues/.migration-v3-complete.json"
      provides: "Idempotency marker (exact filename chosen in implementation; document in SUMMARY)"
      contains: "migration"
    - path: "src/issueMigration.ts"
      provides: "Real migrateIssuesLayoutV3IfNeeded"
    - path: "src/openQuestionStore.ts"
      provides: "Multi-folder CRUD + parse/serialize extended statuses/types"
  key_links:
    - from: "src/extension.ts"
      to: "src/issueMigration.ts"
      via: "call before first list (wired in 08-05)"
      pattern: "migrateIssuesLayoutV3IfNeeded"
    - from: "src/openQuestionStore.ts"
      to: "src/spanHintResolve.ts"
      via: "optional helper import for stale checks"
      pattern: "spanHintResolve"
---

<objective>
Replace Phase 14 single-folder open-question storage with per-type `.leanquill/issues/{type}/` layout, schema v3 migration, full ISSUE-01/02 lifecycle fields, and real `span_hint` resolution helper — per D-01–D-03, D-05, D-07, D-13, D-15/D-16.

Purpose: Single source of truth for webviews, sidebar counts, and gutter index in later plans.
Output: Migrated on-disk layout contract, updated `types.ts`, implemented `issueMigration.ts` + `spanHintResolve.ts`, refactored `openQuestionStore.ts` + workspace sync.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-issue-capture-triage-and-editor-signals/08-CONTEXT.md
@Imported/data-contracts/issue-schema.md
@src/openQuestionStore.ts
@src/types.ts
@src/safeFileSystem.ts
@test/issueMigration.test.ts
@test/openQuestionStore.test.ts
@test/openQuestionChapterCounts.test.ts
@test/spanHintResolve.test.ts
</context>

<interfaces>
From `src/types.ts` (extend — executor must update definitions, not duplicate ad hoc):

- `OpenQuestionRecord` / `OpenQuestionStatus` → add `dismissed` and ensure `resolved` remains; add optional `dismissedReason?: string` on record if not only in raw frontmatter.
- `OpenQuestionAssociation` — preserve `kind: "research"` with `fileName` for `lq_research_file` (PLAN-03, D-15).

Type slug rule: normalize YAML `type` to **lowercase kebab-case** for directory names (D-01); map legacy `author-note` → `question` on migration (D-02).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Contract + types + displayIssueTypeLabel (question type, D-16)</name>
  <files>Imported/data-contracts/issue-schema.md,src/types.ts,src/openQuestionStore.ts</files>
  <read_first>
    - Imported/data-contracts/issue-schema.md
    - .planning/phases/08-issue-capture-triage-and-editor-signals/08-CONTEXT.md
    - src/types.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "question" Imported/data-contracts/issue-schema.md` includes a **Types** table row or bullet describing unified author `question` type (D-02).
    - `grep -nE "dismissed" src/types.ts` shows status union or field including dismissed.
    - `grep -n 'type === "question"' src/openQuestionStore.ts` OR `grep -n "question" src/openQuestionStore.ts | head -5` shows `displayIssueTypeLabel` handling for slug `question` with human label **Question** (D-16).
    - `grep -n "research-question" src/types.ts src/openQuestionStore.ts` returns **no** matches (forbidden distinct type per D-15).
  </acceptance_criteria>
  <action>
    Update **Issue Types** table in `Imported/data-contracts/issue-schema.md`: add `question` — author-captured question migrated from Phase 14; clarify `author-note` as legacy alias mapped to `question` on migration. In `src/types.ts`, extend `OpenQuestionStatus` to `"open" | "deferred" | "dismissed" | "resolved"`; add optional `dismissedReason` on record if serializer uses it. In `openQuestionStore.ts`, update `VALID_STATUSES` / parsers to accept `dismissed`; ensure `serializeOpenQuestionFile` emits `dismissed_reason` when present (issue-schema field name). Extend `displayIssueTypeLabel` so `question` and legacy `author-note` both label as **Question** until all files normalized. **Do not** add `research-question` type anywhere (D-15).
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Contract table updated; types compile; grep acceptance passes.</done>
</task>

<task type="auto">
  <name>Task 2: issueMigration.ts — v3 layout migration (D-01–D-03)</name>
  <files>src/issueMigration.ts</files>
  <read_first>
    - src/issueMigration.ts
    - src/safeFileSystem.ts
    - test/issueMigration.test.ts
    - .planning/phases/08-issue-capture-triage-and-editor-signals/08-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "issues/question" src/issueMigration.ts` shows target path construction.
    - `grep -nE "open-questions|OPEN_QUESTIONS" src/issueMigration.ts` shows legacy path handling.
    - `grep -nE "\\.migration|migration-v3|v3" src/issueMigration.ts` shows idempotency marker write (pick one filename under `.leanquill/issues/` and use consistently in tests).
    - `npm run build:test &amp;&amp; node --test dist-test/issueMigration.test.js` exits 0.
  </acceptance_criteria>
  <action>
    Implement `migrateIssuesLayoutV3IfNeeded(rootPath, safeFs)`: if marker file absent and (legacy `.leanquill/open-questions` exists OR issues dir needs bootstrap), create `.leanquill/issues/question/`, move each `*.md` from open-questions, rewrite frontmatter `type: author-note` → `type: question` where applicable, then write marker JSON `{ "completedAt": ISO string, "version": 3 }`. If `.leanquill/issues/**` already contains targets, resolve conflicts per Claude discretion: prefer not overwriting author files — rename with numeric suffix and log via `console.warn`. Remove empty `open-questions` dir after successful move. Second call: no file moves, `{ ran: false }` if marker present. Update `test/issueMigration.test.ts` to assert real moves using temp dirs + real `SafeFileSystem` or test double consistent with repo patterns.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/issueMigration.test.js</automated>
  </verify>
  <done>Migration tests green; marker prevents re-run.</done>
</task>

<task type="auto">
  <name>Task 3: openQuestionStore + spanHintResolve + counts + workspace sync</name>
  <files>src/openQuestionStore.ts,src/spanHintResolve.ts,src/openQuestionWorkspaceSync.ts</files>
  <read_first>
    - src/openQuestionStore.ts
    - src/issueFilters.ts
    - test/openQuestionStore.test.ts
    - test/openQuestionChapterCounts.test.ts
    - test/spanHintResolve.test.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "\.leanquill/issues" src/openQuestionStore.ts` shows new base path constant replacing sole `open-questions` usage for listing (export name may stay `OPEN_QUESTIONS_DIR` only if grep proves issues path — prefer new `ISSUES_ROOT` or `LEANQUILL_ISSUES_DIR` grep hit).
    - `grep -n "readdir" src/openQuestionStore.ts` shows recursive or per-type-folder enumeration for all `*.md` under issues (exclude marker files).
    - `grep -n "isActiveForSidebarCount\|countOpenQuestionsByChapter" src/openQuestionStore.ts` shows chapter count uses **open + deferred** via `isActiveForSidebarCount` from `issueFilters.ts` (import) or duplicated logic removed in favor of single helper.
    - `grep -n "STUB: Phase 08-02 implements" src/spanHintResolve.ts` returns **no** lines (implementation complete).
    - `npm run build:test &amp;&amp; node --test dist-test/openQuestionStore.test.js dist-test/openQuestionChapterCounts.test.js dist-test/spanHintResolve.test.js` exits 0.
  </acceptance_criteria>
  <action>
    Replace flat dir constant with `.leanquill/issues` root. Implement `listOpenQuestions`: enumerate each subdirectory (type slug) or walk tree skipping hidden/marker files and **never** importing `.leanquill/issues/sessions/**` if present (D-04 out of scope but path reserved). Parse `type` from frontmatter; default file folder slug must match. Update `createOpenQuestion` / add `createIssue` path: **D-14** type selection happens in UI plan — store API must accept `issueType: string` (kebab-case) and write file under `issues/{slug}/` with matching `type:` field. Implement `resolveSpanHintInDocument` in `spanHintResolve.ts`: strip `...` ellipses from hints; find first indexOf fragment; if missing and `preferredStartIndex` provided, search within window ±W (choose W in code constant e.g. 1200) for best partial token match; return `{ kind: "matched", start, end } | { kind: "stale" } | { kind: "ambiguous", candidates }` — shape must satisfy tests enabled from 08-01 (un-skip tests in this task). Update `openQuestionWorkspaceSync.ts` path globs to scan `issues/**`. If chapter index merge needs adjustment, limit edits to `openQuestionStore.ts` when possible. Remove `throw` stubs.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/openQuestionStore.test.js dist-test/openQuestionChapterCounts.test.js dist-test/spanHintResolve.test.js</automated>
  </verify>
  <done>Store lists typed folders; counts include deferred; span resolver tests green; workspace sync paths updated.</done>
</task>

</tasks>

<verification>
`npm test` full suite green after this plan; spot-check `grep -R "open-questions" src/` only matches migration, comments, or tests — not active list path.
</verification>

<success_criteria>
ISSUE-01/02 data layer satisfied for full schema paths; PLAN-03 data via `research` association intact; D-01–D-03 migration proven by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-issue-capture-triage-and-editor-signals/08-02-SUMMARY.md`
</output>
