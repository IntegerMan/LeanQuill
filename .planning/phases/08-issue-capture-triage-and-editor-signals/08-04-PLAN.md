---
phase: 08-issue-capture-triage-and-editor-signals
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/chapterTree.ts
  - src/outlineWebviewPanel.ts
  - src/characterTree.ts
  - src/placeTree.ts
  - src/researchTree.ts
  - src/planningPanelHtml.ts
  - src/planningPanel.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-03
user_setup: []
must_haves:
  truths:
    - "Chapter tree description uses **X Issues** suffix (capital I) for active count = open + deferred (D-09, D-06 alignment)."
    - "Outline webview chapter rows use the same **X Issues** label (replace `N issue(s)` phrasing)."
    - "Characters, Places, Research sidebar tree rows show ` · N Issues` (or equivalent) when N &gt; 0 via `listOpenQuestions` + entity/research association helpers."
  artifacts:
    - path: "src/chapterTree.ts"
      provides: "issueCountText → Issues copy"
    - path: "src/characterTree.ts"
      provides: "TreeItem.description issue count"
  key_links:
    - from: "src/characterTree.ts"
      to: "src/openQuestionStore.ts"
      via: "listOpenQuestions + countOpenQuestionsLinkedToEntity(..., \"character\", fileName)"
      pattern: "countOpenQuestionsLinkedToEntity"
---

<objective>
Surface **X Issues** active counts (open + deferred) on native sidebar trees and outline webview per D-09 and 08-UI-SPEC copy — counts only, no drill-down (999.1 deferred).

Purpose: Align CHAP-01-style visibility with triage defaults (D-06).
Output: Updated `TreeItem.description` / label helpers and outline HTML suffix.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-issue-capture-triage-and-editor-signals/08-UI-SPEC.md
@src/chapterTree.ts
@src/outlineWebviewPanel.ts
@src/characterTree.ts
@src/placeTree.ts
@src/researchTree.ts
@src/openQuestionStore.ts
@src/issueFilters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared active issue count helper + chapterTree copy</name>
  <files>src/openQuestionStore.ts,src/chapterTree.ts</files>
  <read_first>
    - src/chapterTree.ts
    - src/openQuestionStore.ts
    - src/issueFilters.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "Issues" src/chapterTree.ts` shows suffix string **Issues** (capital I) for count display (e.g. `3 Issues`).
    - `grep -n "isActiveForSidebarCount" src/openQuestionStore.ts` shows helper usage inside `countOpenQuestionsByChapter` or a new `countActiveIssuesByChapter` export used by chapter tree merge.
    - `grep -n "deferred" test/openQuestionChapterCounts.test.ts` remains aligned (from 08-01/02).
    - `npm run build:test &amp;&amp; node --test dist-test/openQuestionChapterCounts.test.js` exits 0.
  </acceptance_criteria>
  <action>
    Refactor counting so chapter-linked issues increment when `isActiveForSidebarCount(status)` is true (open + deferred). Update `issueCountText` in `chapterTree.ts` to return `${n} Issues` per D-09. Ensure `mergeOpenQuestionCountsIntoChapterStatusIndex` uses the same count function. If naming collision, export `countActiveIssuesByChapter` as alias and call from `outlineWebviewPanel.ts` + chapter status merge.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/openQuestionChapterCounts.test.js</automated>
  </verify>
  <done>Chapter tree shows **X Issues**; chapter count tests green.</done>
</task>

<task type="auto">
  <name>Task 2: outlineWebviewPanel.ts — Issues suffix on rows</name>
  <files>src/outlineWebviewPanel.ts</files>
  <read_first>
    - src/outlineWebviewPanel.ts
    - .planning/phases/08-issue-capture-triage-and-editor-signals/08-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "Issues" src/outlineWebviewPanel.ts` replaces old `issue(s)` lowercase suffix for chapter rows.
    - `grep -n "countOpenQuestionsByChapter\|countActiveIssuesByChapter" src/outlineWebviewPanel.ts` shows import of updated counter.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <action>
    Update `openQuestionCountSuffix` (or equivalent) to emit ` · N Issues` using the same active-count map as chapter tree (open+deferred). Keep `escapeHtml` usage.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Outline webview copy matches D-09.</done>
</task>

<task type="auto">
  <name>Task 3: characterTree, placeTree, researchTree + Threads tab row suffix (D-09)</name>
  <files>src/characterTree.ts,src/placeTree.ts,src/researchTree.ts,src/planningPanelHtml.ts,src/planningPanel.ts</files>
  <read_first>
    - src/characterTree.ts
    - src/placeTree.ts
    - src/researchTree.ts
    - src/openQuestionStore.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "listOpenQuestions" src/characterTree.ts src/placeTree.ts src/researchTree.ts` shows async load for counts (may be in `getTreeItem` via cached map or `getChildren` prefetch — document pattern in SUMMARY).
    - `grep -n "Issues" src/characterTree.ts` shows description suffix.
    - `grep -n "countOpenQuestionsLinkedToEntity" src/characterTree.ts src/placeTree.ts` OR grep shows new helper name **`countActiveQuestionsLinkedToEntity`** if you rename to use `isActiveForSidebarCount` filtering inside store.
    - `grep -n "research" src/researchTree.ts` includes issue count using association kind `"research"` + `lq_research_file` basename match.
    - `grep -n "Issues" src/planningPanelHtml.ts` shows Threads tab (or shared row renderer) appends active issue count for thread file rows when data provided — if row HTML is built server-side only without counts, add `threadOpenIssueCounts` map parameter parallel to `openQuestionCountSuffix` pattern from outline (document in SUMMARY).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <action>
    For each provider, load issues list once per refresh cycle (constructor injection of a lightweight `() => Promise&lt;OpenQuestionRecord[]&gt;` from `extension.ts` is allowed in plan 08-05 — **this plan** may add `setIssueListProvider` method on each tree class to avoid circular imports). Minimal approach: import `listOpenQuestions` + add optional `rootPath` callback already on constructors; in `getChildren`, `await listOpenQuestions(rootPath)` and compute per-entity active count via existing `countOpenQuestionsLinkedToEntity` updated to count **active** statuses only, or add parallel `countActiveQuestionsLinkedToEntity` in `openQuestionStore.ts`.     Set `TreeItem.description` to `N Issues` when N&gt;0, omit when zero. Ensure performance: cache list per refresh tick same as chapter tree patterns if any exist. **Planning Threads tab:** extend `planningPanelHtml.ts` thread list markup to include ` · N Issues` when `planningPanel.ts` passes per-`fileName` active counts (add fields to template context); wire count computation in `planningPanel.ts` refresh using `countOpenQuestionsLinkedToEntity` / active variant — may duplicate outline pattern (`listOpenQuestions` once per refresh).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test</automated>
  </verify>
  <done>Three trees show D-09 counts; project compiles.</done>
</task>

</tasks>

<verification>
Manual: expand Characters/Places/Research — rows with linked active issues show `2 Issues` style description; outline chapter rows match.
</verification>

<success_criteria>
Sidebar and outline surfaces reflect the same active issue totals as default triage filter.
</success_criteria>

<output>
After completion, create `.planning/phases/08-issue-capture-triage-and-editor-signals/08-04-SUMMARY.md`
</output>
