---
phase: 08-issue-capture-triage-and-editor-signals
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/openQuestionsHtml.ts
  - src/openQuestionsPanel.ts
  - src/planningPanelHtml.ts
  - src/planningPanel.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
  - ISSUE-03
  - PLAN-03
user_setup: []
must_haves:
  truths:
    - "Planning workspace tab and bottom panel share one HTML builder + extended `openQuestion:*` protocol (Phase 14 D-02 parity)."
    - "Master–detail layout: list + detail with status, type label (`displayIssueTypeLabel`), associations including research chip when `lq_research_file` set (PLAN-03, D-15/D-16)."
    - "Filter control implements D-06: default **Active** = open+deferred; **All** shows resolved; **Dismissed** shows dismissed only (ISSUE-03)."
    - "Dismiss action persists `dismissed` + optional `dismissed_reason` (D-07)."
    - "New-issue UX follows D-14: title/name first, then type pick — wire to store API accepting `issueType` + title (from 08-02)."
  artifacts:
    - path: "src/openQuestionsHtml.ts"
      provides: "Toolbar filter + dismiss + detail panel markup"
    - path: "src/planningPanel.ts"
      provides: "Message handlers: filter, dismiss, save detail, new-issue flow"
  key_links:
    - from: "src/planningPanel.ts"
      to: "src/openQuestionStore.ts"
      via: "listOpenQuestions + updateOpenQuestionStatus / save helpers"
      pattern: "listOpenQuestions"
    - from: "src/openQuestionsPanel.ts"
      to: "src/planningPanel.ts"
      via: "mirror handler cases"
      pattern: "openQuestion:"
---

<objective>
Upgrade dual-host issues webviews to Phase 8 master–detail triage: status lifecycle including dismiss, filter semantics (D-06), research-associated rows surfaced like any other issue (PLAN-03), and D-14 create flow.

Purpose: Fulfill ISSUE-02/ISSUE-03 and PLAN-03 in UI before extension/gutter wiring.
Output: Updated HTML/CSS/JS strings and TypeScript message routing in planning + panel providers.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-issue-capture-triage-and-editor-signals/08-UI-SPEC.md
@.planning/phases/08-issue-capture-triage-and-editor-signals/08-CONTEXT.md
@src/openQuestionsHtml.ts
@src/openQuestionsPanel.ts
@src/planningPanel.ts
@src/planningPanelHtml.ts
@src/issueFilters.ts
</context>

<interfaces>
From `src/openQuestionsHtml.ts` (extend — do not break existing message types without dual handling):

- `SerializableOpenQuestionRow` — add fields needed for detail + filter: `status`, `issueType` (YAML type slug), `dismissedReason?`, `researchFileName?` (optional denormalized chip), `filePath` or `relativePath` for editor open.
- Post messages (prefix `openQuestion:`): add e.g. `openQuestion:setFilter`, `openQuestion:dismiss`, `openQuestion:saveDetail`, `openQuestion:newIssueSubmit` (payload shapes defined in handler).

Use `matchesIssueFilter` from `src/issueFilters.ts` in **host** TypeScript when building row lists (single source of truth).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: openQuestionsHtml — master–detail, toolbar, copy (08-UI-SPEC)</name>
  <files>src/openQuestionsHtml.ts,src/planningPanelHtml.ts</files>
  <read_first>
    - .planning/phases/08-issue-capture-triage-and-editor-signals/08-UI-SPEC.md
    - src/openQuestionsHtml.ts
    - src/planningPanelHtml.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "Active issues" src/openQuestionsHtml.ts` OR `grep -n "Active issues" src/planningPanelHtml.ts` matches filter label per 08-UI-SPEC Copywriting.
    - `grep -n "No issues match this filter" src/openQuestionsHtml.ts` returns at least one line.
    - `grep -nE "master|detail|oq-detail|issue-detail" src/openQuestionsHtml.ts` shows master–detail structure (class or id names).
    - `grep -n "post('openQuestion:setFilter" src/openQuestionsHtml.ts` OR `grep -n 'openQuestion:setFilter' src/openQuestionsHtml.ts` shows filter postMessage.
    - `grep -n "Dismiss" src/openQuestionsHtml.ts` shows dismiss control wiring.
  </acceptance_criteria>
  <action>
    Refactor `renderOpenQuestionsHtml` output to **master–detail**: left list filtered client-side or receive pre-filtered rows from host each refresh — **host must apply `matchesIssueFilter`** on refresh for consistency. Add toolbar: **New issue**, filter dropdown (`active` default, `open`, `deferred`, `dismissed`, `all` per ISSUE-03 + D-06), **Refresh**. Detail pane: title, body editor or static + save, status select (`open|deferred|dismissed|resolved`), optional `dismissed_reason` textarea shown when status is `dismissed` or on dismiss flow, association chips (character/place/thread/chapter/selection/research file). Use 08-UI-SPEC spacing tokens. Rename visible tab label in `planningPanelHtml.ts` from **Open questions** to **Issues** (or **Author issues**) — pick one string and use in `TAB_LABELS` / `TAB_IDS` comment; keep `openQuestions` tab id if changing ids is high churn, else rename with coordinated `planningPanel.ts` (prefer minimal: label only). Ensure research association shows file basename chip without implying a special research type (D-15).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>HTML builder emits master–detail + filter + dismiss affordances; `npm run build` passes.</done>
</task>

<task type="auto">
  <name>Task 2: planningPanel.ts — handlers, D-14 new issue, dismiss, filter</name>
  <files>src/planningPanel.ts</files>
  <read_first>
    - src/planningPanel.ts
    - src/openQuestionStore.ts
    - src/issueFilters.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "matchesIssueFilter" src/planningPanel.ts` shows server-side filtering before serializing rows.
    - `grep -n "openQuestion:setFilter" src/planningPanel.ts` handles message and updates persisted filter state field.
    - `grep -nE "dismissed_reason|dismissed" src/planningPanel.ts` shows dismiss/save paths.
    - `grep -nE "showQuickPick|createOpenQuestion|createIssue" src/planningPanel.ts` shows D-14 flow: prompt title then pick type (issue types from shared constant array mirroring `issue-schema.md` types + `question`).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <action>
    Extend `PlanningPanel` private state: `issueListFilter` default `"active"`. On each `refreshOpenQuestions` / render payload, filter `OpenQuestionRecord[]` with `matchesIssueFilter(q.status, filterKey)` where `filterKey` maps UI option to `issueFilters` union (`active` | `open` | `deferred` | `dismissed` | `all`). Implement `openQuestion:dismiss` with modal or inline rationale then call store update setting `status: dismissed` + `dismissed_reason`. Implement detail save: status transitions, body/title writes via existing SafeFileSystem paths. **D-14:** register flow from **New issue** button: `showInputBox` title → `showQuickPick` of allowed types (`question`, `continuity`, `copy-edit`, … from single exported `AUTHOR_ISSUE_TYPES` constant in `openQuestionStore.ts` or new `issueTypes.ts`); then call `createOpenQuestion`/`createIssue` with default association `book` unless host already has context from existing commands. Reuse `vscode` API patterns from Phase 14 character/place/thread issue creation for **contextual** creates (optional second phase in same task): those commands pre-fill association after title+type.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/issueFilters.test.js</automated>
  </verify>
  <done>Planning host filters and mutates issues; new-issue flow matches D-14; build + filter tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: openQuestionsPanel.ts — protocol parity with planning</name>
  <files>src/openQuestionsPanel.ts</files>
  <read_first>
    - src/openQuestionsPanel.ts
    - src/planningPanel.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "openQuestion:setFilter" src/openQuestionsPanel.ts` shows case branch mirroring planningPanel.
    - `grep -n "matchesIssueFilter" src/openQuestionsPanel.ts` returns at least one line.
    - `grep -n "OPEN_QUESTIONS_DIR\|issues" src/openQuestionsPanel.ts` shows paths use store APIs or updated constant from 08-02 (no hardcoded `open-questions` for file open).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <action>
    Duplicate handler coverage for all new `openQuestion:*` messages so **panel** host behaves identically to planning host (D-06 dual parity). Update file open path resolution to use record path from `listOpenQuestions` results (absolute join) rather than assuming flat dir. Call same filter helper when refreshing webview HTML.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Bottom panel parity; grep acceptance satisfied.</done>
</task>

</tasks>

<verification>
Manual smoke: open Planning **Issues** tab + panel view — filter toggles, dismiss with reason, new issue title→type creates file under `.leanquill/issues/<type>/`.
</verification>

<success_criteria>
ISSUE-02/03 and PLAN-03 visible in both webviews; D-06/D-07/D-14/D-15/D-16 honored in UX code paths.
</success_criteria>

<output>
After completion, create `.planning/phases/08-issue-capture-triage-and-editor-signals/08-03-SUMMARY.md`
</output>
