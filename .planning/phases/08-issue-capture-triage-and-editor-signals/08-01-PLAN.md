---
phase: 08-issue-capture-triage-and-editor-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/issueFilters.ts
  - src/spanHintResolve.ts
  - src/issueMigration.ts
  - test/issueMigration.test.ts
  - test/issueFilters.test.ts
  - test/spanHintResolve.test.ts
  - test/openQuestionChapterCounts.test.ts
  - test/openQuestionStore.test.ts
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
  - ISSUE-03
  - ISSUE-04
  - PLAN-03
user_setup: []
must_haves:
  truths:
    - "Wave 0 test files from 08-VALIDATION.md exist and compile via `npm run build:test`."
    - "`matchesIssueFilter` / `isActiveForSidebarCount` contracts are exercised for D-06 (active = open + deferred)."
    - "`spanHintResolve` pure-function tests encode fragment match, stale detection, and bounded fuzzy re-resolution expectations (D-13)."
    - "Migration tests encode idempotent move from `.leanquill/open-questions/*.md` to `.leanquill/issues/question/*.md` plus marker file (D-01–D-03)."
  artifacts:
    - path: "test/issueMigration.test.ts"
      provides: "v3 migration scenarios"
      min_lines: 40
    - path: "test/issueFilters.test.ts"
      provides: "filter matrix for triage UI"
      min_lines: 35
    - path: "test/spanHintResolve.test.ts"
      provides: "ISSUE-04 range resolution unit tests"
      min_lines: 35
    - path: "src/issueFilters.ts"
      provides: "Shared filter predicates (pure functions; complete in 08-01)"
      contains: "matchesIssueFilter"
    - path: "src/spanHintResolve.ts"
      provides: "Span resolution API (stub until 08-02)"
      contains: "STUB: Phase 08-02 implements"
    - path: "src/issueMigration.ts"
      provides: "Migration entry (stub until 08-02)"
      contains: "STUB: Phase 08-02 implements"
  key_links:
    - from: "test/issueFilters.test.ts"
      to: "src/issueFilters.ts"
      via: "import predicates under test"
      pattern: "issueFilters"
    - from: "test/spanHintResolve.test.ts"
      to: "src/spanHintResolve.ts"
      via: "import resolver"
      pattern: "spanHintResolve"
---

<objective>
Establish Phase 8 Wave 0 automated tests and minimal compile stubs for migration, triage filters, and `span_hint` resolution per `08-VALIDATION.md` and `08-RESEARCH.md` § Wave 0 gaps.

Purpose: Nyquist-compliant RED→GREEN path before large refactors in `openQuestionStore.ts` and UI hosts.
Output: Three new test modules, two extended tests, three stub source modules with grep-visible STUB markers.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-issue-capture-triage-and-editor-signals/08-CONTEXT.md
@.planning/phases/08-issue-capture-triage-and-editor-signals/08-RESEARCH.md
@.planning/phases/08-issue-capture-triage-and-editor-signals/08-VALIDATION.md
@Imported/data-contracts/issue-schema.md
@test/openQuestionStore.test.ts
@test/openQuestionChapterCounts.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub modules for Wave 0 imports</name>
  <files>src/issueFilters.ts,src/spanHintResolve.ts,src/issueMigration.ts</files>
  <read_first>
    - .planning/phases/08-issue-capture-triage-and-editor-signals/08-CONTEXT.md
    - .planning/phases/08-issue-capture-triage-and-editor-signals/08-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "STUB: Phase 08-02 implements" src/spanHintResolve.ts src/issueMigration.ts` matches 2 lines (one per file).
    - `grep -nE "export function matchesIssueFilter|export function isActiveForSidebarCount" src/issueFilters.ts` matches both function names (issueFilters is fully implemented in 08-01 — no STUB).
    - `grep -n "export function resolveSpanHintInDocument" src/spanHintResolve.ts` returns at least one line (signature may return a discriminated result: matched | stale | ambiguous).
    - `grep -n "export async function migrateIssuesLayoutV3IfNeeded" src/issueMigration.ts` returns at least one line with parameters `(rootPath: string, safeFs: SafeFileSystem)` or equivalent typed `SafeFileSystem` import from `./safeFileSystem`.
  </acceptance_criteria>
  <action>
    Add three new files. **issueFilters.ts:** fully implement `matchesIssueFilter(status, filter)` where `filter` is `"active" | "open" | "deferred" | "dismissed" | "all"` per 08-RESEARCH.md; implement `isActiveForSidebarCount(status)` true for `open` and `deferred` only (D-06). Export a shared `IssueStatus` string union type aligned with D-05 (`open` | `deferred` | `dismissed` | `resolved`). No STUB marker in this file. **spanHintResolve.ts:** export `resolveSpanHintInDocument(fullText: string, spanHint: string, preferredStartIndex?: number)` — body `throw new Error("STUB: Phase 08-02 implements")` until 08-02. Top line comment exactly `// STUB: Phase 08-02 implements`. **issueMigration.ts:** export `migrateIssuesLayoutV3IfNeeded(rootPath, safeFs)` returning `Promise&lt;{ ran: boolean }&gt;`; stub returns `{ ran: false }` without filesystem writes. Top line comment exactly `// STUB: Phase 08-02 implements`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All three files exist; acceptance greps pass; `npx tsc --noEmit` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: issueFilters.test.ts + spanHintResolve.test.ts</name>
  <files>test/issueFilters.test.ts,test/spanHintResolve.test.ts</files>
  <read_first>
    - src/issueFilters.ts
    - .planning/phases/08-issue-capture-triage-and-editor-signals/08-RESEARCH.md
    - test/threadStore.test.ts
  </read_first>
  <acceptance_criteria>
    - `grep -n "active" test/issueFilters.test.ts` includes cases for open+deferred included, dismissed excluded.
    - `grep -nE "deferred|dismissed|resolved" test/issueFilters.test.ts` covers status boundary cases for ISSUE-03.
    - `grep -n "matchesIssueFilter" test/issueFilters.test.ts` shows import from `../src/issueFilters`.
    - `grep -n "resolveSpanHintInDocument" test/spanHintResolve.test.ts` imports resolver from `../src/spanHintResolve`.
    - `grep -nE "STUB: Phase 08-02 implements|assert\\.throws" test/spanHintResolve.test.ts` shows stub guard test passes in 08-01.
    - `grep -nE "fuzzy|nearby|bounds|D-13" test/spanHintResolve.test.ts` documents planned D-13 cases in comments or `test.skip` titles for 08-02.
  </acceptance_criteria>
  <action>
    Create `test/issueFilters.test.ts` using `node:test`, importing `matchesIssueFilter` and `isActiveForSidebarCount`. Assert: `active` includes `open` and `deferred`; excludes `dismissed`, `resolved`; `all` includes every status; `dismissed` filter only matches dismissed; `open` only open; `deferred` only deferred. Assert `isActiveForSidebarCount("deferred") === true` and `isActiveForSidebarCount("dismissed") === false`. Create `test/spanHintResolve.test.ts` that imports `resolveSpanHintInDocument` and uses `assert.throws(() => resolveSpanHintInDocument("body text", "fragment"), /STUB: Phase 08-02 implements/)`; add commented or `test.skip` blocks listing future cases: exact match, empty hint stale, fuzzy window (max N chars from `preferredStartIndex`) per D-13 for 08-02 to enable.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/issueFilters.test.js dist-test/spanHintResolve.test.js</automated>
  </verify>
  <done>`npm run build:test` succeeds; `node --test dist-test/issueFilters.test.js dist-test/spanHintResolve.test.js` passes; acceptance greps pass.</done>
</task>

<task type="auto">
  <name>Task 3: issueMigration.test.ts + extend store and chapter count tests</name>
  <files>test/issueMigration.test.ts,test/openQuestionChapterCounts.test.ts,test/openQuestionStore.test.ts</files>
  <read_first>
    - src/issueMigration.ts
    - src/openQuestionStore.ts
    - .planning/phases/08-issue-capture-triage-and-editor-signals/08-CONTEXT.md
    - Imported/data-contracts/issue-schema.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "open-questions" test/issueMigration.test.ts` references legacy path `.leanquill/open-questions`.
    - `grep -n "issues/question" test/issueMigration.test.ts` references target folder `.leanquill/issues/question`.
    - `grep -nE "migration|marker|idempotent|\.migration" test/issueMigration.test.ts` includes idempotency expectation (second call no-op).
    - `grep -n "dismissed" test/openQuestionStore.test.ts` adds ISSUE-02 coverage for `dismissed` status and `dismissed_reason` round-trip (may stay failing until 08-02 — compile must pass).
    - `grep -nE "deferred|active|sidebar" test/openQuestionChapterCounts.test.ts` extends counting rules to include `deferred` in active sidebar/issue index counts per D-06 note in 08-RESEARCH.md (update test name/comments to say `open + deferred`).
  </acceptance_criteria>
  <action>
    Add `test/issueMigration.test.ts` using temp directories under `os.tmpdir()`: create fake workspace with `.leanquill/open-questions/foo.md` (minimal frontmatter); call `migrateIssuesLayoutV3IfNeeded`; assert stub phase returns `{ ran: false }` and **document** expected post-08-02 assertions in comments (file moved, marker written). Include conflict case comment: pre-existing `.leanquill/issues/question/same.md`. Extend `test/openQuestionStore.test.ts` with cases for `status: dismissed` and non-empty `dismissed_reason` serialization expectations aligned with `issue-schema.md`. Extend `test/openQuestionChapterCounts.test.ts` so chapter counts treat **`open` + `deferred`** as counting toward the same metric used for sidebar/`openIssueCount` (rename assertions from open-only where needed). Do not break existing imports; if current production code still counts only `open`, tests should assert the **new** intended behavior and fail until 08-02 updates `countOpenQuestionsByChapter` / merge helper — use `test.skip` only if repo policy forbids failing tests; otherwise allow RED.
  </action>
  <verify>
    <automated>npm run build:test</automated>
  </verify>
  <done>Third test file added; extended tests satisfy acceptance greps; `npm run build:test` succeeds.</done>
</task>

</tasks>

<verification>
After all tasks: `npm run build:test`. Full `npm test` may be RED until plan 08-02 implements stubs and store changes; gate this plan on compile-only if CI requires green tests — then temporarily `test.skip` migration/filter suites with `// 08-02 enables` comments (last resort).
</verification>

<success_criteria>
Wave 0 artifacts from `08-VALIDATION.md` exist; stub modules are grep-tagged; filter and span tests encode D-06 and D-13; migration tests encode D-01–D-03 intent.
</success_criteria>

<output>
After completion, create `.planning/phases/08-issue-capture-triage-and-editor-signals/08-01-SUMMARY.md`
</output>
