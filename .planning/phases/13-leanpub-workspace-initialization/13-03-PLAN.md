---
phase: 13-leanpub-workspace-initialization
plan: 03
type: execute
wave: 3
depends_on:
  - "13-01"
  - "13-02"
files_modified:
  - src/planningPanel.ts
  - src/initialize.ts
  - src/extension.ts
autonomous: true
requirements:
  - INIT-01
  - INIT-02

must_haves:
  truths:
    - "Planning Workspace can open with Cards tab active via a first-class API mirroring showCharacter (D-15)."
    - "LeanQuill: Initialize performs manuscript scaffold when needed without falsely prompting to wipe .leanquill when only manuscript is incomplete."
    - "Successful scaffold/init logs per-path create/skip to output channel and shows information toast (D-08, D-14, D-16)."
    - "D-18 blocked path shows error (or modal) with actionable text referencing opening Book.txt / leanquill.openBookTxt."
    - "After success, extension refreshes context and chapter-order/outline pipeline without window reload (D-17)."
  artifacts:
    - path: "src/planningPanel.ts"
      provides: "showCards() or show({ initialTab: 'cards' })"
    - path: "src/initialize.ts"
      provides: "runInitializeFlow branches: full init vs scaffold-only; calls applyLeanpubManuscriptScaffold; refined overwrite gate"
    - path: "src/extension.ts"
      provides: "Passes PlanningPanel into runInitializeFlow or equivalent; openPlanningWorkspace optional tab arg if implemented"
  key_links:
    - from: "src/initialize.ts"
      to: "src/leanpubScaffold.ts"
      via: "applyLeanpubManuscriptScaffold after full or partial flow"
      pattern: "applyLeanpub|leanpubScaffold"
    - from: "src/initialize.ts"
      to: "src/planningPanel.ts"
      via: "showCards on success per D-15"
      pattern: "showCards"
---

<objective>
Wire **manuscript scaffold** into **`runInitializeFlow`**, add **Planning Cards** success UX, and **refresh** workspace state so INIT-01/INIT-02 behavior is coherent end-to-end.

Purpose: Single command (`leanquill.initialize`) remains the entry point (D-01); success UX matches CONTEXT D-08–D-18.

Output: Updated `planningPanel.ts`, `initialize.ts`, and targeted `extension.ts` wiring.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-leanpub-workspace-initialization/13-CONTEXT.md
@.planning/phases/13-leanpub-workspace-initialization/13-RESEARCH.md
@src/initialize.ts
@src/planningPanel.ts
@src/extension.ts
@src/chapterOrder.ts

<interfaces>
From `src/planningPanel.ts` — pattern to mirror:

```typescript
public async showCharacter(fileName: string): Promise<void> {
  this._activeTab = "characters";
  // ...
}
```

Tab id for cards must be **`"cards"`** (see `TAB_IDS` in `planningPanelHtml.ts`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlanningPanelProvider — open on Cards tab (D-15)</name>
  <files>src/planningPanel.ts</files>
  <read_first>
    - `src/planningPanel.ts` (`show`, `showCharacter`, `_activeTab`, `_renderPanel`)
    - `src/planningPanelHtml.ts` (`TAB_IDS` — confirm `"cards"` literal)
  </read_first>
  <action>
    Add **`public async showCards(): Promise<void>`** that sets **`this._activeTab = "cards"`** before **`show()`** / **`reveal`** / **`_renderPanel()`**, mirroring **`showCharacter`** (clear `_selectedCharacterFileName` if needed so Cards view is not confused).

    Optionally extend **`leanquill.openPlanningWorkspace`** in `extension.ts` to accept **`{ tab: "cards" }`** and delegate to `showCards` — only if you need command-level entry; not required if Initialize calls `planningPanel.showCards()` directly.
  </action>
  <acceptance_criteria>
    - `rg -n "showCards" src/planningPanel.ts` matches.
    - `rg -n "_activeTab = \"cards\"" src/planningPanel.ts` matches.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Cards tab can be selected programmatically before first paint.</done>
</task>

<task type="auto">
  <name>Task 2: runInitializeFlow — scaffold branch, logging, D-15 success, D-18 failure</name>
  <files>src/initialize.ts</files>
  <read_first>
    - `src/initialize.ts` (`runInitializeFlow`, `initializeProject`, `ensureOverwriteIfNeeded`, `shouldPromptInitialize`)
    - `src/leanpubScaffold.ts` (`applyLeanpubManuscriptScaffold` and result type)
    - `src/projectConfig.ts` (`readProjectConfig`, `validateProjectYamlForSetup`)
    - `.planning/phases/13-leanpub-workspace-initialization/13-CONTEXT.md` (D-07, D-11, D-14–D-18)
  </read_first>
  <action>
    1. **Change `runInitializeFlow` signature** to accept optional **`planningPanel?: PlanningPanelProvider`** (import type from `./planningPanel` — avoid circular import if needed via interface or lazy require; prefer clean TS).

    2. **Overwrite gate:** Refine **`ensureOverwriteIfNeeded`** so if **`.leanquill/project.yaml` exists** and **`validateProjectYamlForSetup`** passes, **do not** ask to overwrite `.leanquill` **solely** to add manuscript files. Only prompt overwrite when (a) creating a **new** project that would replace existing invalid/incomplete state per product rules, or (b) keep existing modal when user explicitly re-inits — document branch in code comment. Minimal fix: if valid yaml on disk, skip destructive overwrite prompt and proceed to scaffold branch.

    3. **Branch A — full init:** When yaml missing/invalid, keep **`gatherInitInput`** + **`initializeProject`**. After **`initializeProject`**, call **`applyLeanpubManuscriptScaffold(rootPath, { safeFs })`** (construct `SafeFileSystem` in `initialize.ts` like today) so new repos always get Book.txt + ch1 rules even if `initializeProject` alone did not write them.

    4. **Branch B — scaffold-only:** When yaml **valid** and **`workspace` would still need manuscript scaffold** (mirror `manuscriptScaffoldComplete` logic: !dir or !Book.txt), run **`applyLeanpubManuscriptScaffold`** only (no title prompt unless you deem necessary — default **no** new prompts).

    5. **D-08 / D-16:** On every apply result, **`log.info`** each path in `created` / `skipped` arrays; on failure/blocked, **`log.error`** with message.

    6. **D-14 / D-16:** `vscode.window.showInformationMessage` on success; `showErrorMessage` (or modal per discretion) on blocked/failure. Blocked D-18 message must mention user can **`Open Book.txt`** via existing command: `command:leanquill.openBookTxt` in markdown not available from `showErrorMessage` — use string **“Run command LeanQuill: Open Book.txt”** or `vscode.commands.executeCommand("leanquill.openBookTxt")` as a **Show** action if using modal with buttons.

    7. **D-15:** On **successful** scaffold (any path that created or confirmed layout), call **`await planningPanel?.showCards()`** and **remove** **`showTextDocument(project.yaml)`** as the primary success tail — per CONTEXT. If `planningPanel` undefined (edge), fall back to toast only.

    8. **`shouldPromptInitialize`:** Change **`Book.txt`** check from **`path.join(folderPath, "Book.txt")`** to **`path.join(folderPath, "manuscript", "Book.txt")`**; align `hasManuscript` with Phase 13 layout (per RESEARCH).

    9. After flow, callers must **`setWorkspaceContext`** — already in `extension.ts` command wrapper; ensure **`initializeProject` internal path** also triggers refresh when invoked from same command (wrapper already runs after try/catch).

    10. **INIT-02:** After successful scaffold, re-run **`resolveChapterOrder`** and write **`.leanquill/chapter-order.json`** when appropriate (same as `initializeProject` tail) so sidebar/webviews pick up order without reload (D-17).
  </action>
  <acceptance_criteria>
    - `rg -n "path\\.join\\(folderPath, \"manuscript\", \"Book\\.txt\"\\)" src/initialize.ts` matches inside `shouldPromptInitialize`.
    - `rg -n "showCards|showTextDocument\\(result\\.projectYamlPath" src/initialize.ts` shows `showCards` present and **no** unconditional `showTextDocument` after success for the unified success path (grep may still find `showTextDocument` elsewhere — acceptance: success tail uses Cards per D-15).
    - `rg -n "applyLeanpubManuscriptScaffold" src/initialize.ts` matches.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Initialize command performs scaffold when needed; success opens Cards; blocked paths are user-actionable; shouldPromptInitialize uses manuscript/Book.txt.</done>
</task>

<task type="auto">
  <name>Task 3: extension.ts — pass PlanningPanel into runInitializeFlow + optional command arg</name>
  <files>src/extension.ts</files>
  <read_first>
    - `src/extension.ts` (`leanquill.initialize` registration, `PlanningPanelProvider` instantiation)
    - `src/initialize.ts` (updated `runInitializeFlow` signature from Task 2)
  </read_first>
  <action>
    1. Update **`vscode.commands.registerCommand("leanquill.initialize", ...)`** to call **`runInitializeFlow(context, log, { planningPanel })`** passing the **`planningPanel`** instance created in `activate`.

    2. Optionally update **`leanquill.openPlanningWorkspace`** handler to **`async (opts?: { tab?: "cards" })`** — if `opts?.tab === "cards"`, call **`planningPanel.showCards()`**; else **`planningPanel.show()`**. Keep backward compatibility when `opts` undefined.

    3. If **`syncBookTxt` / outline watcher** interaction risks wiping new `Book.txt` (RESEARCH pitfall), add a **short comment** at call site or guard in Plan 13-04 tests — if you add a guard in this task, reference `generateBookTxt` empty case in comment only (minimal code change).
  </action>
  <acceptance_criteria>
    - `rg -n "runInitializeFlow\\(" src/extension.ts` shows third argument object including `planningPanel`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Initialize command receives planning panel; optional planning command tab routing implemented if chosen.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes.
2. Single user entry `leanquill.initialize` covers full + scaffold-only paths (INIT-01).
3. Chapter order file refresh aligns with INIT-02 after scaffold.
</verification>

<success_criteria>
- D-15 success UX: Planning Workspace opens on Cards.
- D-18: no silent Book.txt mutation.
- INIT-01 satisfied via unified Initialize command; INIT-02 via chapter-order refresh + valid Book.txt.
</success_criteria>

<output>
After completion, create `.planning/phases/13-leanpub-workspace-initialization/13-03-SUMMARY.md`
</output>
