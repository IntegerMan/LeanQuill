---
phase: 13-leanpub-workspace-initialization
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/extension.ts
  - package.json
autonomous: true
requirements:
  - INIT-01
  - INIT-02

must_haves:
  truths:
    - "VS Code context keys expose manuscript scaffold completeness and project.yaml validity for declarative viewsWelcome (D-04)."
    - "Setup welcome copy distinguishes not-ready states without assuming a pre-existing LeanQuill repo (D-03 partial — full string audit in 13-04)."
    - "Outline/Research/Characters welcome `when` clauses gate on full workspace readiness where today they incorrectly used file-exists-only initialization."
  artifacts:
    - path: "src/extension.ts"
      provides: "setWorkspaceContext sets leanquill.projectYamlValid, leanquill.manuscriptScaffoldComplete, leanquill.workspaceLeanQuillReady (or equivalent names used consistently in package.json)"
    - path: "package.json"
      provides: "viewsWelcome when clauses updated to D-02/D-03; no stale leanquill.isInitialized-only 'ready' message when Book.txt missing"
  key_links:
    - from: "src/extension.ts"
      to: "vscode.commands.executeCommand(\"setContext\", ...)"
      via: "setWorkspaceContext after fs checks + validateProjectYamlForSetup"
      pattern: "setContext.*leanquill\\."
    - from: "package.json"
      to: "src/extension.ts"
      via: "viewsWelcome when matches keys set in setWorkspaceContext"
      pattern: "viewsWelcome"
---

<objective>
Push **Setup eligibility** into **`setContext` keys** and fix **`package.json` `viewsWelcome`** so authors see Initialize when `.leanquill` looks present but **LeanPub manuscript scaffolding** is incomplete (D-02, D-04).

Purpose: Declarative UI; avoid false “ready” state; align sidebar with Phase 13 CONTEXT.

Output: Updated `setWorkspaceContext` in `src/extension.ts` and revised `viewsWelcome` in `package.json`.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-leanpub-workspace-initialization/13-CONTEXT.md
@src/extension.ts
@src/projectConfig.ts
@package.json

<interfaces>
Current `setWorkspaceContext` (replace/extend — do not leave stale semantics):

```typescript
async function setWorkspaceContext(rootPath: string): Promise<void> {
  const hasBookTxt = await fs.stat(path.join(rootPath, "manuscript", "Book.txt")).then(() => true).catch(() => false);
  const hasManuscript = await fs.stat(path.join(rootPath, "manuscript")).then(() => true).catch(() => false);
  const hasLeanquill = await fs.stat(path.join(rootPath, ".leanquill", "project.yaml")).then(() => true).catch(() => false);

  await vscode.commands.executeCommand("setContext", "leanquill.hasManuscriptMarkers", hasBookTxt || hasManuscript);
  await vscode.commands.executeCommand("setContext", "leanquill.isInitialized", hasLeanquill);
}
```

Use **`validateProjectYamlForSetup`** from Plan 13-01: read file if present, else invalid.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend setWorkspaceContext with D-02/D-04 keys</name>
  <files>src/extension.ts</files>
  <read_first>
    - `src/extension.ts` (`setWorkspaceContext`, activation order)
    - `src/projectConfig.ts` (`readProjectConfig`, `validateProjectYamlForSetup` from 13-01)
    - `.planning/phases/13-leanpub-workspace-initialization/13-CONTEXT.md` (D-02, D-04)
  </read_first>
  <action>
    In **`src/extension.ts`**, extend **`setWorkspaceContext(rootPath)`** to:

    1. Keep existing stats for `manuscript/`, `manuscript/Book.txt`, `.leanquill/project.yaml`.
    2. Set **`leanquill.isInitialized`** = `project.yaml` **file exists** (preserve current meaning for backward compatibility).
    3. After reading YAML text (if file exists), set **`leanquill.projectYamlValid`** = `validateProjectYamlForSetup(content).ok`; if file missing, false.
    4. Set **`leanquill.manuscriptScaffoldComplete`** = `hasManuscript && hasBookTxt` (D-02: both required).
    5. Set **`leanquill.workspaceLeanQuillReady`** = `leanquill.projectYamlValid && leanquill.manuscriptScaffoldComplete` (both must be true strings in `setContext` — use boolean per VS Code API).
    6. Set **`leanquill.setupNeedsAttention`** = `!workspaceLeanQuillReady` (single OR key for welcome copy).

    Ensure **every activation path** that already calls `setWorkspaceContext` still does so after filesystem changes in Plan 13-03 (no orphaned stale context).

    Import `readProjectConfig`/`fs.readFile` as needed; prefer one read for validation (avoid double disk hit).
  </action>
  <acceptance_criteria>
    - `rg -n "setContext\", \"leanquill\\.(projectYamlValid|manuscriptScaffoldComplete|workspaceLeanQuillReady|setupNeedsAttention)" src/extension.ts` returns ≥4 matches (one per key or equivalent batching).
    - `validateProjectYamlForSetup` is referenced from `src/extension.ts` (`rg -n "validateProjectYamlForSetup" src/extension.ts`).
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All new context keys are set from filesystem + validation in one place.</done>
</task>

<task type="auto">
  <name>Task 2: Refine viewsWelcome when clauses and Setup copy (D-01, D-02, D-03)</name>
  <files>package.json</files>
  <read_first>
    - `package.json` (`viewsWelcome` section only + surrounding JSON structure)
    - `src/extension.ts` (exact context key names from Task 1)
    - `.planning/phases/13-leanpub-workspace-initialization/13-CONTEXT.md` (D-01–D-03)
  </read_first>
  <action>
    Update **`package.json`** `viewsWelcome` for `leanquill.actions`:

    1. Replace the third block’s **`when`: `leanquill.isInitialized`** with **`leanquill.workspaceLeanQuillReady`** so “ready to use” only shows when **valid yaml + manuscript dir + Book.txt** (per D-02).
    2. Add a **fourth** `leanquill.actions` entry: **`when`: `leanquill.setupNeedsAttention`** (or `!leanquill.workspaceLeanQuillReady` if you did not add `setupNeedsAttention`) with copy that **does not** say “open a LeanQuill directory” — use **any workspace folder** framing (D-03). Include **`[Initialize or complete setup](command:leanquill.initialize)`** (exact label may vary; must invoke `leanquill.initialize`).
    3. Adjust first two blocks’ `when` so they **do not overlap** the new “incomplete but has markers” states incorrectly — typical pattern:
       - Block A: `setupNeedsAttention && !leanquill.hasManuscriptMarkers`
       - Block B: `setupNeedsAttention && leanquill.hasManuscriptMarkers`
       - Block C (ready): `workspaceLeanQuillReady`
    4. Update **`leanquill.outlineTree`**, **`leanquill.research`**, **`leanquill.characters`** welcome entries: replace **`leanquill.isInitialized`** checks that mean “can use feature” with **`leanquill.workspaceLeanQuillReady`** for the positive path; **`!leanquill.isInitialized`** for “not a project” should become **`!leanquill.workspaceLeanQuillReady`** where the intent is “setup incomplete” (per RESEARCH — false negatives).

    Preserve valid JSON (no trailing commas).
  </action>
  <acceptance_criteria>
    - `rg -n "workspaceLeanQuillReady|setupNeedsAttention" package.json` returns at least one match each (or document equivalent logic with only negated ready key in SUMMARY if intentionally simplified).
    - No `viewsWelcome` block for `leanquill.actions` uses bare `"when": "leanquill.isInitialized"` as the sole condition for the “ready to use” message (`rg -n "This project is initialized and ready" -n package.json` context must pair with `workspaceLeanQuillReady`).
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('ok')"</automated>
  </verify>
  <done>Setup view states match D-02; copy allows any-folder workflow (D-03 partial).</done>
</task>

</tasks>

<verification>
1. `npm run build` passes.
2. `package.json` parses as JSON.
3. Context key names in `extension.ts` exactly match `package.json` `when` clauses.
</verification>

<success_criteria>
- Authors missing `manuscript/Book.txt` still see an Initialize path in Setup.
- Feature views gate on true readiness, not yaml file existence alone.
</success_criteria>

<output>
After completion, create `.planning/phases/13-leanpub-workspace-initialization/13-02-SUMMARY.md`
</output>
