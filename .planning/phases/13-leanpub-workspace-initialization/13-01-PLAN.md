---
phase: 13-leanpub-workspace-initialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/projectConfig.ts
  - src/leanpubScaffold.ts
autonomous: true
requirements:
  - INIT-02

must_haves:
  truths:
    - "Scaffold logic never writes outside workspace root/manuscript/ for manuscript files."
    - "Existing manuscript/Book.txt is never modified by the scaffold (create-only)."
    - "Existing chapter file matching ch1 case-insensitively is never overwritten; its on-disk basename is used in a new Book.txt line."
    - "New Book.txt contains only relative paths under manuscript/ matching resolver rules (e.g. ch1.md line)."
    - "When Book.txt exists but listing omits an existing default chapter file, scaffold returns blocked with no filesystem mutation (D-18)."
    - "Invalid or incomplete project.yaml is detectable for Setup eligibility (D-02)."
  artifacts:
    - path: "src/projectConfig.ts"
      provides: "validateProjectYamlForSetup(content) or equivalent export used by setWorkspaceContext"
    - path: "src/leanpubScaffold.ts"
      provides: "Pure/temp-dir-testable manuscript scaffold plan + apply with D-07–D-12 semantics"
  key_links:
    - from: "src/leanpubScaffold.ts"
      to: "manuscript/Book.txt"
      via: "create-only write when missing; parse lines same semantics as chapterOrder"
      pattern: "Book\\.txt"
    - from: "src/projectConfig.ts"
      to: ".leanquill/project.yaml"
      via: "read + validate for boolean context"
      pattern: "validateProjectYamlForSetup|project\\.yaml"
---

<objective>
Add **testable** manuscript scaffold helpers and **project.yaml validity** rules so Phase 13 orchestration can run without broadening `SafeFileSystem` to all `manuscript/*.md` (D-05).

Purpose: Isolate filesystem and `Book.txt` rules in one module; keep INIT-02 alignment (`resolveChapterOrder` / `Book.txt` line format) explicit.

Output: `src/leanpubScaffold.ts`, extended `src/projectConfig.ts` with a setup-validation export.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-leanpub-workspace-initialization/13-CONTEXT.md
@.planning/phases/13-leanpub-workspace-initialization/13-RESEARCH.md
@src/chapterOrder.ts
@src/safeFileSystem.ts

<interfaces>
From `src/chapterOrder.ts` (Book.txt parsing — scaffold must not invent a conflicting format):

```typescript
export async function resolveChapterOrder(rootPath: string): Promise<ChapterOrderResult>;
```

New manuscript paths must remain **relative to `manuscript/`** on each non-empty, non-`#`, non-`part:` line (see `resolveChapterOrder` implementation).

From `src/safeFileSystem.ts` (D-05 — do **not** add permanent `manuscript/**/*.md` allowlist):

```typescript
export class SafeFileSystem {
  public canWrite(targetPath: string, isFileOperation?: boolean): boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export project.yaml validation for Setup (D-02)</name>
  <files>src/projectConfig.ts</files>
  <read_first>
    - `src/projectConfig.ts` (full file — extend in place)
    - `src/initialize.ts` (`renderProjectYaml` — required keys/shape to mirror)
    - `.planning/phases/13-leanpub-workspace-initialization/13-CONTEXT.md` (D-02, Claude discretion on exact rules)
  </read_first>
  <action>
    Add and export **`validateProjectYamlForSetup(content: string): { ok: boolean; reason?: string }`** (name exact or rename consistently across call sites in Plan 13-02).

    **Concrete rules (minimal, per D-02 discretion):**
    1. `content.trim().length > 0`; else `{ ok: false, reason: "empty" }`.
    2. `parseProjectConfig(content)` must yield `schemaVersion` **only** `"1"` or `"2"` (after trim); else not ok.
    3. Regex must confirm **`project_id:`** and **`working_title:`** each present on their own lines (match `renderProjectYaml` output shape).
    4. If `folders:` block exists, require **`manuscript:`** under `folders:` (same indentation style as `renderProjectYaml`).

    Do **not** add a YAML npm dependency unless you also add it to `package.json` and justify in SUMMARY (default: **no** new deps).

    Reuse `parseProjectConfig` where possible; do not duplicate folder parsing logic unnecessarily.
  </action>
  <acceptance_criteria>
    - `rg -n "validateProjectYamlForSetup" src/projectConfig.ts` returns at least one match.
    - `rg -n "export (async )?function validateProjectYamlForSetup|export \\{[^}]*validateProjectYamlForSetup" src/projectConfig.ts` confirms export.
    - For string containing only `schema_version: "2"\nproject_id: 'x'\nworking_title: 'y'\n` plus minimal `folders:` with `manuscript:` — function returns `ok: true` (unit test in Plan 13-04 may assert; optional inline `node` snippet in SUMMARY if no test yet).
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Project YAML validation export exists, compiles, and encodes minimal schema + required fields for Setup gating.</done>
</task>

<task type="auto">
  <name>Task 2: leanpubScaffold module — plan, apply, D-09–D-12, D-18 blocked path</name>
  <files>src/leanpubScaffold.ts</files>
  <read_first>
    - `src/chapterOrder.ts` (Book.txt line rules, `listMarkdownFiles` if exported or duplicate logic carefully)
    - `src/safeFileSystem.ts` (path escape guard pattern: `path.relative(root, abs)`, reject `..`)
    - `.planning/phases/13-leanpub-workspace-initialization/13-CONTEXT.md` (D-07–D-12, D-18)
  </read_first>
  <action>
    Create **`src/leanpubScaffold.ts`** with **Node `fs/promises` only** for `ch1.md` / mkdir (D-06 raw fs with strict checks). Use **`safeFs.writeFile` only** for `manuscript/Book.txt` when you already have a `SafeFileSystem` instance in callers **or** keep this module fs-only and document that `Book.txt` write path must be validated identical to `SafeFileSystem` rules before call from `extension.ts` / `initialize.ts`.

    **Recommended shape (implementation may vary; behavior must match):**
    - `findDefaultChapterBasename(manuscriptDir)` — returns Promise resolving to string basename or null: scan directory entries for `.md` whose basename equals `ch1` case-insensitively; return **actual** basename or null.
    - `readBookTxtLinesIfExists(bookTxtPath)` — returns Promise resolving to string lines or null if file missing.
    - `chapterListedInBookTxt(lines, basename): boolean` — trim, ignore `#` and `part:` lines like `resolveChapterOrder`.
    - `applyLeanpubManuscriptScaffold(rootPath, options?)` — returns Promise of `LeanpubScaffoldResult` discriminated union:
      - `{ status: "noop" | "success" | "blocked"; created: string[]; skipped: string[]; message: string }` — **no overwrites** (D-07); **blocked** when `Book.txt` exists and default chapter exists on disk but is **not** listed (D-11 + D-13 + D-18).
    - **Create `manuscript/`** if missing (mkdir recursive).
    - **Create `Book.txt`** only when missing; content: single line with basename from D-09/D-10 (e.g. `ch1.md\n` or existing `Ch1.md\n`).
    - **Create placeholder `ch1.md`** only when no case-insensitive `ch1` match; content first line **`# Chapter 1`** plus one short stub sentence (D-12).
    - **Never** append/edit existing `Book.txt`.

    If `options.safeFs` provided, use `safeFs.writeFile` for `path.join(rootPath, "manuscript", "Book.txt")` only; use raw `fs.writeFile` for chapter only after proving resolved absolute path is under `rootPath/manuscript` with `relative` guard.

    Export types used by `initialize.ts` / tests.
  </action>
  <acceptance_criteria>
    - File `src/leanpubScaffold.ts` exists and `rg -n "applyLeanpubManuscriptScaffold|LeanpubScaffoldResult" src/leanpubScaffold.ts` matches.
    - `rg -n "manuscript\\*\\*\\.md|allowPath\\(.*manuscript" src/safeFileSystem.ts` still **no** broad manuscript md allowlist (D-05).
    - Scaffold function returns `blocked` (or equivalent enum) **without** calling `writeFile` on existing `Book.txt` when scenario is: Book.txt on disk lists no chapter line matching existing `ch1.md` on disk (document exact condition in code comment near D-18 handling).
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Scaffold module compiles; implements create-only Book.txt, case-insensitive ch1 detection, placeholder content, and blocked path for D-18.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes.
2. No new permanent `SafeFileSystem` allowlist for arbitrary manuscript markdown (D-05).
3. `validateProjectYamlForSetup` and `applyLeanpubManuscriptScaffold` are exported for Plan 13-02/13-04.
</verification>

<success_criteria>
- Setup validation and scaffold helpers live in dedicated modules with explicit D-07/D-11/D-18 behavior.
- INIT-02: generated `Book.txt` lines remain compatible with `resolveChapterOrder`.
</success_criteria>

<output>
After completion, create `.planning/phases/13-leanpub-workspace-initialization/13-01-SUMMARY.md`
</output>
