---
phase: 13-leanpub-workspace-initialization
plan: 04
type: execute
wave: 4
depends_on:
  - "13-03"
files_modified:
  - test/leanpubScaffold.test.ts
  - test/projectConfig.test.ts
  - src/extension.ts
  - src/initialize.ts
  - package.json
autonomous: true
requirements:
  - INIT-01
  - INIT-02

must_haves:
  truths:
    - "Unit tests cover scaffold create-only, case-insensitive ch1 basename, blocked D-18, and no writes outside manuscript/."
    - "User-visible strings in scoped files do not assume a pre-existing LeanQuill-only folder (D-03)."
    - "Full `npm test` passes after `npm run build:test`."
  artifacts:
    - path: "test/leanpubScaffold.test.ts"
      provides: "Temp-dir tests for leanpubScaffold module"
    - path: "test/projectConfig.test.ts"
      provides: "Tests for validateProjectYamlForSetup true/false cases"
  key_links:
    - from: "test/leanpubScaffold.test.ts"
      to: "src/leanpubScaffold.ts"
      via: "imports apply/API under test"
      pattern: "leanpubScaffold"
---

<objective>
Lock Phase 13 behavior with **automated tests** and finish **copy audit** for INIT-01/INIT-02 onboarding strings.

Purpose: Nyquist-style fast feedback; D-03 string hygiene.

Output: New/extended tests under `test/`; grep-verifiable copy updates in whitelisted files.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-leanpub-workspace-initialization/13-VALIDATION.md
@.planning/phases/13-leanpub-workspace-initialization/13-CONTEXT.md
@test/safeFileSystem.test.ts
@test/chapterOrder.test.ts

<interfaces>
Follow **`withTempDir`** (or equivalent) from `test/safeFileSystem.test.ts` for filesystem isolation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests — validateProjectYamlForSetup + leanpubScaffold</name>
  <files>test/projectConfig.test.ts, test/leanpubScaffold.test.ts</files>
  <read_first>
    - `test/safeFileSystem.test.ts` (temp dir helper pattern)
    - `test/projectConfig.test.ts` (existing tests — extend)
    - `src/leanpubScaffold.ts` (exported functions)
    - `src/projectConfig.ts` (`validateProjectYamlForSetup`)
  </read_first>
  <behavior>
    - `validateProjectYamlForSetup`: returns ok for minimal valid v2 template-shaped yaml; not ok for empty string; not ok for missing `project_id`; not ok for `schema_version: "9"`.
    - `applyLeanpubManuscriptScaffold`: empty temp dir → creates `manuscript/ch1.md`, `manuscript/Book.txt` with line `ch1.md`, `ch1.md` content contains `# Chapter 1`.
    - Existing `manuscript/Book.txt` on disk → file byte-identical after run (read before/after).
    - Existing `manuscript/Ch1.md` (case) + no Book.txt → creates Book.txt containing **`Ch1.md`** line (actual basename), does not create second file.
    - Book.txt exists listing other file only + ch1.md exists → result **`blocked`** (or equivalent) and Book.txt unchanged.
    - Attempt to write outside `root/manuscript` is impossible via public API (negative test if any escape path exists).
  </behavior>
  <action>
    Add **`test/leanpubScaffold.test.ts`** compiled by existing **`npm run build:test`** pipeline (`test/*.test.ts`).

    Extend **`test/projectConfig.test.ts`** with cases for **`validateProjectYamlForSetup`**.

    Use **`node:test`** + **`assert/strict`** consistent with repo.
  </action>
  <acceptance_criteria>
    - File `test/leanpubScaffold.test.ts` exists.
    - `npm run build:test && npm test -- dist-test/leanpubScaffold.test.js` passes (or full `npm test` if filter unsupported on Windows — then `npm test` alone is acceptable).
    - `rg -n "blocked|Blocked" test/leanpubScaffold.test.ts` matches at least once (D-18 case).
  </acceptance_criteria>
  <verify>
    <automated>npm run build:test && npm test</automated>
  </verify>
  <done>Automated coverage for validation + scaffold invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Copy audit D-03 — grep and fix onboarding strings</name>
  <files>package.json, src/initialize.ts, src/extension.ts</files>
  <read_first>
    - `.planning/phases/13-leanpub-workspace-initialization/13-CONTEXT.md` (D-03)
    - `package.json` (viewsWelcome strings)
    - `src/initialize.ts` (`showInputBox` titles, `showErrorMessage` strings)
    - `src/extension.ts` (user-facing `showInformationMessage` / errors if any)
  </read_first>
  <action>
    1. Run ripgrep (or VS Code search) for patterns: **`LeanQuill directory`**, **`LeanQuill project`**, **`manuscript folder or Book.txt`** (root Book.txt implication), **`Open a workspace`** phrasing that excludes “any folder”.

    2. Update matches in **`package.json`**, **`src/initialize.ts`**, **`src/extension.ts`** only (Phase 13 scope) so strings allow **initializing from any open folder** and **LeanPub layout under `manuscript/`** when relevant.

    3. Do **not** change unrelated files unless grep hits a clear Phase 13 onboarding string in those three files only.

    4. Add one line to **13-04-SUMMARY.md** listing changed string fingerprints (before→after not required — list keys/paths updated).
  </action>
  <acceptance_criteria>
    - `rg -n "Open a workspace containing your manuscript folder or Book\\.txt" package.json` returns **no matches** (replace with manuscript-path-accurate wording).
    - `rg -ni "leanquill directory" package.json src/initialize.ts src/extension.ts` returns **no matches** after edits.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:test && npm test</automated>
  </verify>
  <done>D-03 satisfied for scoped files; tests still green.</done>
</task>

</tasks>

<verification>
1. `npm run build:test && npm test` full suite green.
2. Scaffold tests prevent D-07/D-11/D-18 regressions.
3. Copy grep checks pass.
</verification>

<success_criteria>
- INIT-01/INIT-02 behaviors regression-tested via scaffold + validation tests.
- Onboarding copy consistent with “any folder” CONTEXT.
</success_criteria>

<output>
After completion, create `.planning/phases/13-leanpub-workspace-initialization/13-04-SUMMARY.md`
</output>
