---
phase: 15-import-claude-desktop-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/initialize.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Fresh LeanQuill init writes `.leanquill/workflows/import-external-research.md` beside `research.md`"
    - "Import harness entry points are created for Copilot, Cursor, and Claude when missing, each pointing at the import workflow file"
    - "Import workflow text requires reading `.leanquill/workflows/research.md` for output shape and states D-07–D-12 normalization, collision, and save rules"
  artifacts:
    - path: "src/initialize.ts"
      provides: "RESEARCH_IMPORT_WORKFLOW_CONTENT, initializeProject writes import workflow, writeHarnessEntryPoints emits three import harness files"
  key_links:
    - from: "writeHarnessEntryPoints"
      to: ".leanquill/workflows/import-external-research.md"
      via: "harness file content instructs read canonical import workflow"
      pattern: "import-external-research"
---

<objective>
Add the canonical **import external research** workflow markdown and **three harness entry points** (`LeanQuill-Import-Research` / `leanquill-import-research`) generated on init and on activation when files are absent — matching Phase 12’s pattern (D-04, D-06).

Purpose: Book repos get a single on-disk contract agents follow for normalization into Phase 12 research notes (D-07–D-12).

Output: Updated `src/initialize.ts` with embedded import workflow content, `initializeProject` writing `.leanquill/workflows/import-external-research.md`, and extended `writeHarnessEntryPoints` creating import harness files under `.github/agents/`, `.cursor/skills/`, and `.claude/agents/`.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-import-claude-desktop-research/15-CONTEXT.md
@.planning/phases/15-import-claude-desktop-research/15-RESEARCH.md
@src/initialize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embed import workflow content and write on initialize</name>
  <files>src/initialize.ts</files>
  <read_first>
    - C:/Dev/LQ/src/initialize.ts
    - C:/Dev/LQ/.planning/phases/15-import-claude-desktop-research/15-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "RESEARCH_IMPORT_WORKFLOW_CONTENT" C:/Dev/LQ/src/initialize.ts` returns at least one match
    - `rg -n "import-external-research\\.md" C:/Dev/LQ/src/initialize.ts` returns at least one match
    - `rg -n "workflows/import-external-research" C:/Dev/LQ/src/initialize.ts` returns at least one match
    - `rg -n "workflows/research\\.md" C:/Dev/LQ/src/initialize.ts` matches within RESEARCH_IMPORT_WORKFLOW_CONTENT or adjacent comments (import workflow must reference reading research.md for output shape per D-06/D-07)
    - `rg -n "Imported content|D-12|collision|folders\\.research" C:/Dev/LQ/src/initialize.ts` returns at least three distinct lines (normalization + collision + save location prose present)
  </acceptance_criteria>
  <action>
    1. Add a new exported or module-level const `RESEARCH_IMPORT_WORKFLOW_CONTENT` (multi-line template string) in `src/initialize.ts` with YAML frontmatter including literal lines `name: LeanQuill Import External Research Workflow` and `version: 1` (or equivalent single version field matching `RESEARCH_WORKFLOW_CONTENT` style).

    2. Body must explicitly instruct the agent to: (a) read `.leanquill/workflows/research.md` for **Result File Format** (frontmatter keys `name`, `query`, `created`, `tags`, `sources` and body sections per Phase 12); (b) map external source material best-effort into that shape without dropping unmapped content — park under **Summary** and/or a `### Imported content` subsection (D-07); (c) `created`: use import-time ISO unless the source has an explicit reliable date (D-08); (d) `sources`: URLs when obvious else `[]` (D-09); (e) `query`: from user clarification and/or inferred from headings/first lines, else a clear placeholder string such as `(import — topic to be confirmed)` (D-10); (f) prefer writing the final `.md` under `folders.research` from `project.yaml`; if the harness cannot write files, output markdown only for manual save (D-11); (g) before write, list existing `*.md` in `folders.research`, enforce `{topic-slug}-{YYYY-MM-DD}.md` and on collision pick a new slug (no overwrite) (D-12); (h) mention paste/attachments for PDF/DOCX when the model cannot read binary (CONTEXT discretion).

    3. In `initializeProject`, after writing `.leanquill/workflows/research.md`, call `safeFs.writeFile` for path `.leanquill/workflows/import-external-research.md` with `RESEARCH_IMPORT_WORKFLOW_CONTENT` (same directory creation pattern as `research.md`).
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build</automated>
  </verify>
  <done>Build passes; `initializeProject` writes both workflow files when run; import template string documents D-07–D-12 and points at `research.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Extend writeHarnessEntryPoints with three import harness files</name>
  <files>src/initialize.ts</files>
  <read_first>
    - C:/Dev/LQ/src/initialize.ts (functions `writeHarnessEntryPoints`, researcher `copilotContent` / `cursorContent` / `claudeContent` patterns)
    - C:/Dev/LQ/.planning/phases/15-import-claude-desktop-research/15-CONTEXT.md (D-04, D-05, D-06)
  </read_first>
  <acceptance_criteria>
    - `rg -n "leanquill-import-research" C:/Dev/LQ/src/initialize.ts` returns at least 6 matches (paths and/or YAML `name:` / prose)
    - `rg -n "import-external-research\\.md" C:/Dev/LQ/src/initialize.ts` returns at least 2 matches outside RESEARCH_IMPORT_WORKFLOW_CONTENT (harness bodies must reference the file)
    - `rg -n "\\.github/agents/leanquill-import-research\\.agent\\.md|agents/leanquill-import-research" C:/Dev/LQ/src/initialize.ts` returns ≥1
    - `rg -n "\\.cursor/skills/leanquill-import-research" C:/Dev/LQ/src/initialize.ts` returns ≥1
    - `rg -n "\\.claude/agents/leanquill-import-research\\.md" C:/Dev/LQ/src/initialize.ts` returns ≥1
    - `rg -n "name: leanquill-import-research" C:/Dev/LQ/src/initialize.ts` returns ≥3 (one per harness template)
  </acceptance_criteria>
  <action>
    1. In `writeHarnessEntryPoints`, define three **new** paths (do not remove existing researcher files):
       - Copilot: `.github/agents/leanquill-import-research.agent.md`
       - Cursor: `.cursor/skills/leanquill-import-research/SKILL.md`
       - Claude: `.claude/agents/leanquill-import-research.md`

    2. Each file’s YAML frontmatter must use `name: leanquill-import-research` and a description string containing the exact substring `LeanQuill-Import-Research` (D-05 user-facing naming). Copilot: reuse tools pattern from researcher (`['read', 'write', 'search', 'web']` or equivalent write-capable set). Claude: reuse tools line pattern from existing `researcher.md` template in the same function. Cursor: include `<cursor_skill_adapter>` block stating invocation when the user mentions `leanquill-import-research` / import of external research, and that execution reads `.leanquill/workflows/import-external-research.md` first.

    3. Body prose for each harness: read `folders.research` from `project.yaml`, read `.leanquill/workflows/import-external-research.md`, then perform import normalization per that file; do not reference a different workflow filename.

    4. Append these three entries to the same `entries` array loop pattern: `fs.mkdir` recursive, `fs.stat` skip-if-exists (preserve idempotent behavior per Phase 12).
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build</automated>
  </verify>
  <done>Idempotent harness writer creates three import files when absent; each references `import-external-research.md` and `name: leanquill-import-research`.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Grep checks in acceptance_criteria pass for `src/initialize.ts`
3. Fresh init path (manual or test fixture): both workflow files and new harness files appear when missing
</verification>

<success_criteria>
- `RESEARCH_IMPORT_WORKFLOW_CONTENT` exists and encodes D-07–D-12 + link to `research.md` output shape
- `initializeProject` writes `.leanquill/workflows/import-external-research.md`
- `writeHarnessEntryPoints` emits Copilot/Cursor/Claude import harness files with `name: leanquill-import-research`
</success_criteria>

<output>
After completion, create `.planning/phases/15-import-claude-desktop-research/15-01-SUMMARY.md`
</output>
