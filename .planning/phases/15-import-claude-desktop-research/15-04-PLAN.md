---
phase: 15-import-claude-desktop-research
plan: 04
type: execute
wave: 4
depends_on:
  - "02"
  - "03"
files_modified:
  - test/harnessChatDraft.test.ts
  - .planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "`npm test` passes with new unit tests covering all four draft combinations and both fallback hints"
    - "`15-VERIFICATION.md` lists grep-style checks and a manual UAT matrix keyed to D-01–D-03 and D-06–D-12"
  artifacts:
    - path: "test/harnessChatDraft.test.ts"
      provides: "Automated coverage for harnessChatDraft pure functions"
    - path: ".planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md"
      provides: "Phase verification checklist + manual steps"
  key_links:
    - from: "harnessChatDraft.test.ts"
      to: "src/harnessChatDraft.ts"
      via: "imports and asserts exported functions"
      pattern: "harnessChatDraft"
---

<objective>
Lock behavior with **unit tests** for chat draft strings and publish a **VERIFICATION.md** checklist for harness-specific manual UAT (D-01–D-03, agent reads workflow, collision policy D-12).

Purpose: Nyquist-style automated coverage for pure logic; human matrix for Copilot/Cursor/Claude differences.

Output: `test/harnessChatDraft.test.ts` and `.planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md`.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-import-claude-desktop-research/15-VALIDATION.md
@.planning/phases/15-import-claude-desktop-research/15-RESEARCH.md (Validation Architecture)
@src/harnessChatDraft.ts
@test/researchTree.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for harnessChatDraft</name>
  <files>test/harnessChatDraft.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/harnessChatDraft.ts
    - C:/Dev/LQ/test/researchTree.test.ts (first 40 lines: imports, `describe` / `it` style)
    - C:/Dev/LQ/package.json (`scripts.test`, `scripts.build:test`)
  </read_first>
  <acceptance_criteria>
    - `test -f C:/Dev/LQ/test/harnessChatDraft.test.ts` exits 0
    - `rg -n "buildHarnessDraftQuery|buildHarnessFallbackHint" C:/Dev/LQ/test/harnessChatDraft.test.ts` returns ≥2
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
    - `rg -n "@leanquill-researcher " C:/Dev/LQ/test/harnessChatDraft.test.ts` returns ≥1
    - `rg -n "@leanquill-import-research " C:/Dev/LQ/test/harnessChatDraft.test.ts` returns ≥1
    - `rg -n "Import research:" C:/Dev/LQ/test/harnessChatDraft.test.ts` returns ≥1
    - `rg -n "Research:" C:/Dev/LQ/test/harnessChatDraft.test.ts` returns ≥1
  </acceptance_criteria>
  <action>
    1. Add `test/harnessChatDraft.test.ts` using `node:test` and `assert` (same as existing tests).

    2. Assert `buildHarnessDraftQuery({ isCursorOrCopilot: true, kind: "research" }) === "@leanquill-researcher "` (exact).

    3. Assert `buildHarnessDraftQuery({ isCursorOrCopilot: true, kind: "import" }) === "@leanquill-import-research "` (exact).

    4. Assert `buildHarnessDraftQuery({ isCursorOrCopilot: false, kind: "research" }) === "Research: "` (exact).

    5. Assert `buildHarnessDraftQuery({ isCursorOrCopilot: false, kind: "import" }) === "Import research: "` (exact).

    6. Assert `buildHarnessFallbackHint("research")` contains substring `/agent:leanquill-researcher` and `LeanQuill-Researcher`.

    7. Assert `buildHarnessFallbackHint("import")` contains substring `/agent:leanquill-import-research` and `LeanQuill-Import-Research`.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>All assertions green; draft coverage matches post-plan-03 strings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Author 15-VERIFICATION.md checklist</name>
  <files>.planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md</files>
  <read_first>
    - C:/Dev/LQ/.planning/phases/15-import-claude-desktop-research/15-VALIDATION.md
    - C:/Dev/LQ/.planning/phases/15-import-claude-desktop-research/15-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `test -f C:/Dev/LQ/.planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md` exits 0
    - `rg -n "D-01|D-02|D-03|D-06|D-07|D-12" C:/Dev/LQ/.planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md` returns ≥5 lines
    - `rg -n "leanquill\\.startImportResearch|startImportResearch" C:/Dev/LQ/.planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md` returns ≥1
    - `rg -n "import-external-research\\.md" C:/Dev/LQ/.planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md` returns ≥1
    - `rg -n "Cursor|Copilot|Claude" C:/Dev/LQ/.planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md` returns ≥3
    - File contains a markdown checkbox list with at least 6 `- [ ]` items
  </acceptance_criteria>
  <action>
    Create `.planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md` with YAML frontmatter keys: `phase: 15`, `slug: import-claude-desktop-research`, `status: draft`, `created: 2026-04-11` (or current execution date).

    Sections required:
    1. **Automated** — bullet listing exact commands: `npm run build`, `npm run build:test`, `npm test`.
    2. **Static grep checks** — bullets with exact `rg` patterns verifying `leanquill.startImportResearch` in `package.json` and `extension.ts`, and `import-external-research.md` in `initialize.ts`.
    3. **Manual UAT matrix** — table or bullet rows for **Cursor**, **VS Code + Copilot Chat**, **Claude Code** each with pass criteria: chat opens with prefilled text, message **not** auto-sent (D-01/D-02); user can attach/paste per D-03; agent reads `.leanquill/workflows/import-external-research.md` (D-06); saved file under `folders.research` with Phase 12 frontmatter keys (D-07/D-11); two imports same day same slug yield distinct filenames, no overwrite (D-12).

    4. **Sign-off** — checkbox line for reviewer name/date placeholder.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && rg -n "D-01" .planning/phases/15-import-claude-desktop-research/15-VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md exists; covers automated, static, manual, sign-off; decision IDs traceable.</done>
</task>

</tasks>

<verification>
1. `npm run build:test && npm test` green after Task 1
2. `15-VERIFICATION.md` satisfies acceptance grep checks
3. Human completes checkpoint Task 2 review of checklist accuracy
</verification>

<success_criteria>
- Pure draft logic covered by tests
- Phase verification artifact ready for `/gsd-verify-work` or human UAT
</success_criteria>

<output>
After completion, create `.planning/phases/15-import-claude-desktop-research/15-04-SUMMARY.md`
</output>
