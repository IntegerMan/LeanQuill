---
phase: 15-import-claude-desktop-research
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/harnessChatDraft.ts
  - src/extension.ts
  - package.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Command `leanquill.startImportResearch` is registered and opens chat with `isPartialQuery: true`"
    - "Cursor/Copilot branch prefixes draft with `@leanquill-import-research ` (trailing space)"
    - "Research view title area exposes an Import action alongside New Research"
    - "viewsWelcome for Research mentions Import path when workspace is ready"
  artifacts:
    - path: "src/harnessChatDraft.ts"
      provides: "Pure helpers for harness-specific draft prefixes (research vs import)"
    - path: "src/extension.ts"
      provides: "startImportResearch command + startResearch refactored to use helpers"
    - path: "package.json"
      provides: "Command contribution, activationEvents, menus, welcome copy"
  key_links:
    - from: "leanquill.startImportResearch"
      to: "workbench.action.chat.open"
      via: "{ query, isPartialQuery: true }"
      pattern: "startImportResearch|isPartialQuery"
---

<objective>
Expose **Import** in the Research sidebar and command surface: new chat, partial query invoking **leanquill-import-research**, never auto-send (D-01, D-02, D-03).

Purpose: Parity with `leanquill.startResearch` / `+` while keeping harness strings testable in isolation.

Output: `src/harnessChatDraft.ts`, refactored `leanquill.startResearch` + new `leanquill.startImportResearch` in `src/extension.ts`, and `package.json` contributions (command, `onCommand` activation, `view/title`, `viewsWelcome`).
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-import-claude-desktop-research/15-CONTEXT.md
@src/extension.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add harnessChatDraft helpers and wire startResearch + startImportResearch</name>
  <files>src/harnessChatDraft.ts, src/extension.ts</files>
  <read_first>
    - C:/Dev/LQ/src/extension.ts (block registering `leanquill.startResearch`, lines ~379–405)
    - C:/Dev/LQ/package.json (scripts and main entry)
  </read_first>
  <acceptance_criteria>
    - `rg -n "leanquill\\.startImportResearch" C:/Dev/LQ/src/extension.ts` returns ≥1
    - `rg -n "registerCommand\\(\"leanquill\\.startImportResearch\"" C:/Dev/LQ/src/extension.ts` returns ≥1
    - `rg -n "@leanquill-import-research " C:/Dev/LQ/src/harnessChatDraft.ts` returns ≥1 (exact trailing space after mention)
    - `rg -n "isPartialQuery:\\s*true" C:/Dev/LQ/src/extension.ts` returns ≥2 (both research and import commands use partial query)
    - `rg -n "workbench\\.action\\.chat\\.newChat" C:/Dev/LQ/src/extension.ts` returns ≥2
    - `rg -n "buildHarnessDraftQuery|harnessChatDraft" C:/Dev/LQ/src/extension.ts` returns ≥1
    - `test -f C:/Dev/LQ/src/harnessChatDraft.ts` exits 0
  </acceptance_criteria>
  <action>
    1. Create `src/harnessChatDraft.ts` exporting a pure function, e.g. `buildHarnessDraftQuery(options: { isCursorOrCopilot: boolean; kind: "research" | "import" }): string`:
       - When `isCursorOrCopilot === true` and `kind === "research"`, return exactly `@researcher ` (single trailing space) — **unchanged** from current `extension.ts` behavior until plan 03 renames.
       - When `isCursorOrCopilot === true` and `kind === "import"`, return exactly `@leanquill-import-research ` (single trailing space).
       - When `isCursorOrCopilot === false` and `kind === "research"`, return exactly `Research: ` (trailing space).
       - When `isCursorOrCopilot === false` and `kind === "import"`, return exactly `Import research: ` (trailing space).

    2. Export `buildHarnessFallbackHint(kind: "research" | "import"): string` where:
       - `research` includes substring `/agent:researcher` (preserve current fallback text intent).
       - `import` includes substring `/agent:leanquill-import-research` and substring `LeanQuill-Import-Research`.

    3. Refactor `leanquill.startResearch` in `src/extension.ts` to compute `isCursorOrCopilot` exactly as today (`appName` includes `"cursor"` OR extension `github.copilot-chat` present), then set `query = buildHarnessDraftQuery({ isCursorOrCopilot, kind: "research" })`, and on `workbench.action.chat.open` failure show `showInformationMessage(buildHarnessFallbackHint("research"))`.

    4. Register `leanquill.startImportResearch` duplicating the same try/catch sequence: `workbench.action.chat.newChat` (ignore errors), build query with `buildHarnessDraftQuery({ isCursorOrCopilot, kind: "import" })`, `workbench.action.chat.open` with `{ query, isPartialQuery: true }`, fallback `buildHarnessFallbackHint("import")`.

    5. Push the new command registration onto `context.subscriptions` beside `startResearchCommand`.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build</automated>
  </verify>
  <done>Both commands compile; import uses `@leanquill-import-research ` on Cursor/Copilot branch; partial query preserved.</done>
</task>

<task type="auto">
  <name>Task 2: package.json command, activation, view title, viewsWelcome</name>
  <files>package.json</files>
  <read_first>
    - C:/Dev/LQ/package.json (`commands`, `activationEvents`, `menus.view/title`, `viewsWelcome` for `leanquill.research`)
  </read_first>
  <acceptance_criteria>
    - `rg -n "\"leanquill\\.startImportResearch\"" C:/Dev/LQ/package.json` returns ≥3 (activationEvents + commands + menu minimum)
    - `rg -n "startImportResearch" C:/Dev/LQ/package.json` returns ≥4
    - `rg -n "view == leanquill\\.research" C:/Dev/LQ/package.json` matches both `startResearch` and `startImportResearch` menu entries
    - `rg -n "Import" C:/Dev/LQ/package.json` matches the new command `title` containing `Import` (e.g. `LeanQuill: Import External Research` or `Import Research`)
    - `rg -n "leanquill\\.research" C:/Dev/LQ/package.json` in `viewsWelcome` block shows updated copy containing substring `Import` and retaining substring `+` or `plus` reference to New Research (case-insensitive OK)
  </acceptance_criteria>
  <action>
    1. Add to `contributes.commands` an object with `"command": "leanquill.startImportResearch"` and `"title"` including the word `Import` and category `LeanQuill`, with `"icon": "$(cloud-download)"` (or `$(import)` if valid codicon in theme; prefer `$(cloud-download)` for portability).

    2. Add `"onCommand:leanquill.startImportResearch"` to `activationEvents` array (exact string).

    3. Under `menus` → `view/title`, add a second entry for `view == leanquill.research` with `command`: `leanquill.startImportResearch`, `group`: `navigation` (use `group` `navigation@2` or similar if needed so Import appears next to + ; must not remove existing `leanquill.startResearch` entry).

    4. Update `viewsWelcome` content for `view`: `leanquill.research` (when `leanquill.workspaceLeanQuillReady`) to mention using the **Import** title button / command in addition to the + button for new research, without claiming the extension sends chat messages.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && node -e "const p=require('./package.json'); const ids=p.contributes.commands.map(c=>c.command); if(!ids.includes('leanquill.startImportResearch')) process.exit(1); if(!p.activationEvents.includes('onCommand:leanquill.startImportResearch')) process.exit(2); console.log('ok');"</automated>
  </verify>
  <done>package.json validates; new command discoverable; Research welcome copy references Import.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Node parse check in Task 2 verify passes
3. `rg` acceptance criteria hold
</verification>

<success_criteria>
- Import command mirrors research chat-open pattern with partial query
- Research view shows Import alongside New Research
- Activation loads extension when Import command runs
</success_criteria>

<output>
After completion, create `.planning/phases/15-import-claude-desktop-research/15-02-SUMMARY.md`
</output>
