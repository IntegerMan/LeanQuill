---
phase: 15-import-claude-desktop-research
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/initialize.ts
  - src/harnessChatDraft.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Newly generated researcher harness templates use YAML `name: leanquill-researcher` and prose aligned to LeanQuill-Researcher (D-05)"
    - "`RESEARCH_WORKFLOW_CONTENT` Harness Setup lists paths that include the leanquill-researcher entry files"
    - "Chat draft for research uses `@leanquill-researcher ` on Cursor/Copilot branch; Claude fallback references `/agent:leanquill-researcher`"
    - "Existing on-disk harness files are never overwritten by `writeHarnessEntryPoints` (idempotent skip preserved; no migration — CONTEXT deferred)"
  artifacts:
    - path: "src/initialize.ts"
      provides: "Researcher harness templates + RESEARCH_WORKFLOW_CONTENT harness section updated for branded naming"
    - path: "src/harnessChatDraft.ts"
      provides: "Research branch draft/fallback strings updated to leanquill-researcher"
  key_links:
    - from: "buildHarnessDraftQuery(kind: research)"
      to: "@leanquill-researcher mention"
      via: "returned query prefix"
      pattern: "@leanquill-researcher"
---

<objective>
Align **LeanQuill-Researcher** naming (D-05) for **new** harness files and **chat drafts**, without automatic migration of existing author repos (CONTEXT deferred: no batch rename).

Purpose: User-facing discoverability matches D-05; new projects and newly-created harness files get consistent ids; legacy `researcher` files remain if already present.

Output: Updated templates in `writeHarnessEntryPoints` for **parallel** researcher files at `.github/agents/leanquill-researcher.agent.md`, `.cursor/skills/leanquill-researcher/SKILL.md`, `.claude/agents/leanquill-researcher.md` (skip if exists); updated `RESEARCH_WORKFLOW_CONTENT` **Harness Setup** section listing these paths; updated `src/harnessChatDraft.ts` research branch to `@leanquill-researcher ` and fallback `/agent:leanquill-researcher`.

**Alias strategy (locked for this phase):** Add **new** `leanquill-researcher` harness files alongside legacy `researcher.*` paths (which stay unchanged and still skipped when present). Do **not** delete or rewrite existing `researcher` files automatically.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-import-claude-desktop-research/15-CONTEXT.md
@src/initialize.ts
@src/harnessChatDraft.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add leanquill-researcher harness trio and update RESEARCH_WORKFLOW_CONTENT Harness Setup</name>
  <files>src/initialize.ts</files>
  <read_first>
    - C:/Dev/LQ/src/initialize.ts (`RESEARCH_WORKFLOW_CONTENT`, `writeHarnessEntryPoints`, existing researcher templates)
    - C:/Dev/LQ/.planning/phases/15-import-claude-desktop-research/15-CONTEXT.md (D-04, D-05, deferred migration)
  </read_first>
  <acceptance_criteria>
    - `rg -n "leanquill-researcher\\.agent\\.md|agents/leanquill-researcher" C:/Dev/LQ/src/initialize.ts` returns ≥1
    - `rg -n "skills/leanquill-researcher" C:/Dev/LQ/src/initialize.ts` returns ≥1
    - `rg -n "leanquill-researcher\\.md" C:/Dev/LQ/src/initialize.ts` returns ≥1
    - `rg -n "name: leanquill-researcher" C:/Dev/LQ/src/initialize.ts` returns ≥3 (one per new harness template)
    - `rg -n "Harness Setup" C:/Dev/LQ/src/initialize.ts` matches a block that includes substring `leanquill-researcher` and substring `research.md`
    - `rg -n "LeanQuill-Researcher" C:/Dev/LQ/src/initialize.ts` returns ≥2 (user-facing string in descriptions)
  </acceptance_criteria>
  <action>
    1. Edit `RESEARCH_WORKFLOW_CONTENT` **Harness Setup** section to document **both** legacy paths (`.github/agents/researcher.agent.md`, `.cursor/skills/researcher/SKILL.md`, `.claude/agents/researcher.md`) **and** new branded paths:
       - `.github/agents/leanquill-researcher.agent.md`
       - `.cursor/skills/leanquill-researcher/SKILL.md`
       - `.claude/agents/leanquill-researcher.md`
       State one sentence: prefer **leanquill-researcher** entry points for new setups; older `researcher` files may exist and are left untouched.

    2. In `writeHarnessEntryPoints`, add three **additional** `entries` for the new filenames (mirror structure of existing researcher content): YAML `name: leanquill-researcher`, description includes exact substring `LeanQuill-Researcher`, body instructs reading `.leanquill/workflows/research.md` and saving under `folders.research`. Cursor adapter: invocation when user mentions `leanquill-researcher` or asks for LeanQuill research.

    3. Do **not** change the skip-if-exists behavior: legacy files and new files each independent.

    4. Keep existing `researcher` template generation as-is (still written when those paths missing) so greenfield init may create both sets; acceptable per deferred migration (executor may optionally stop creating legacy trio on **full** init only — if so, document in SUMMARY; default per this plan: **keep** legacy generation for maximum backward compatibility).
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build</automated>
  </verify>
  <done>initialize.ts builds; new trio present; workflow markdown documents paths; descriptions include LeanQuill-Researcher.</done>
</task>

<task type="auto">
  <name>Task 2: Point chat drafts at leanquill-researcher</name>
  <files>src/harnessChatDraft.ts</files>
  <read_first>
    - C:/Dev/LQ/src/harnessChatDraft.ts
    - C:/Dev/LQ/src/extension.ts (only if needed for imports — prefer no extension change)
  </read_first>
  <acceptance_criteria>
    - `rg -n "@researcher " C:/Dev/LQ/src/harnessChatDraft.ts` returns 0 matches (research branch must not use legacy mention)
    - `rg -n "@leanquill-researcher " C:/Dev/LQ/src/harnessChatDraft.ts` returns ≥1
    - `rg -n "/agent:leanquill-researcher" C:/Dev/LQ/src/harnessChatDraft.ts` returns ≥1
    - `rg -n "/agent:researcher" C:/Dev/LQ/src/harnessChatDraft.ts` returns 0 matches (no legacy Claude fallback id)
  </acceptance_criteria>
  <action>
    1. Update `buildHarnessDraftQuery` research branch for Cursor/Copilot to return exactly `@leanquill-researcher ` (trailing space).

    2. Update `buildHarnessFallbackHint("research")` to use `/agent:leanquill-researcher` and plain text `LeanQuill-Researcher` (exact substring) in the message.

    3. Leave import branches from plan 02 unchanged.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build</automated>
  </verify>
  <done>Research chat draft and fallback reference leanquill-researcher only; import unchanged.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Grep checks confirm no `@researcher ` in `harnessChatDraft.ts`
3. `initialize.ts` contains new harness trio + updated Harness Setup text
</verification>

<success_criteria>
- D-05 satisfied for drafts and new harness files
- No automatic overwrite/migration of existing author harness files
- RESEARCH_WORKFLOW documents both legacy and leanquill-researcher paths
</success_criteria>

<output>
After completion, create `.planning/phases/15-import-claude-desktop-research/15-03-SUMMARY.md`
</output>
