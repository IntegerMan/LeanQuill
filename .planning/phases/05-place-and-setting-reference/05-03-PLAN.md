---
phase: 05-place-and-setting-reference
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/planningPanelHtml.ts
  - src/planningPanel.ts
autonomous: true
requirements:
  - PLACE-01
must_haves:
  truths:
    - "Places tab renders list/detail with region grouping and Uncategorized bucket"
    - "postMessage types place:select|create|updateField|addCustomField|delete|openInEditor are handled"
    - "Detail shows manuscript paths and outline beat ids; debounced saves match character pattern"
  artifacts:
    - path: "src/planningPanelHtml.ts"
      provides: "renderPlacesTab + renderPlanningHtml branch for id===places"
    - path: "src/planningPanel.ts"
      provides: "Place state, handlers, showPlace, flush on dispose"
  key_links:
    - from: "planningPanelHtml script"
      to: "vscode.postMessage"
      via: "type: 'place:…'"
      pattern: "place:"
    - from: "planningPanel._renderPanel"
      to: "listPlaces"
      via: "await listPlaces(this.rootPath, config)"
      pattern: "listPlaces"
---

<objective>
Replace the **Places** stub with a **Characters-tab-parity** webview: region-grouped list (D-06–D-08), inline editing, Add Field, delete with confirmation, Open in Editor, `+ New Place` header; show **`referencedByNameIn`** (manuscript) and **`referencedInBeats`** (outline node ids).

Purpose: PLACE-01 author workflow inside the planning workspace.

**Intentional deviation from strict CHAR-01 UI parity:** Characters detail currently lists only manuscript paths; Places also lists **beat** ids per roadmap / PLACE-01 (05-RESEARCH.md Option C). Characters beat visibility remains unchanged in this phase.

Output: HTML/CSS/JS in `planningPanelHtml.ts`, state + handlers in `planningPanel.ts`, extended `renderPlanningHtml(...)`.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/planningPanelHtml.ts
@src/planningPanel.ts
@src/types.ts
@src/placeStore.ts
</context>

<interfaces>
**`renderPlanningHtml` signature change** — insert after character arguments:

```typescript
export function renderPlanningHtml(
  index: OutlineIndex,
  characters: CharacterProfile[],
  selectedCharacterFileName: string | undefined,
  places: PlaceProfile[],
  selectedPlaceFileName: string | undefined,
  themes: ThemesDocument,
  // ... rest unchanged
): string
```

**Tab branch:** Replace `else if (id === "places") { content = renderStubTab(...) }` with `renderPlacesTab(places, selectedPlaceFileName)`.

**CSS:** Reuse `char-*` class names where possible **or** add `place-*` duplicates mirroring `char-container`, `char-list`, `char-detail`, `char-field-row`, `char-btn-new`, etc. (implementation discretion per 05-CONTEXT "Claude's Discretion"). Region groups mirror `renderCharactersTab` role groups: outer iteration = **region** string; empty/whitespace `region` → group label **`Uncategorized`** (D-07); within group sort by `name` A→Z case-insensitive (D-08).

**Message types (exact strings):** `place:select`, `place:create`, `place:updateField`, `place:addCustomField`, `place:delete`, `place:openInEditor` with payloads mirroring character messages (`fileName`, `field`, `value`, `fieldName`).

**`data-action` attributes in HTML:** same strings as message types for click/update handlers.

**Field keys for `place:updateField`:** `name`, `aliases` (comma-separated in UI like characters), `category`, `region`, `description`, `body`, `custom:{key}`.

**Detail sections:**
- "Appears in manuscript" — `<ul>` of `referencedByNameIn` paths or muted empty line (same prose as character "Appears in manuscript").
- "Referenced in outline (beats)" — `<ul>` of `referencedInBeats` ids (plain text); empty → muted "No outline beat references detected yet."

**Delete confirmation:** `window.confirm` in embedded script identical pattern to character delete (grep `character:delete` handler in script section).

**`PlanningPanelProvider`:** Add `_selectedPlaceFileName`, `_pendingPlace`, `_placeDebounceTimer`, `_placeUpdateLock` mirroring character fields. Implement `_flushPendingPlace`, `_createPlace`, `_updatePlaceField`, `_addPlaceCustomField`, `_deletePlace`, `_openPlaceInEditor`. On dispose, flush pending place after character (extend existing dispose chain). Import `listPlaces`, `createPlace`, `savePlace`, `deletePlace` from `./placeStore`.

**`showPlace(fileName: string)`:** Set `_activeTab = "places"`, `_selectedPlaceFileName = fileName`, reveal panel, `_renderPanel()` — mirror `showCharacter` (lines 104–113).

**`_renderPanel`:** `await listPlaces(this.rootPath, config)`; pass to `renderPlanningHtml`; validate selection exists in list else clear `_selectedPlaceFileName`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Render Places tab HTML, CSS, and webview script handlers</name>
  <read_first>
    - src/planningPanelHtml.ts
    - src/types.ts
  </read_first>
  <files>src/planningPanelHtml.ts</files>
  <action>
    Add `renderPlaceDetail(place: PlaceProfile)` and `renderPlacesTab(places: PlaceProfile[], selectedFileName: string | undefined)` following `renderCharacterDetail` / `renderCharactersTab` structure:

    - Standard fields: Name, Category (with datalist suggestions e.g. `interior`, `city`, `landmark`, `recurring` — optional, same pattern as role datalist), Aliases, Region, Description, custom fields, body textarea.
    - Header button: `data-action="place:create"` label `+ New Place`.
    - Group list by `place.region.trim() || "Uncategorized"`; sort region keys with `"Uncategorized"` last; inside each region sort places by `name` localeCompare sensitivity `base`.
    - Wire embedded script: duplicate `character:*` branches as `place:*` with `fileName` / `field` / `value` / `fieldName` postMessage payloads (find `character:select` block ~1448 in `planningPanelHtml.ts`).

    Update `renderPlanningHtml` signature to accept `places` and `selectedPlaceFileName`; in `TAB_IDS` loop use real places content for `id === "places"`.

    Remove any `renderStubTab("Places")` usage for the places panel — `rg 'renderStubTab\\(\"Places\"\\)' src/` must be zero after this plan (executor verify).

    Import or reference `PlaceProfile` type in TypeScript only — HTML is string templates.
  </action>
  <verify>
    <automated>npm run build:test && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "renderPlacesTab" src/planningPanelHtml.ts` matches.
    - `rg "place:create" src/planningPanelHtml.ts` matches.
    - `rg 'renderStubTab\\(\"Places\"\\)' src/` returns no matches.
    - `rg "places: PlaceProfile" src/planningPanelHtml.ts` OR `rg "places:" src/planningPanelHtml.ts` shows `renderPlanningHtml` parameter.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    Places tab renders from data; stub removed; build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: PlanningPanelProvider state, _renderPanel wiring, message handlers</name>
  <read_first>
    - src/planningPanel.ts
    - src/placeStore.ts
    - src/projectConfig.ts
  </read_first>
  <files>src/planningPanel.ts</files>
  <action>
    Import `PlaceProfile` from `./types` and `listPlaces`, `createPlace`, `savePlace`, `deletePlace` from `./placeStore`.

    Add private fields and mirror character debounce (`_charDebounceTimer` → `_placeDebounceTimer`, 300ms or same as character), `_placeUpdateLock`, `_flushPendingPlace` pattern identical to `_flushPendingCharacter`.

    In `_renderPanel`, after loading characters, `const places = await listPlaces(this.rootPath, config)`; pass `places` and `this._selectedPlaceFileName` into `renderPlanningHtml` in the new argument positions defined in Task 1.

    Extend `_handleMessage` `switch` with cases: `place:select`, `place:create`, `place:updateField`, `place:addCustomField`, `place:delete`, `place:openInEditor` — copy character case bodies substituting place functions and field mapping (`aliases` split/join like character).

    Implement `showPlace(fileName: string)` mirroring `showCharacter` with tab `"places"`.

    On panel dispose async flush, call `await this._flushPendingPlace()` after `_flushPendingCharacter()` (order preserved in existing IIFE).

    `_createPlace`: `vscode.window.showInputBox` for place name (mirror new character UX text); on success `createPlace` then set selection + `await this._renderPanel()`.

    `_openPlaceInEditor`: resolve path with `path.join(rootPath, config.folders.settings, fileName)` and `openTextDocument` — mirror `_openCharacterInEditor` path logic with `folders.settings`.
  </action>
  <verify>
    <automated>npm run build:test && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "case \"place:" src/planningPanel.ts` shows at least 6 case lines for the six message types.
    - `rg "showPlace" src/planningPanel.ts` matches.
    - `rg "listPlaces" src/planningPanel.ts` matches.
    - `rg "_flushPendingPlace" src/planningPanel.ts` matches.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>
    Extension host handles all `place:*` messages; planning panel loads places from disk each render.
  </done>
</task>

</tasks>

<verification>
```bash
npm run build:test && npm test
rg "place:" src/planningPanel.ts | wc -l
rg "renderStubTab" src/planningPanelHtml.ts
```
</verification>

<success_criteria>
- No Places stub in HTML output path.
- Full parity UX for CRUD + grouping except explicit beat list addition documented above.
- Typecheck and tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-place-and-setting-reference/05-03-SUMMARY.md`
</output>
