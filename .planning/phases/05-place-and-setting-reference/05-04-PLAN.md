---
phase: 05-place-and-setting-reference
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/extension.ts
  - package.json
autonomous: true
requirements:
  - PLACE-01
must_haves:
  truths:
    - "SafeFileSystem allows .md writes under resolved folders.settings"
    - "Manuscript open/save runs place manuscript scan then refreshes planning panel"
    - "Outline index watcher runs outline place scan before planning refresh"
    - "Command leanquill.newPlace exists and opens planning workspace on Places tab"
  artifacts:
    - path: "src/extension.ts"
      provides: "allowPath settings, watchers, scan hooks, command registration"
    - path: "package.json"
      provides: "contributes.commands + activationEvents for newPlace"
  key_links:
    - from: "extension.ts scanManuscriptFile"
      to: "scanManuscriptFileForPlaces"
      via: "await after scanManuscriptFileForCharacters"
      pattern: "scanManuscriptFileForPlaces"
    - from: "extension.ts onOutlineChanged"
      to: "scanOutlineIndexForPlaces"
      via: "readOutlineIndex then scan"
      pattern: "scanOutlineIndexForPlaces"
---

<objective>
Wire **folders.settings** into SafeFileSystem, filesystem watchers, manuscript/outline scan hooks, and **`leanquill.newPlace`** command — mirroring characters + threads patterns in `src/extension.ts` and `package.json`.

Purpose: End-to-end PLACE-01 in the running extension: persistence stays within approved paths; scans keep frontmatter linkage fields current; authors discover creation via command palette.

Output: Production wiring only (no new sidebar tree for places — out of scope per 05-RESEARCH.md).
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/extension.ts
@package.json
@src/placeStore.ts
@src/projectConfig.ts
@src/outlineStore.ts
@src/planningPanel.ts
</context>

<interfaces>
From `extension.ts` character wiring (~lines 200–211, 279–286, 330–339, 794–812):

- **allowPath:** After `threads` block, resolve `config.folders.settings` with default `"notes/settings"`, strip trailing slashes, reject if path equals `manuscript` or starts with `manuscript/` (same guard as characters), then `safeFileSystem.allowPath(safeSettingsFolder, ".md")`.

- **Watcher:** `vscode.workspace.createFileSystemWatcher` on `**/${settingsFolderGlob}/**/*.md` using the resolved relative settings folder (mirror `threadsWatcher` lines ~291–293) calling `void planningPanel?.refresh()`.

- **scanManuscriptFile:** Import `scanManuscriptFileForPlaces` from `./placeStore`. After `await scanManuscriptFileForCharacters(...)`, call `await scanManuscriptFileForPlaces(filePath, rootPath, latestConfig, safeFileSystem)` then existing `planningPanel.refresh()` and `characterTreeProvider.refresh()`.

- **Outline watcher:** In `onOutlineChanged` (where `planningPanel.refresh()` is already called), `await readOutlineIndex(rootPath)` (use existing import); if index non-null, `await scanOutlineIndexForPlaces(index, rootPath, latestConfig, safeFileSystem)` before `planningPanel.refresh()`. Use try/catch: log errors without breaking outline refresh.

- **Command `leanquill.newPlace`:** Mirror `leanquill.newCharacter` (input box for name → `createPlace` with `readProjectConfigWithDefaults` + `safeFileSystem`); then `await planningPanel.show()` and `planningPanel.showPlace(profile.fileName)`.

`package.json`:
- Add to `activationEvents`: `"onCommand:leanquill.newPlace"`
- Add to `contributes.commands` after `leanquill.newCharacter`:
  ```json
  {
    "command": "leanquill.newPlace",
    "title": "New Place",
    "category": "LeanQuill",
    "icon": "$(map)"
  }
  ```
- Register in `context.subscriptions` with `vscode.commands.registerCommand("leanquill.newPlace", ...)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SafeFileSystem allowPath and settings folder watcher</name>
  <read_first>
    - src/extension.ts
    - src/projectConfig.ts
  </read_first>
  <files>src/extension.ts</files>
  <action>
    Duplicate the **characters** folder resolution block for `config.folders.settings ?? "notes/settings"`, manuscript-prefix guard, `safeSettingsFolder` variable, and `safeFileSystem.allowPath(safeSettingsFolder, ".md")`.

    Create `settingsWatcher` with `vscode.workspace.createFileSystemWatcher` pattern `**/${safeSettingsFolder}/**/*.md` (mirror threads watcher construction).

    Push `settingsWatcher` onto disposables / subscriptions alongside `threadsWatcher`.

    Ensure `latestConfig` / activation path used when config is re-read still applies allowPath (if extension re-initializes config, follow same pattern as threads).
  </action>
  <verify>
    <automated>npm run build:test && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "folders\\.settings|safeSettingsFolder|settingsWatcher" src/extension.ts` matches at least one line each for settings folder and watcher.
    - `rg "allowPath\\(safeSettingsFolder" src/extension.ts` matches.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    Settings notes path is writable through SafeFileSystem and watched for panel refresh.
  </done>
</task>

<task type="auto">
  <name>Task 2: Manuscript and outline scan hooks + leanquill.newPlace command</name>
  <read_first>
    - src/extension.ts
    - src/placeStore.ts
    - src/outlineStore.ts
    - package.json
  </read_first>
  <files>src/extension.ts, package.json</files>
  <action>
    Import `scanManuscriptFileForPlaces` and `scanOutlineIndexForPlaces` from `./placeStore`.

    In `scanManuscriptFile` async function: after `scanManuscriptFileForCharacters`, await `scanManuscriptFileForPlaces` with same arguments.

    In `onOutlineChanged` callback: after loading outline (use `readOutlineIndex`), if result not null, `await scanOutlineIndexForPlaces(index, rootPath, latestConfig, safeFileSystem)` inside try/catch; then proceed with existing `planningPanel.refresh()` calls.

    Register `leanquill.newPlace` command: copy `newCharacterCommand` structure — prompt for name, `createPlace` from `./placeStore`, error message on failure, success path `planningPanel.show()` + `planningPanel.showPlace(profile.fileName)`.

    Update `package.json` `activationEvents` and `contributes.commands` per `<interfaces>`.
  </action>
  <verify>
    <automated>npm run build:test && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "scanManuscriptFileForPlaces" src/extension.ts` matches.
    - `rg "scanOutlineIndexForPlaces" src/extension.ts` matches.
    - `rg "leanquill\\.newPlace" package.json` matches in both activationEvents and commands.
    - `rg "registerCommand\\(\"leanquill.newPlace\"" src/extension.ts` matches.
  </acceptance_criteria>
  <done>
    Scans run on manuscript and outline events; command registered and discoverable.
  </done>
</task>

</tasks>

<verification>
```bash
npm run build:test && npm test
rg "newPlace|scanOutlineIndexForPlaces|scanManuscriptFileForPlaces" src/extension.ts
grep "leanquill.newPlace" package.json
```
</verification>

<success_criteria>
- Extension compiles; tests pass.
- New command appears in package manifest.
- Place scans integrate without breaking outline or character flows.
</success_criteria>

<output>
After completion, create `.planning/phases/05-place-and-setting-reference/05-04-SUMMARY.md`
</output>
