---
phase: 05-place-and-setting-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/projectConfig.ts
  - src/placeStore.ts
  - test/projectConfig.test.ts
  - test/placeStore.test.ts
autonomous: true
requirements:
  - PLACE-01
must_haves:
  truths:
    - "PlaceProfile exists with D-01 default fields plus linkage arrays for chapters and beats"
    - "project.yaml folders.settings resolves to notes/settings/ by default"
    - "placeStore lists, creates, saves, and deletes .md profiles under folders.settings"
    - "referencedByNameIn and referencedInBeats round-trip through frontmatter"
  artifacts:
    - path: "src/types.ts"
      provides: "PlaceProfile interface"
      exports: ["PlaceProfile"]
    - path: "src/projectConfig.ts"
      provides: "ProjectConfig.folders.settings"
      contains: "settings"
    - path: "src/placeStore.ts"
      provides: "Place CRUD without manuscript/outline scanning"
      exports: ["slugifyPlaceName", "parsePlaceFile", "serializePlaceFile", "listPlaces", "createPlace", "savePlace", "deletePlace"]
    - path: "test/placeStore.test.ts"
      provides: "Unit tests for place store and parsing"
  key_links:
    - from: "src/placeStore.ts"
      to: "src/projectConfig.ts"
      via: "config.folders.settings path resolution"
      pattern: "folders\\.settings"
    - from: "src/placeStore.ts"
      to: "src/safeFileSystem.ts"
      via: "SafeFileSystem.writeFile / mkdir / canWrite"
      pattern: "safeFs\\."
---

<objective>
Add `PlaceProfile`, `folders.settings` in project config, and `placeStore` CRUD (parse/serialize/list/create/save/delete) mirroring `characterStore.ts`, including frontmatter arrays `referencedByNameIn` and `referencedInBeats` (populated by Plan 02 scans).

Purpose: Wave-2 UI and scanning plans import a stable data layer. Config key **`settings`** maps to author-facing **Places** notes at default `notes/settings/` per **05-RESEARCH.md** and `Imported/data-contracts/project-config-schema.md` (D-01–D-05: name, aliases, category, region, description, custom fields, body).

Output: Typed place profiles, parsed `folders.settings`, and tested store module.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-place-and-setting-reference/05-CONTEXT.md
@.planning/phases/05-place-and-setting-reference/05-RESEARCH.md
@src/types.ts
@src/projectConfig.ts
@src/characterStore.ts
@src/safeFileSystem.ts
@test/characterStore.test.ts
@test/projectConfig.test.ts
</context>

<interfaces>
From `src/projectConfig.ts` (extend — mirror `threads` parsing):
```typescript
export interface ProjectConfig {
  schemaVersion: string;
  folders: {
    research: string;
    characters: string;
    threads: string;
    settings: string; // default notes/settings/
  };
}
```

New in `src/types.ts` (append after `CharacterProfile`):
```typescript
export interface PlaceProfile {
  fileName: string;
  name: string;
  aliases: string[];
  category: string;
  region: string;
  description: string;
  referencedByNameIn: string[]; // manuscript paths relative to workspace root, forward slashes
  referencedInBeats: string[]; // outline node ids (Plan 02 writers)
  customFields: Record<string, string>;
  body: string;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PlaceProfile and ProjectConfig.folders.settings</name>
  <read_first>
    - src/types.ts
    - src/projectConfig.ts
    - test/projectConfig.test.ts
  </read_first>
  <files>src/types.ts, src/projectConfig.ts, test/projectConfig.test.ts</files>
  <behavior>
    - `parseProjectConfig` YAML with `settings: notes/settings/` inside `folders:` yields `folders.settings === "notes/settings/"`
    - `parseProjectConfig` missing `settings:` yields default `"notes/settings/"`
    - `parseProjectConfig` with `settings: custom/places/` yields that trimmed value
  </behavior>
  <action>
    Append `PlaceProfile` to `src/types.ts` exactly with fields: `fileName`, `name`, `aliases`, `category`, `region`, `description`, `referencedByNameIn`, `referencedInBeats`, `customFields`, `body` (per D-01; `referencedInBeats` for Option C in 05-RESEARCH.md).

    Extend `ProjectConfig.folders` with `settings: string`. Set `DEFAULT_PROJECT_CONFIG.folders.settings` to `"notes/settings/"`.

    In `parseProjectConfig`, inside the existing `folders:` block loop, add parsing for `^\s+settings:\s*["']?(.+?)["']?\s*$` into variable `settings` defaulting to `"notes/settings/"`, and return it in `folders` alongside `research`, `characters`, `threads`.

    Add three tests in `test/projectConfig.test.ts` mirroring the existing `threads`/`characters` tests: explicit `settings:` line, omitted key → default, custom path value.
  </action>
  <verify>
    <automated>npm run build:test && npm test -- --test-name-pattern="parseProjectConfig"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export interface PlaceProfile" src/types.ts` returns a match.
    - `rg "settings: string" src/projectConfig.ts` returns a match.
    - `rg "settingsMatch|settings:" src/projectConfig.ts` shows loop extraction for `settings:` inside `folders:`.
    - `rg "notes/settings" src/projectConfig.ts` matches `DEFAULT_PROJECT_CONFIG`.
    - `npm test -- --test-name-pattern="parseProjectConfig"` exits 0.
  </acceptance_criteria>
  <done>
    TypeScript builds; `PlaceProfile` exported; `folders.settings` parsed with default `notes/settings/`; new projectConfig tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement placeStore.ts CRUD (no scanning functions yet)</name>
  <read_first>
    - src/placeStore.ts (create if missing after Task 1)
    - src/characterStore.ts
    - src/types.ts
    - src/projectConfig.ts
    - src/safeFileSystem.ts
  </read_first>
  <files>src/placeStore.ts</files>
  <behavior>
    - `slugifyPlaceName("The Old Mill")` → `"the-old-mill.md"`
    - `parsePlaceFile` reads `name`, `aliases`, `category`, `region`, `description`, `referencedByNameIn`, `referencedInBeats`, unknown keys → `customFields`, body after `---`
    - `serializePlaceFile` emits `aliases: []` / list, `referencedByNameIn: []` / list, `referencedInBeats: []` / list, forward slashes in paths
    - Round-trip parse(serialize(p)) preserves logical equality for standard fields
  </behavior>
  <action>
    Create `src/placeStore.ts` by cloning the structure of `src/characterStore.ts`:

    - `settingsDir(rootPath, config)` using `config.folders.settings.replace(/\/+$/, "").split("/")` and `path.join` (same pattern as `charsDir`).
    - `slugifyPlaceName(name: string)` — same algorithm as `slugifyCharacterName` but default slug basename `"place"` when empty.
    - `parsePlaceFile` / `serializePlaceFile` — mirror the character frontmatter state machine; known keys: `name`, `aliases` (list), `category`, `region`, `description` (scalar or block scalar like character `description`), `referencedByNameIn` (list), `referencedInBeats` (list); all other keys → `customFields`.
    - `listPlaces`, `createPlace`, `savePlace`, `deletePlace` — mirror character equivalents with `PlaceProfile` template: `category: ""`, `region: ""`, `referencedByNameIn: []`, `referencedInBeats: []`.

    Do **not** add `scanManuscriptFileForPlaces` or `scanOutlineIndexForPlaces` in this task (Plan 02).

    Reuse `escapeRegex` as private helper if needed only by scan (optional); omit until Plan 02 if unused.
  </action>
  <verify>
    <automated>npm run build:test && npm test -- --test-name-pattern="placeStore|place"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export (async )?function (listPlaces|createPlace|savePlace|deletePlace|parsePlaceFile|serializePlaceFile|slugifyPlaceName)" src/placeStore.ts` shows all seven exports.
    - `rg "scanManuscriptFileForPlaces|scanOutlineIndexForPlaces" src/placeStore.ts` returns no matches.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    `placeStore.ts` compiles and exposes CRUD + parse/serialize + slugify; no scan functions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add test/placeStore.test.ts filesystem and pure function coverage</name>
  <read_first>
    - test/characterStore.test.ts
    - src/placeStore.ts
    - src/safeFileSystem.ts
  </read_first>
  <files>test/placeStore.test.ts</files>
  <behavior>
    - Pure: slugify, parse/serialize round-trip, empty frontmatter defaults
    - I/O with `withTempDir`: list empty → []; createPlace writes under `notes/settings/`; duplicate name → `-2.md`; savePlace overwrites; deletePlace removes file; `deletePlace` throws when `safeFs.canWrite` false for path
  </behavior>
  <action>
    Create `test/placeStore.test.ts` mirroring `test/characterStore.test.ts`: same `withTempDir` / `SafeFileSystem` setup, but `config.folders` must include `research`, `characters`, `threads`, `settings` matching `DEFAULT_PROJECT_CONFIG` plus any overrides.

    Mock `ProjectConfig` for I/O tests must use `folders.settings: "notes/settings/"` (or temp-relative equivalent).

    Minimum 12 assertions/tests covering slugify, parse edge cases, serialize lists for `referencedInBeats`, and full CRUD.
  </action>
  <verify>
    <automated>npm run build:test && npm test -- --test-name-pattern="placeStore"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/placeStore.test.ts` is true.
    - `rg "listPlaces|createPlace" test/placeStore.test.ts` matches.
    - `npm test -- --test-name-pattern="placeStore"` exits 0.
  </acceptance_criteria>
  <done>
    `placeStore` tests green; CRUD verified on temp filesystem with SafeFileSystem allowPath for settings folder.
  </done>
</task>

</tasks>

<verification>
```bash
npm run build:test && npm test
rg "PlaceProfile" src/types.ts
rg "settings" src/projectConfig.ts | head -20
rg "export function" src/placeStore.ts
```
</verification>

<success_criteria>
- `PlaceProfile` and `folders.settings` are in the codebase with defaults.
- `placeStore` exports CRUD and serialization functions (no scanners).
- `test/placeStore.test.ts` passes.
- Full `npm test` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-place-and-setting-reference/05-01-SUMMARY.md`
</output>
