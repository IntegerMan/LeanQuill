---
phase: 12-standardized-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/projectConfig.ts
  - src/safeFileSystem.ts
  - src/initialize.ts
  - test/projectConfig.test.ts
  - test/safeFileSystem.test.ts
autonomous: true
requirements: [RES-01, RES-04]

must_haves:
  truths:
    - "parseProjectConfig extracts schemaVersion and folders.research from project.yaml text"
    - "readProjectConfig returns null when project.yaml is missing"
    - "SafeFileSystem.allowPath enables writing .md files under a dynamic path prefix"
    - "SafeFileSystem blocks non-.md files in an allowed research path"
    - "SafeFileSystem.mkdir works for directories under an allowed path (no ext filter on directories)"
    - "New projects initialize with schema_version 2 and folders.research research/leanquill/"
    - "Existing v1 project.yaml is migrated to v2 with updated research default on activation"
    - "Canonical workflow file is created at .leanquill/workflows/research.md during init"
    - "Copilot entry point is created at .github/agents/researcher.agent.md during init"
    - "Cursor entry point is created at .cursor/skills/researcher/SKILL.md during init"
    - "Claude entry point is created at .claude/agents/researcher.md during init"
  artifacts:
    - path: "src/projectConfig.ts"
      provides: "Project config parser — reads project.yaml and returns typed config"
      exports: ["parseProjectConfig", "readProjectConfig", "ProjectConfig"]
    - path: "src/safeFileSystem.ts"
      provides: "Extended write boundary with allowPath method for dynamic folder permissions"
      exports: ["SafeFileSystem"]
    - path: "src/initialize.ts"
      provides: "Schema v2 defaults, v1-to-v2 migration, canonical workflow + harness entry point generation"
    - path: "test/projectConfig.test.ts"
      provides: "Unit tests for config parsing edge cases"
    - path: "test/safeFileSystem.test.ts"
      provides: "Unit tests for allowPath with extension filtering"
  key_links:
    - from: "src/projectConfig.ts"
      to: ".leanquill/project.yaml"
      via: "fs.readFile + line-based regex extraction"
      pattern: "readFile.*project\\.yaml"
    - from: "src/safeFileSystem.ts"
      to: "configured research folder"
      via: "allowPath() stores prefix + extFilter, canWrite() checks against them"
      pattern: "additionalAllowed|allowPath"
    - from: "src/initialize.ts"
      to: ".leanquill/workflows/research.md"
      via: "safeFileSystem.writeFile during init flow"
      pattern: "workflows/research"
    - from: "src/initialize.ts"
      to: ".github/agents/researcher.agent.md"
      via: "raw fs.writeFile (outside SafeFileSystem boundary — harness config files)"
      pattern: "agents/researcher"
    - from: "src/initialize.ts"
      to: ".cursor/skills/researcher/SKILL.md"
      via: "raw fs.writeFile (outside SafeFileSystem boundary — harness config files)"
      pattern: "skills/researcher"
    - from: "src/initialize.ts"
      to: ".claude/agents/researcher.md"
      via: "raw fs.writeFile (outside SafeFileSystem boundary — harness config files)"
      pattern: "agents/researcher"
---

<objective>
Establish the config, safety, and initialization foundation for the research workflow feature, including all three AI harness entry points.

Purpose: Phase 12 requires reading `project.yaml` at runtime (not done today), extending SafeFileSystem to allow research folder writes, updating the init/migration flow for schema v2, and generating the canonical workflow definition plus Copilot/Cursor/Claude entry points. This plan creates all foundational pieces.

Output: `src/projectConfig.ts` (config reader), extended `src/safeFileSystem.ts`, updated `src/initialize.ts` with schema v2 + canonical workflow + three harness entry points, plus unit tests for all new behavior.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-add-the-ability-to-run-research-in-standardized-ways-and-collect-research-results-in-a-dedicated-research-folder-next-to-the-manuscript/12-CONTEXT.md
@.planning/phases/12-add-the-ability-to-run-research-in-standardized-ways-and-collect-research-results-in-a-dedicated-research-folder-next-to-the-manuscript/12-RESEARCH.md

@src/safeFileSystem.ts
@src/initialize.ts
@src/types.ts
@test/safeFileSystem.test.ts

<interfaces>
<!-- Current SafeFileSystem API (src/safeFileSystem.ts, 44 lines) -->
```typescript
export class SafeFileSystem {
  constructor(private readonly rootPath: string) {}
  public canWrite(targetPath: string): boolean;
  public async writeFile(targetPath: string, content: string): Promise<void>;
  public async mkdir(targetPath: string): Promise<void>;
}
```

<!-- Current initialize.ts renderProjectYaml (lines 20-50) -->
```typescript
function renderProjectYaml(input: InitInput): string {
  // Generates YAML string with:
  //   schema_version: "1"
  //   folders.research: notes/research/
  //   (and other fields)
}
```

<!-- InitInput type (src/types.ts) -->
```typescript
export interface InitInput {
  projectId: string;
  workingTitle: string;
  genre: string[];
}
```

<!-- Harness entry point formats (from existing repo files) -->

<!-- Copilot: .github/agents/{name}.agent.md -->
```yaml
--- 
name: researcher
description: Runs the LeanQuill research workflow
tools: ['read', 'search', 'web']
--- 
```

<!-- Cursor: .cursor/skills/{name}/SKILL.md -->
```yaml
--- 
name: researcher
description: "Runs the LeanQuill research workflow"
--- 
<cursor_skill_adapter>
## A. Skill Invocation
- This skill is invoked when the user mentions `researcher` or asks to research something.
...
```

<!-- Claude: .claude/agents/{name}.md -->
```yaml
--- 
name: researcher
description: Runs the LeanQuill research workflow
tools: Read, Write, Edit, Bash, Grep, Glob
--- 
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create project config reader</name>
  <files>src/projectConfig.ts, test/projectConfig.test.ts</files>
  <behavior>
    - parseProjectConfig('schema_version: "1"\nfolders:\n  research: notes/research/') returns { schemaVersion: "1", folders: { research: "notes/research/" } }
    - parseProjectConfig('schema_version: "2"\nfolders:\n  research: research/leanquill/') returns { schemaVersion: "2", folders: { research: "research/leanquill/" } }
    - parseProjectConfig with missing folders.research returns default "research/leanquill/"
    - parseProjectConfig with missing schema_version returns default "1"
    - parseProjectConfig with empty string returns all defaults
    - readProjectConfig returns parsed config when .leanquill/project.yaml exists
    - readProjectConfig returns null when .leanquill/project.yaml does not exist
  </behavior>
  <action>
    Create `src/projectConfig.ts` with:

    1. `ProjectConfig` interface: `{ schemaVersion: string; folders: { research: string } }`
    2. `parseProjectConfig(content: string): ProjectConfig` — Line-based regex parser for project.yaml. Extract `schema_version` via `/^schema_version:\s*["']?(.+?)["']?\s*$/m` and `folders.research` via detecting the `folders:` block then matching `research:\s*(.+)`. Default `schemaVersion` to `"1"`, default `folders.research` to `"research/leanquill/"`. No YAML library — the format is controlled by LeanQuill's own `renderProjectYaml`.
    3. `readProjectConfig(rootPath: string): Promise<ProjectConfig | null>` — Reads `path.join(rootPath, ".leanquill", "project.yaml")` via `fs.readFile`. Returns `null` if file doesn't exist (catch ENOENT). Otherwise delegates to `parseProjectConfig`.

    Create `test/projectConfig.test.ts` following the `withTempDir` pattern from `test/safeFileSystem.test.ts`. Test all behaviors listed above. For readProjectConfig tests, create a temp dir with/without `.leanquill/project.yaml`.
  </action>
  <verify>
    <automated>npm run build:test && npm run test</automated>
  </verify>
  <done>parseProjectConfig correctly extracts schemaVersion and folders.research from project.yaml text with sensible defaults. readProjectConfig handles missing file gracefully. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SafeFileSystem with dynamic allowed paths</name>
  <files>src/safeFileSystem.ts, test/safeFileSystem.test.ts</files>
  <behavior>
    - allowPath("research/leanquill", ".md") then canWrite("research/leanquill/topic.md") returns true
    - allowPath("research/leanquill", ".md") then canWrite("research/leanquill/topic.txt") returns false
    - allowPath("research/leanquill", ".md") then canWrite("research/leanquill/sub/deep.md") returns true
    - allowPath("research/leanquill") without extFilter then canWrite("research/leanquill/anything.txt") returns true
    - mkdir on "research/leanquill" after allowPath("research/leanquill", ".md") succeeds (directory paths have no ext)
    - writeFile to "research/leanquill/test.md" after allowPath succeeds and creates file
    - writeFile to "research/leanquill/test.txt" after allowPath with ".md" filter throws
    - Existing .leanquill and manuscript/Book.txt behavior is unchanged
    - canWrite for paths outside all allowed areas still returns false
  </behavior>
  <action>
    Modify `src/safeFileSystem.ts`:

    1. Add private field: `private readonly additionalAllowed: Array<{ prefix: string; extFilter?: string }> = [];`
    2. Add public method: `public allowPath(prefix: string, extFilter?: string): void` — Pushes `{ prefix, extFilter }` to the array. The prefix should be stored as-is (forward slashes); normalization happens in `canWrite`.
    3. Update `canWrite`: After the existing `.leanquill` and `manuscript/Book.txt` checks, iterate `additionalAllowed`. For each entry, normalize the prefix to OS separators (`prefix.split("/").join(path.sep)`). Check if `rel === normalizedPrefix` or `rel.startsWith(normalizedPrefix + path.sep)`. If matched and `extFilter` is set, check `path.extname(rel)` — if ext is empty string (directory path) OR matches `extFilter`, return true; otherwise continue. If matched and no `extFilter`, return true.

    Update `test/safeFileSystem.test.ts`: Add test cases for all behaviors listed above. Use the existing `withTempDir` pattern. Test both `canWrite` directly and the `writeFile`/`mkdir` integration.
  </action>
  <verify>
    <automated>npm run build:test && npm run test</automated>
  </verify>
  <done>SafeFileSystem.allowPath enables controlled write access to dynamic paths with optional extension filtering. Directory operations work without extension restriction. All existing tests still pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Schema v2 migration, canonical workflow, and three harness entry points</name>
  <files>src/initialize.ts</files>
  <action>
    Modify `src/initialize.ts`:

    1. **Update `renderProjectYaml`**: Change `schema_version: "1"` to `schema_version: "2"`. Change `research: notes/research/` to `research: research/leanquill/`. Per D-01, D-02, D-03.

    2. **Add `migrateProjectYaml(rootPath: string, safeFs: SafeFileSystem): Promise<boolean>` function** (exported):
       - Read `.leanquill/project.yaml` as text
       - If content contains `schema_version: "1"` (or `schema_version: '1'`):
         - Replace `schema_version: "1"` with `schema_version: "2"`
         - Replace `research: notes/research/` with `research: research/leanquill/` (only if the old default is present — if user customized, leave it)
         - Write back via `safeFs.writeFile`
         - Return `true` (migrated)
       - Otherwise return `false` (no migration needed)

    3. **Add canonical workflow generation to `initializeProject` function**:
       After writing project.yaml, create `.leanquill/workflows/` directory and write `.leanquill/workflows/research.md` via `safeFs.writeFile`. Template content (stored as a const string) includes:
       - YAML frontmatter: `name: LeanQuill Research Workflow`, `version: 1`
       - Research process steps per D-11 through D-16:
         1. Read project context (project.yaml, outline-index.json, relevant manuscript)
         2. Understand the query in the context of the author's book project
         3. Ask clarifying questions when the query is ambiguous
         4. Break into 1-5 core sub-topics
         5. Research each sub-topic using web search (warn if unavailable per D-14)
         6. Produce one markdown result file
       - Result File Format section per D-17, D-19, D-20:
         - Frontmatter: name, query, created, tags, sources
         - Body: Summary, Sub-topics (1-5), Sources, Open Questions, Project Relevancy
         - File naming: `{topic-slug}-{YYYY-MM-DD}.md`
         - Save to: `{folders.research}` (resolved from project.yaml)
       - Harness Setup section documenting all three entry point locations

    4. **Generate three harness entry points** in `initializeProject`, using raw `fs.writeFile` (NOT SafeFileSystem — these directories are outside the safety boundary):

       **Copilot** — `.github/agents/researcher.agent.md`:
       ```markdown
       ---
       name: researcher
       description: "Run the LeanQuill research workflow — investigates a topic with web search and produces a structured research document"
       tools: ['read', 'search', 'web']
       ---

       You are the LeanQuill research agent. Follow the canonical workflow defined in `.leanquill/workflows/research.md`.

       Read that file first, then execute the research process it describes.
       ```

       **Cursor** — `.cursor/skills/researcher/SKILL.md`:
       ```markdown
       ---
       name: researcher
       description: "Run the LeanQuill research workflow — investigates a topic with web search and produces a structured research document"
       ---

       <cursor_skill_adapter>
       ## A. Skill Invocation
       - This skill is invoked when the user mentions `researcher` or asks to research a topic for their book.
       - Treat all user text after the skill mention as the research query.

       ## B. Execution
       Read the canonical workflow at `.leanquill/workflows/research.md` and follow its process.
       </cursor_skill_adapter>
       ```

       **Claude** — `.claude/agents/researcher.md`:
       ```markdown
       ---
       name: researcher
       description: "Run the LeanQuill research workflow — investigates a topic with web search and produces a structured research document"
       tools: Read, Write, Edit, Bash, Grep, Glob
       ---

       You are the LeanQuill research agent. Follow the canonical workflow defined in `.leanquill/workflows/research.md`.

       Read that file first, then execute the research process it describes.
       ```

       For each entry point, use `fs.mkdir(dir, { recursive: true })` to ensure the parent directory exists, then `fs.writeFile`. Do NOT overwrite existing files — check with `fs.stat` first and skip if the file already exists (user may have customized).

    5. **Export `migrateProjectYaml`** so `extension.ts` can call it on activation (wired in Plan 02).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>renderProjectYaml outputs schema v2 with research/leanquill/ default. migrateProjectYaml can detect v1 and upgrade to v2. initializeProject creates canonical workflow at .leanquill/workflows/research.md AND three harness entry points (.github/agents/researcher.agent.md, .cursor/skills/researcher/SKILL.md, .claude/agents/researcher.md). Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no TypeScript errors
2. `npm run build:test && npm run test` — all existing + new tests pass
3. New `parseProjectConfig` handles v1 and v2 formats correctly
4. SafeFileSystem allows .md writes to configured research path, blocks .txt
5. `renderProjectYaml` now outputs schema_version "2" and research: research/leanquill/
6. Initialization generates canonical workflow + all three harness entry points
</verification>

<success_criteria>
- `src/projectConfig.ts` exports `parseProjectConfig`, `readProjectConfig`, `ProjectConfig`
- `src/safeFileSystem.ts` has `allowPath(prefix, extFilter?)` method that gates writes by extension
- `src/initialize.ts` generates schema v2 project.yaml, `.leanquill/workflows/research.md`, and three harness entry points
- `migrateProjectYaml` exported for activation-time v1→v2 migration
- All unit tests pass including new projectConfig and safeFileSystem test cases
</success_criteria>

<output>
After completion, create `.planning/phases/12-add-the-ability-to-run-research-in-standardized-ways-and-collect-research-results-in-a-dedicated-research-folder-next-to-the-manuscript/12-01-SUMMARY.md`
</output>
