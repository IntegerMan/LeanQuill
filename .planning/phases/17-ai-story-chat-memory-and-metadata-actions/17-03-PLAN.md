---
phase: 17-ai-story-chat-memory-and-metadata-actions
plan: 03
type: execute
wave: 3
depends_on:
  - "17-02"
files_modified:
  - src/metadataActionContract.ts
  - src/metadataActionLog.ts
  - src/metadataActionApplier.ts
  - test/metadataActionContract.test.ts
  - test/metadataActionLog.test.ts
  - test/metadataActionApplier.test.ts
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - "Metadata actions are typed records with operation, target path, field path, old value, new value, rationale, risk, and source chat id."
    - "Actions targeting manuscript prose, path traversal, unknown operations, stale old values, missing required approval, or schema-invalid payloads are blocked before write."
    - "Theme metadata, research associations, and chat-log provenance updates are explicit supported action targets per D-09."
    - "Applied, rejected, and blocked action outcomes are appended to a compact durable log under `.leanquill/`."
  artifacts:
    - path: "src/metadataActionContract.ts"
      provides: "MetadataAction types and validation"
    - path: "src/metadataActionLog.ts"
      provides: "JSONL action log append/read helpers"
    - path: "src/metadataActionApplier.ts"
      provides: "Extension-side action application through stores and SafeFileSystem"
  key_links:
    - from: "src/metadataActionApplier.ts"
      to: "src/safeFileSystem.ts"
      via: "SafeFileSystem.canWrite/writeFile before mutations"
      pattern: "safeFs"
    - from: "src/metadataActionApplier.ts"
      to: "src/metadataActionLog.ts"
      via: "appendMetadataActionLogEntry for applied/blocked/rejected"
      pattern: "appendMetadataActionLogEntry"
---

<objective>
Implement the standardized metadata action contract, action log, and deterministic extension-side applier.

Purpose: D-09 through D-16 require portable structured action proposals, validation/application by extension code, explicit approval for higher-risk updates, compact action logging, and hard safety blocks.
Output: Contract, log, applier modules plus unit tests.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-CONTEXT.md
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-RESEARCH.md
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-UI-SPEC.md
@src/safeFileSystem.ts
@src/openQuestionStore.ts
@src/storyMemoryStore.ts
@src/characterStore.ts
@src/placeStore.ts
@src/threadStore.ts
@src/themesStore.ts
@src/projectConfig.ts
</context>

<interfaces>
Create this public contract in `src/metadataActionContract.ts`:

```typescript
export type MetadataActionOperation = "set" | "append" | "removeFromList" | "createMemory" | "supersedeMemory" | "createIssue" | "updateIssueStatus" | "updateThemeMetadata" | "updateResearchAssociation" | "updateChatLogProvenance";
export type MetadataActionRisk = "low" | "requiresApproval";
export type MetadataActionApprovalMethod = "native-tool-confirmation" | "chat-text-confirmation" | "extension-command";
export interface MetadataAction {
  schemaVersion: "1";
  actionId: string;
  sourceChatId: string;
  operation: MetadataActionOperation;
  targetPath: string;
  fieldPath: string[];
  oldValue?: unknown;
  newValue?: unknown;
  rationale: string;
  risk: MetadataActionRisk;
  authorApproval?: {
    approvedAt: string;
    method: MetadataActionApprovalMethod;
    transcriptExcerpt?: string;
  };
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Metadata action contract validation</name>
  <files>src/metadataActionContract.ts, test/metadataActionContract.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/metadataActionContract.ts (create if absent)
    - C:/Dev/LQ/src/projectConfig.ts
    - C:/Dev/LQ/src/safeFileSystem.ts
    - C:/Dev/LQ/test/safeFileSystem.test.ts
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `test -f C:/Dev/LQ/src/metadataActionContract.ts` exits 0
    - `rg -n "export interface MetadataAction" C:/Dev/LQ/src/metadataActionContract.ts` returns >=1
    - `rg -n "validateMetadataAction" C:/Dev/LQ/src/metadataActionContract.ts C:/Dev/LQ/test/metadataActionContract.test.ts` returns >=2
    - `rg -n "manuscript/|path traversal|Unknown operation|requiresApproval|authorApproval|native-tool-confirmation|chat-text-confirmation|extension-command" C:/Dev/LQ/src/metadataActionContract.ts C:/Dev/LQ/test/metadataActionContract.test.ts` returns >=8
    - `rg -n "updateThemeMetadata|updateResearchAssociation|updateChatLogProvenance|Only \\.leanquill memory/chat-log actions may be low risk" C:/Dev/LQ/src/metadataActionContract.ts C:/Dev/LQ/test/metadataActionContract.test.ts` returns >=5
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    1. Create `src/metadataActionContract.ts` exporting the interfaces in `<interfaces>`.
    2. Add `export interface MetadataActionValidationResult { ok: boolean; action?: MetadataAction; errors: string[]; blockedReasons: string[]; }`.
    3. Implement `validateMetadataAction(raw: unknown, options: { configuredWritableRoots: string[] }): MetadataActionValidationResult`.
       - Required fields: `schemaVersion`, `actionId`, `sourceChatId`, `operation`, `targetPath`, `fieldPath`, `rationale`, `risk`.
       - `schemaVersion` must equal `"1"`.
       - `operation` must be one of `"set"`, `"append"`, `"removeFromList"`, `"createMemory"`, `"supersedeMemory"`, `"createIssue"`, `"updateIssueStatus"`, `"updateThemeMetadata"`, `"updateResearchAssociation"`, `"updateChatLogProvenance"`.
       - `risk` must be `"low"` or `"requiresApproval"`.
       - If `risk === "requiresApproval"`, require `authorApproval.approvedAt` and `authorApproval.method`.
       - `authorApproval.method` must be one of `"native-tool-confirmation"`, `"chat-text-confirmation"`, `"extension-command"`.
       - Enforce D-13 exactly: only low-risk `.leanquill/` memory/chat-log/action-log actions may omit approval. Specifically, `risk: "low"` is valid without `authorApproval` only when `targetPath` starts with `.leanquill/memory/`, starts with `.leanquill/chats/`, or equals `.leanquill/metadata-actions.jsonl`.
       - Any action targeting configured story-note/entity/research metadata roots, including `notes/characters/`, `notes/settings/`, `notes/threads/`, theme metadata paths, or `research/leanquill/`, must use `risk: "requiresApproval"` and include `authorApproval.approvedAt` plus `authorApproval.method`; do not allow a caller to bypass approval by declaring `risk: "low"`.
       - Block path traversal when `targetPath` includes `..`, starts with `/`, starts with `\\`, or matches a Windows drive prefix like `C:`.
       - Block any `targetPath` equal to `manuscript/Book.txt`, starting with `manuscript/`, or containing `/manuscript/`.
       - Allow target roots only when `targetPath` starts with `.leanquill/` or one of `configuredWritableRoots` normalized with a trailing slash. Configured roots will include character, place/settings, thread, theme, and research roots from `ProjectConfig`.
    4. Tests must include valid low-risk `.leanquill/memory/x.md`, valid low-risk `.leanquill/chats/session.md`, valid requiresApproval `notes/characters/hero.md`, valid requiresApproval theme metadata, valid requiresApproval research association, valid low-risk chat-log provenance update, and blocked cases for manuscript path, `../escape.md`, `C:/temp/x.md`, unknown operation, invalid risk, missing approval, unconfigured `notes/private/x.md`, and `risk: "low"` on a configured story-note/entity/research metadata path.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Action contract validation is deterministic and blocks unsafe or schema-invalid proposals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compact metadata action log</name>
  <files>src/metadataActionLog.ts, test/metadataActionLog.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/metadataActionLog.ts (create if absent)
    - C:/Dev/LQ/src/metadataActionContract.ts
    - C:/Dev/LQ/src/safeFileSystem.ts
    - C:/Dev/LQ/test/safeFileSystem.test.ts
  </read_first>
  <acceptance_criteria>
    - `test -f C:/Dev/LQ/src/metadataActionLog.ts` exits 0
    - `rg -n "LEANQUILL_METADATA_ACTION_LOG|metadata-actions\\.jsonl" C:/Dev/LQ/src/metadataActionLog.ts C:/Dev/LQ/test/metadataActionLog.test.ts` returns >=2
    - `rg -n "proposed|applied|rejected|blocked" C:/Dev/LQ/src/metadataActionLog.ts C:/Dev/LQ/test/metadataActionLog.test.ts` returns >=4
    - `rg -n "appendMetadataActionLogEntry|readMetadataActionLog" C:/Dev/LQ/src/metadataActionLog.ts C:/Dev/LQ/test/metadataActionLog.test.ts` returns >=4
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    1. Create `src/metadataActionLog.ts` exporting `export const LEANQUILL_METADATA_ACTION_LOG = ".leanquill/metadata-actions.jsonl";`.
    2. Export `MetadataActionLogStatus = "proposed" | "applied" | "rejected" | "blocked"` and `MetadataActionLogEntry`.
       - Fields: `timestamp`, `actionId`, `sourceChatId`, `operation`, `targetPath`, `fieldPath`, `status`, `rationale`, `message`.
    3. Implement `appendMetadataActionLogEntry(rootPath: string, safeFs: SafeFileSystem, entry: MetadataActionLogEntry): Promise<void>`.
       - Write one JSON object per line.
       - Use `safeFs.canWrite(logPath, true)` before appending.
       - Use `fs.mkdir(path.dirname(logPath), { recursive: true })` and `fs.appendFile(logPath, JSON.stringify(entry) + "\n", "utf8")`.
    4. Implement `readMetadataActionLog(rootPath: string): Promise<MetadataActionLogEntry[]>`.
       - Missing file returns `[]`.
       - Invalid JSON lines are skipped.
    5. Tests must append applied and blocked entries, read them back, assert the target file path is `.leanquill/metadata-actions.jsonl`, and assert missing log returns `[]`.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Action outcomes can be durably appended and read from `.leanquill/metadata-actions.jsonl`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extension-side metadata action applier</name>
  <files>src/metadataActionApplier.ts, test/metadataActionApplier.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/metadataActionApplier.ts (create if absent)
    - C:/Dev/LQ/src/metadataActionContract.ts
    - C:/Dev/LQ/src/metadataActionLog.ts
    - C:/Dev/LQ/src/storyMemoryStore.ts
    - C:/Dev/LQ/src/openQuestionStore.ts
    - C:/Dev/LQ/src/characterStore.ts
    - C:/Dev/LQ/src/placeStore.ts
    - C:/Dev/LQ/src/threadStore.ts
    - C:/Dev/LQ/src/projectConfig.ts
  </read_first>
  <acceptance_criteria>
    - `test -f C:/Dev/LQ/src/metadataActionApplier.ts` exits 0
    - `rg -n "applyMetadataAction" C:/Dev/LQ/src/metadataActionApplier.ts C:/Dev/LQ/test/metadataActionApplier.test.ts` returns >=2
    - `rg -n "validateMetadataAction|appendMetadataActionLogEntry|SafeFileSystem|saveOpenQuestion|saveStoryMemory|supersedeStoryMemory|parseThemeFile|serializeThemeFile|updateResearchAssociation|updateChatLogProvenance" C:/Dev/LQ/src/metadataActionApplier.ts` returns >=10
    - `rg -n "stale old value|oldValue|blocked" C:/Dev/LQ/src/metadataActionApplier.ts C:/Dev/LQ/test/metadataActionApplier.test.ts` returns >=3
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    1. Create `src/metadataActionApplier.ts` exporting `applyMetadataAction(rootPath: string, safeFs: SafeFileSystem, rawAction: unknown): Promise<{ status: "applied" | "blocked" | "rejected"; message: string; actionId?: string }>` and helper `configuredMetadataWritableRoots(config: ProjectConfig): string[]`.
    2. `configuredMetadataWritableRoots` returns the configured character, place/settings, thread, theme, and research roots after normalizing trailing slashes. Use existing `ProjectConfig` folder fields where present; if theme metadata uses the existing themes store path rather than a config folder, include that existing theme root explicitly.
    3. `applyMetadataAction` must:
       - Read `ProjectConfig` with defaults.
       - Call `validateMetadataAction(rawAction, { configuredWritableRoots })`.
       - If validation fails or blocks, append a `blocked` log entry when `actionId` and `sourceChatId` exist, return `{ status: "blocked", message }`, and perform no file writes except the log.
       - Reject stale old values when `oldValue` is present and the current field value does not deep-equal `oldValue`; append `rejected` log and return message containing `This metadata action is out of date.`
       - Apply only known operations below; unknown operations should already be blocked.
    4. Implement operation behavior:
       - `createMemory`: `newValue` must contain `{ topic: string; body: string; association?: StoryMemoryAssociation }`; call `createStoryMemory` with `sourceChatId`.
       - `supersedeMemory`: `fieldPath[0]` must be `"memory"` and `fieldPath[1]` must be old memory id; call `supersedeStoryMemory`.
       - `createIssue`: `newValue` must contain `{ title: string; issueType?: string; association: OpenQuestionAssociation; body?: string }`; call `createOpenQuestion`, then if `body` exists save the returned record with `body`.
       - `updateIssueStatus`: `targetPath` must start `.leanquill/issues/`; `fieldPath` must equal `["status"]`; find the issue by basename id and save status from `newValue`.
       - `set`, `append`, `removeFromList`: support only `fieldPath` rooted at `["customFields", key]` for character/place/thread/theme markdown records. Use `parseCharacterFile`/`serializeCharacterFile`, `parsePlaceFile`/`serializePlaceFile`, `parseThreadFile`/`serializeThreadFile`, or the existing theme store parse/serialize helpers based on target root. `append` appends string with `\n`; `removeFromList` removes exact string from a newline-delimited custom field.
       - `updateThemeMetadata`: require a configured theme target path and `risk: "requiresApproval"` with `authorApproval`; update only known theme metadata/frontmatter fields through the existing theme store.
       - `updateResearchAssociation`: require a configured research target path and `risk: "requiresApproval"` with `authorApproval`; update only LeanQuill association metadata fields such as linked character/place/thread/theme/issue ids through the existing research store/persistence pattern.
       - `updateChatLogProvenance`: allow low-risk updates only under `.leanquill/chats/`; update `memory_entry_ids`, `metadata_action_ids`, `story_updates_made`, or `story_update_notes` using `StoryChatLogSummary` serialization, not arbitrary markdown edits.
    5. After any successful operation, append an `applied` log entry and return `{ status: "applied", message: "Applied LeanQuill metadata action.", actionId }`.
    6. Tests must cover createMemory, supersedeMemory, updateIssueStatus, customFields set for a character, theme metadata update, research association update, chat-log provenance update, stale `oldValue` rejection, manuscript path blocking, and approval bypass blocking for a `risk: "low"` entity metadata action.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Accepted actions apply only through deterministic stores/SafeFileSystem, unsafe actions are blocked, and all outcomes are logged.</done>
</task>

</tasks>

<verification>
Run `npm run build:test && npm test`. Static checks must show validator, applier, SafeFileSystem, action log, story memory, and issue store connections.
</verification>

<success_criteria>
- D-09/D-10/D-11/D-13/D-15/D-16 are implemented in deterministic code.
- Higher-risk metadata changes require `risk: "requiresApproval"` plus approval fields before validation succeeds; low-risk auto-apply is limited to `.leanquill/` memory/chat-log/action-log artifacts.
- Manuscript prose writes are structurally blocked and logged as blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-03-SUMMARY.md`
</output>
