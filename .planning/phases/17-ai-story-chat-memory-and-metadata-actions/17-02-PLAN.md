---
phase: 17-ai-story-chat-memory-and-metadata-actions
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - src/storyMemoryStore.ts
  - src/storyChatLogStore.ts
  - test/storyMemoryStore.test.ts
  - test/storyChatLogStore.test.ts
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - "Story memory records are stored under `.leanquill/memory/` and are separate from author-owned notes."
    - "Memory records include association, status, recency, `source_chat_id`, and `supersedes` metadata."
    - "Story chat logs are stored under `.leanquill/chats/` and can reference memory entries and metadata action ids."
    - "A completed story chat can be persisted as both a chat-log summary and automatic low-risk session memory."
  artifacts:
    - path: "src/storyMemoryStore.ts"
      provides: "parse/serialize/list/save/supersede for `.leanquill/memory/*.md`"
    - path: "src/storyChatLogStore.ts"
      provides: "story-chat log summary serialization under `.leanquill/chats/` and session-summary artifact persistence"
    - path: "test/storyMemoryStore.test.ts"
      provides: "Memory schema, association, and supersede regression coverage"
    - path: "test/storyChatLogStore.test.ts"
      provides: "Chat log schema and path regression coverage"
  key_links:
    - from: "src/storyMemoryStore.ts"
      to: ".leanquill/memory/"
      via: "SafeFileSystem.writeFile"
      pattern: "\\.leanquill.*memory"
    - from: "src/storyChatLogStore.ts"
      to: ".leanquill/chats/"
      via: "SafeFileSystem.writeFile"
      pattern: "\\.leanquill.*chats"
    - from: "src/storyChatLogStore.ts"
      to: "src/storyMemoryStore.ts"
      via: "saveStoryChatSessionSummary writes chat log then automatic memory"
      pattern: "saveStoryChatSessionSummary"
---

<objective>
Implement durable story memory and story chat log stores.

Purpose: D-04 through D-08 require automatic low-risk session memory, durable chat summaries, topic-linked associations, and superseding instead of destructive overwrite.
Output: Two store modules plus unit tests.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-CONTEXT.md
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-RESEARCH.md
@Imported/data-contracts/chat-log-schema.md
@src/openQuestionStore.ts
@src/safeFileSystem.ts
@src/types.ts
@test/openQuestionStore.test.ts
</context>

<interfaces>
Create these contracts:

```typescript
export type StoryMemoryStatus = "active" | "superseded" | "archived";
export type StoryMemoryRecency = "current" | "stale";
export type StoryMemoryAssociation =
  | { kind: "book" }
  | { kind: "chapter"; chapterRef: string }
  | { kind: "selection"; chapterRef: string; spanHint: string }
  | { kind: "issue"; issueId: string }
  | { kind: "character"; fileName: string }
  | { kind: "place"; fileName: string }
  | { kind: "thread"; fileName: string }
  | { kind: "theme"; id: string }
  | { kind: "research"; fileName: string };
export interface StoryMemoryRecord {
  fileName: string;
  schemaVersion: "1";
  id: string;
  topic: string;
  status: StoryMemoryStatus;
  createdAt: string;
  updatedAt: string;
  recency: StoryMemoryRecency;
  supersedes: string[];
  sourceChatId: string;
  association: StoryMemoryAssociation;
  body: string;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Story memory store with superseding semantics</name>
  <files>src/storyMemoryStore.ts, test/storyMemoryStore.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/storyMemoryStore.ts (create if absent)
    - C:/Dev/LQ/src/openQuestionStore.ts
    - C:/Dev/LQ/src/safeFileSystem.ts
    - C:/Dev/LQ/src/yamlUtils.ts
    - C:/Dev/LQ/test/openQuestionStore.test.ts
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `test -f C:/Dev/LQ/src/storyMemoryStore.ts` exits 0
    - `rg -n "export interface StoryMemoryRecord" C:/Dev/LQ/src/storyMemoryStore.ts` returns >=1
    - `rg -n "LEANQUILL_MEMORY_DIR|\\.leanquill/memory" C:/Dev/LQ/src/storyMemoryStore.ts` returns >=1
    - `rg -n "storyMemoryToContext|StoryChatMemoryContext|active.*memory" C:/Dev/LQ/src/storyMemoryStore.ts C:/Dev/LQ/test/storyMemoryStore.test.ts` returns >=3
    - `rg -n "supersedeStoryMemory|supersedes" C:/Dev/LQ/src/storyMemoryStore.ts C:/Dev/LQ/test/storyMemoryStore.test.ts` returns >=4
    - `rg -n "lq_assoc_kind|lq_issue_id|lq_character_file|lq_place_file|lq_thread_file|lq_research_file|lq_theme_id" C:/Dev/LQ/src/storyMemoryStore.ts` returns >=7
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    1. Create `src/storyMemoryStore.ts` with constant `export const LEANQUILL_MEMORY_DIR = ".leanquill/memory";`.
    2. Implement `parseStoryMemoryFile(fileName: string, content: string): StoryMemoryRecord` and `serializeStoryMemoryFile(record: StoryMemoryRecord): string`.
       - Frontmatter keys must be exact snake_case: `schema_version`, `id`, `topic`, `status`, `created_at`, `updated_at`, `recency`, `supersedes`, `source_chat_id`, `lq_assoc_kind`.
       - Association keys must be exact: `chapter_ref`, `span_hint`, `lq_issue_id`, `lq_character_file`, `lq_place_file`, `lq_thread_file`, `lq_theme_id`, `lq_research_file`.
       - Unknown or missing status defaults to `active`; unknown or missing recency defaults to `current`; missing association defaults to `{ kind: "book" }`.
    3. Implement `listStoryMemory(rootPath: string, options?: { includeSuperseded?: boolean; includeArchived?: boolean }): Promise<StoryMemoryRecord[]>`.
       - Default list returns only `status === "active"` records.
       - Sort by `updatedAt` descending.
    4. Implement `saveStoryMemory(record: StoryMemoryRecord, rootPath: string, safeFs: SafeFileSystem): Promise<void>` and `createStoryMemory(input, rootPath, safeFs)`.
       - Save path is `path.join(rootPath, ".leanquill", "memory", fileName)`.
       - `createStoryMemory` ids must use prefix `mem-` and suffix `-story-chat`; file names must be `${id}.md`.
    5. Implement `supersedeStoryMemory(rootPath, safeFs, oldId, replacementInput): Promise<{ oldRecord: StoryMemoryRecord; newRecord: StoryMemoryRecord }>`:
       - Old record is rewritten with `status: superseded`, `recency: stale`, and updated timestamp.
       - New record is written with `status: active`, `recency: current`, and `supersedes` containing old id.
       - Do not delete the old file.
    6. Implement `storyMemoryToContext(record: StoryMemoryRecord): StoryChatMemoryContext` so Plan 17-05 can include active memory in chat launch context without coupling extension command code to frontmatter details.
       - Output `path` must be `.leanquill/memory/${record.fileName}`.
       - Output `associationLabel` must be human-readable for book, chapter, selection, issue, character, place, thread, theme, and research associations.
    7. Create `test/storyMemoryStore.test.ts` covering parse/serialize for book, chapter, selection, issue, character, place, thread, theme, research associations; default list excludes superseded; supersede keeps both files; `storyMemoryToContext` produces a path and association label for active memory.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Memory records round-trip and supersede without deletion through `SafeFileSystem`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Story chat log summary store</name>
  <files>src/storyChatLogStore.ts, test/storyChatLogStore.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/storyChatLogStore.ts (create if absent)
    - C:/Dev/LQ/src/storyMemoryStore.ts
    - C:/Dev/LQ/src/safeFileSystem.ts
    - C:/Dev/LQ/Imported/data-contracts/chat-log-schema.md
    - C:/Dev/LQ/test/storyMemoryStore.test.ts
  </read_first>
  <acceptance_criteria>
    - `test -f C:/Dev/LQ/src/storyChatLogStore.ts` exits 0
    - `rg -n "export interface StoryChatLogSummary" C:/Dev/LQ/src/storyChatLogStore.ts` returns >=1
    - `rg -n "saveStoryChatSessionSummary|Saved to story memory|createStoryMemory|saveStoryChatLogSummary" C:/Dev/LQ/src/storyChatLogStore.ts C:/Dev/LQ/test/storyChatLogStore.test.ts` returns >=5
    - `rg -n "session_type: story-chat|metadata_action_ids|memory_entry_ids" C:/Dev/LQ/src/storyChatLogStore.ts C:/Dev/LQ/test/storyChatLogStore.test.ts` returns >=3
    - `rg -n "\\.leanquill.*chats" C:/Dev/LQ/src/storyChatLogStore.ts C:/Dev/LQ/test/storyChatLogStore.test.ts` returns >=2
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    1. Create `src/storyChatLogStore.ts` exporting:
       - `export const LEANQUILL_CHATS_DIR = ".leanquill/chats";`
       - `export interface StoryChatLogSummary { sessionId: string; startedAt: string; endedAt: string; launchedFrom: string; chapterRef: string; chaptersInContext: string[]; summary: string; memoryEntryIds: string[]; metadataActionIds: string[]; transcript?: string; }`
       - `serializeStoryChatLogSummary(summary: StoryChatLogSummary): string`
       - `saveStoryChatLogSummary(rootPath: string, safeFs: SafeFileSystem, summary: StoryChatLogSummary): Promise<string>`
       - `saveStoryChatSessionSummary(rootPath: string, safeFs: SafeFileSystem, input: { chatLog: StoryChatLogSummary; memoryTopic: string; memoryBody: string; memoryAssociation: StoryMemoryAssociation }): Promise<{ chatLogPath: string; memoryRecord: StoryMemoryRecord }>`
    2. `serializeStoryChatLogSummary` frontmatter must include exact keys:
       - `session_id`
       - `session_type: story-chat`
       - `started_at`
       - `ended_at`
       - `chapter_ref`
       - `chapters_in_context`
       - `agent_mode: story-chat`
       - `summary`
       - `memory_entry_ids`
       - `metadata_action_ids`
       - `story_updates_made`
       - `story_update_notes`
    3. `story_updates_made` must be `true` when either `memoryEntryIds.length > 0` or `metadataActionIds.length > 0`; otherwise `false`.
    4. `saveStoryChatLogSummary` must write to `.leanquill/chats/${sessionId}.md` through `SafeFileSystem.writeFile` and return the repo-relative path `.leanquill/chats/${sessionId}.md`.
    5. `saveStoryChatSessionSummary` implements D-04/D-07 low-risk automatic persistence:
       - Save the chat log first with `saveStoryChatLogSummary`.
       - Create a memory record with `createStoryMemory`, `sourceChatId = chatLog.sessionId`, supplied topic/body/association, and status `active`.
       - Update the chat log's `memoryEntryIds` to include the created memory id before final write, so `.leanquill/chats/` and `.leanquill/memory/` cross-reference the same session.
       - Return both the chat log path and created memory record.
    6. Create `test/storyChatLogStore.test.ts` covering serialization keys, true/false `story_updates_made`, list formatting, SafeFileSystem write path, and `saveStoryChatSessionSummary` writing both `.leanquill/chats/${sessionId}.md` and `.leanquill/memory/${memoryId}.md`.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Story chat logs serialize to `.leanquill/chats/` with memory/action provenance fields and tests pass.</done>
</task>

</tasks>

<verification>
Run `npm run build:test && npm test`. Grep checks must confirm `.leanquill/memory`, `.leanquill/chats`, `supersedes`, `source_chat_id`, `memory_entry_ids`, and `metadata_action_ids` are implemented.
</verification>

<success_criteria>
- D-04/D-05/D-06/D-07/D-08 are covered by durable memory, chat log stores, and a low-risk post-chat session-summary persistence helper.
- Memory superseding preserves older records for audit.
- Chat logs can record generated memory and metadata actions.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-02-SUMMARY.md`
</output>
