---
phase: 17-ai-story-chat-memory-and-metadata-actions
plan: 05
type: execute
wave: 4
depends_on:
  - "17-01"
  - "17-02"
  - "17-03"
files_modified:
  - src/extension.ts
  - package.json
  - test/storyChatExtensionCommands.test.ts
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - "Command `leanquill.startStoryChat` opens an unsent story chat draft with project metadata and `manuscriptScope = none`."
    - "Contextual commands exist for issue, character, place, thread, theme, research, chapter, and selection story chat launches, with active story memory included in the launch context."
    - "Selection chat launch context contains only bounded selected text/excerpt plus chapter identity."
    - "Command `leanquill.saveStoryChatSummary` saves a completed story chat summary to `.leanquill/chats/` and automatic low-risk memory under `.leanquill/memory/`."
    - "Command `leanquill.applyMetadataAction` validates and applies accepted MetadataAction JSON through extension code."
    - "Command `leanquill.openStoryMemory` lists story memory records in a native QuickPick and opens the selected markdown file."
  artifacts:
    - path: "src/extension.ts"
      provides: "Story chat command wiring, metadata action apply command, story memory QuickPick"
    - path: "package.json"
      provides: "Command contributions, activation events, command palette/context menu entries"
    - path: "test/storyChatExtensionCommands.test.ts"
      provides: "Static command wiring coverage for memory context, selection bounds, and post-chat save command"
  key_links:
    - from: "leanquill.startStoryChat"
      to: "workbench.action.chat.open"
      via: "{ query, isPartialQuery: true }"
      pattern: "startStoryChat|isPartialQuery"
    - from: "openStoryChatWithContext"
      to: "buildStoryChatContextBundle"
      via: "shared base-context path merge before target-specific context"
      pattern: "project.yaml|outline-index.json|chapter-status-index.json"
    - from: "leanquill.applyMetadataAction"
      to: "applyMetadataAction"
      via: "command handler parses JSON then delegates to metadataActionApplier"
      pattern: "applyMetadataAction"
    - from: "leanquill.startStoryChat"
      to: "src/storyMemoryStore.ts"
      via: "listStoryMemory/storyMemoryToContext populate StoryChatContextBundle.activeMemory"
      pattern: "activeMemory"
    - from: "leanquill.saveStoryChatSummary"
      to: "src/storyChatLogStore.ts"
      via: "saveStoryChatSessionSummary writes .leanquill/chats and .leanquill/memory"
      pattern: "saveStoryChatSessionSummary"
---

<objective>
Wire story chat, metadata action application, and story memory browsing into the VS Code extension command surface.

Purpose: D-01, D-13, and D-14 require native entry points while preserving unsent chat drafts and extension-owned metadata writes.
Output: `src/extension.ts` and `package.json` command/menu/activation updates.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-CONTEXT.md
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-UI-SPEC.md
@src/extension.ts
@package.json
@src/harnessChatDraft.ts
@src/storyChatContext.ts
@src/storyMemoryStore.ts
@src/metadataActionApplier.ts
@src/openQuestionStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register general and contextual story chat commands</name>
  <files>src/extension.ts, test/storyChatExtensionCommands.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/extension.ts (`openHarnessChat`, `newOpenQuestionFrom*`, `openQuestionTargetCommand`, tree provider setup)
    - C:/Dev/LQ/src/harnessChatDraft.ts
    - C:/Dev/LQ/src/storyChatContext.ts
    - C:/Dev/LQ/src/openQuestionStore.ts
    - C:/Dev/LQ/src/storyMemoryStore.ts
    - C:/Dev/LQ/src/projectConfig.ts
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "leanquill\\.startStoryChat|leanquill\\.chatAboutIssue|leanquill\\.chatAboutChapter|leanquill\\.chatAboutSelection" C:/Dev/LQ/src/extension.ts` returns >=4
    - `rg -n "leanquill\\.chatAboutCharacter|leanquill\\.chatAboutPlace|leanquill\\.chatAboutThread|leanquill\\.chatAboutTheme|leanquill\\.chatAboutResearch" C:/Dev/LQ/src/extension.ts` returns >=5
    - `rg -n "buildStoryChatContextBundle|buildStoryChatDraftQuery|workbench\\.action\\.chat\\.open|isPartialQuery:\\s*true" C:/Dev/LQ/src/extension.ts` returns >=4
    - `rg -n "project\\.yaml|outline-index\\.json|chapter-status-index\\.json|baseContextPaths|excludeBaseContext" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns >=8
    - `rg -n "kind:\\s*[\"']issue[\"']|kind:\\s*[\"']chapter[\"']|kind:\\s*[\"']thread[\"']|kind:\\s*[\"']theme[\"']|fileName|label|id" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns >=10
    - `rg -n "manuscriptScope:\\s*\\\"none\\\"|manuscriptScope:\\s*\\\"chapter\\\"|manuscriptScope:\\s*\\\"selection\\\"" C:/Dev/LQ/src/extension.ts` returns >=3
    - `rg -n "listStoryMemory|storyMemoryToContext|activeMemory|selectedTextExcerpt|spanHint|selection\\.active" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns >=8
    - `cd C:/Dev/LQ && npm run build:test && npm test && npm run build` exits 0
  </acceptance_criteria>
  <action>
    1. Import `buildStoryChatContextBundle` from `./storyChatContext`, `buildStoryChatDraftQuery` from `./harnessChatDraft`, and `listStoryMemory` plus `storyMemoryToContext` from `./storyMemoryStore`.
    2. Add local helper `openStoryChatWithContext(input)` beside `openHarnessChat`:
       - Compute `isCursorOrCopilot` exactly like `openHarnessChat`.
       - Before calling `buildStoryChatContextBundle`, merge target-specific `input.includedPaths` with shared base context paths `[ ".leanquill/project.yaml", ".leanquill/outline-index.json", ".leanquill/chapter-status-index.json" ]` per D-02. Preserve target paths, de-duplicate paths, and include these lightweight indexes for every launch unless the input explicitly sets `excludeBaseContext: true`.
       - Load active story memory with `listStoryMemory(rootPath)` before building the bundle, map records through `storyMemoryToContext`, and pass them as `activeMemory` into `buildStoryChatContextBundle(input)`.
       - Build query with `buildStoryChatDraftQuery({ isCursorOrCopilot, contextSummary: bundle.summary })`.
       - Execute `workbench.action.chat.newChat` in try/catch.
       - Execute `workbench.action.chat.open` with `{ query, isPartialQuery: true }`.
       - On failure, show `buildHarnessFallbackHint("storyChat")`.
    3. Register these command ids and push them into `context.subscriptions`:
       - `leanquill.startStoryChat`: `launchedFrom: "general"`, `manuscriptScope: "none"`, included paths `[ ".leanquill/project.yaml", ".leanquill/outline-index.json", ".leanquill/chapter-status-index.json" ]`, excluded paths `[ "manuscript/**" ]`.
       - `leanquill.chatAboutIssue`: accepts issue id string or `{ id: string }`, loads issue with `getOpenQuestion`, target `{ kind: "issue", id, label: issue.title, path: issue.fileName }`, includes issue path `.leanquill/issues/${issue.fileName}`. Use `manuscriptScope: "chapter"` only for chapter/selection associations; otherwise `"none"`.
         - If invoked without args, show a QuickPick of open issues using the existing open-question store; if no issue is selected, cancel without opening chat.
       - `leanquill.chatAboutChapter`: accepts `{ chapterPath?: string }`, target label/path from chapter path, includes that path, `manuscriptScope: "chapter"`.
         - If invoked without args, use the active manuscript editor path when available; otherwise prompt for a chapter path with `showInputBox` and cancel on empty input.
       - `leanquill.chatAboutSelection`: uses active editor; require non-empty selection under `manuscript/`; target label `Selection in ${chapterRef}`, includes chapter path, `manuscriptScope: "selection"`, and target fields `chapterRef`, `spanHint`, and `selectedTextExcerpt`.
         - `spanHint` must include the selected start/end line numbers.
         - `selectedTextExcerpt` must be exactly the selected editor text, truncated only by the shared 2,000-character bound from `StoryChatContextBundle`; do not read or inject unselected chapter prose.
       - `leanquill.chatAboutCharacter`, `leanquill.chatAboutPlace`, `leanquill.chatAboutThread`, `leanquill.chatAboutTheme`, `leanquill.chatAboutResearch`: accept `{ fileName?: string; id?: string; label?: string }`, include the target note/research path when available, use `manuscriptScope: "none"`.
         - If thread/theme/entity/research commands are invoked without args, show a lightweight `showInputBox` fallback for target label or path, build target metadata with the appropriate `kind`, and cancel on empty input.
    4. Do not include full manuscript context in any command. Do not read `manuscript/**/*.md` for general/entity/research chat.
    5. Create `test/storyChatExtensionCommands.test.ts` as a static source test that reads `src/extension.ts` and asserts `listStoryMemory`, `storyMemoryToContext`, `activeMemory`, `spanHint`, `selectedTextExcerpt`, `selection.active`, and `manuscriptScope: "selection"` are present.
    6. Extend `test/storyChatExtensionCommands.test.ts` acceptance checks so every contextual launch includes the D-02 shared base paths `.leanquill/project.yaml`, `.leanquill/outline-index.json`, and `.leanquill/chapter-status-index.json`, and so issue/chapter/thread/theme commands can receive target metadata (`kind`, `id`/`fileName`, `label`, and path where available).
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test && npm run build</automated>
  </verify>
  <done>All story chat command handlers compile and open unsent chat drafts through the shared helper.</done>
</task>

<task type="auto">
  <name>Task 2: Register metadata action apply, story summary save, and memory browser commands</name>
  <files>src/extension.ts, test/storyChatExtensionCommands.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/extension.ts
    - C:/Dev/LQ/src/metadataActionApplier.ts
    - C:/Dev/LQ/src/storyMemoryStore.ts
    - C:/Dev/LQ/src/storyChatLogStore.ts
    - C:/Dev/LQ/src/safeFileSystem.ts
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "leanquill\\.applyMetadataAction|leanquill\\.saveStoryChatSummary|leanquill\\.openStoryMemory" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns >=4
    - `rg -n "applyMetadataAction\\(|listStoryMemory\\(|showQuickPick|Saved to story memory|Applied LeanQuill metadata action|Metadata action was not applied" C:/Dev/LQ/src/extension.ts` returns >=6
    - `rg -n "saveStoryChatSessionSummary|\\.leanquill/chats|\\.leanquill/memory|Story chat summary saved|Saved to story memory" C:/Dev/LQ/src/extension.ts C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns >=6
    - `rg -n "No story memory yet|Start a story chat to preserve session summaries and decisions as LeanQuill memory" C:/Dev/LQ/src/extension.ts` returns >=2
    - `cd C:/Dev/LQ && npm run build:test && npm test && npm run build` exits 0
  </acceptance_criteria>
  <action>
    1. Import `applyMetadataAction` from `./metadataActionApplier`, `listStoryMemory` plus `LEANQUILL_MEMORY_DIR` from `./storyMemoryStore`, and `saveStoryChatSessionSummary` from `./storyChatLogStore`.
    2. Register command `leanquill.applyMetadataAction`:
       - If the active editor has selected text, use that as JSON input.
       - Otherwise show an input box with prompt `Paste accepted LeanQuill MetadataAction JSON`.
       - Parse JSON. On parse failure, show error `Metadata action was not applied. Invalid JSON.`
       - Call `applyMetadataAction(rootPath, safeFileSystem, parsed)`.
       - If result status is `applied`, show information message `Applied LeanQuill metadata action.`
       - If result status is `blocked`, show error message `LeanQuill blocked this metadata action because it would write outside approved project state.`
       - If result status is `rejected`, show warning message `Metadata action was not applied.`
    3. Register command `leanquill.saveStoryChatSummary` for the post-chat/session-summary flow required by D-04/D-07:
       - If the active editor has selected text, use that as JSON input; otherwise show an input box with prompt `Paste LeanQuill story chat summary JSON`.
       - Parse JSON shaped as `{ sessionId, startedAt, endedAt, launchedFrom, chapterRef, chaptersInContext, summary, memoryTopic, memoryBody, memoryAssociation, metadataActionIds }`.
       - Call `saveStoryChatSessionSummary(rootPath, safeFileSystem, parsedInput)` so `.leanquill/chats/${sessionId}.md` and `.leanquill/memory/${memoryId}.md` are both written through deterministic stores.
       - On success, show information message `Saved to story memory.` and include the chat log path in the notification detail when practical.
       - On parse/validation failure, show warning `Story chat summary was not saved.`
    4. Register command `leanquill.openStoryMemory`:
       - Load active memory with `listStoryMemory(rootPath)`.
       - If empty, show information message `No story memory yet. Start a story chat to preserve session summaries and decisions as LeanQuill memory.`
       - Otherwise show QuickPick rows with `label = record.topic`, `description = association label plus status` such as `Chapter: manuscript/ch03.md - Current`, and `detail = ${record.sourceChatId} - ${record.updatedAt}`.
       - On selection, open `path.join(rootPath, ".leanquill", "memory", record.fileName)` in the editor.
    5. Push all three command registrations into `context.subscriptions`.
    6. Extend `test/storyChatExtensionCommands.test.ts` to assert `leanquill.saveStoryChatSummary`, `saveStoryChatSessionSummary`, `.leanquill/chats`, `.leanquill/memory`, and `Saved to story memory.` appear in command wiring source.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test && npm run build</automated>
  </verify>
  <done>Apply, story-summary save, and memory commands compile with fixed UI copy from the UI spec.</done>
</task>

<task type="auto">
  <name>Task 3: package.json command, activation, and menu contributions</name>
  <files>package.json</files>
  <read_first>
    - C:/Dev/LQ/package.json (`activationEvents`, `contributes.commands`, `menus.commandPalette`, `menus.editor/context`, `menus.view/item/context`)
    - C:/Dev/LQ/src/extension.ts
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "\"leanquill\\.startStoryChat\"|\"LeanQuill: Start Story Chat\"" C:/Dev/LQ/package.json` returns >=2
    - `rg -n "\"leanquill\\.applyMetadataAction\"|\"LeanQuill: Apply Metadata Action\"" C:/Dev/LQ/package.json` returns >=2
    - `rg -n "\"leanquill\\.saveStoryChatSummary\"|\"LeanQuill: Save Story Chat Summary\"" C:/Dev/LQ/package.json` returns >=2
    - `rg -n "\"leanquill\\.openStoryMemory\"|\"Open Story Memory\"" C:/Dev/LQ/package.json` returns >=2
    - `rg -n "leanquill\\.chatAboutIssue|leanquill\\.chatAboutChapter|leanquill\\.chatAboutSelection|leanquill\\.chatAboutCharacter|leanquill\\.chatAboutPlace|leanquill\\.chatAboutThread|leanquill\\.chatAboutTheme|leanquill\\.chatAboutResearch" C:/Dev/LQ/package.json` returns >=8
    - `rg -n "leanquill\\.chatAboutIssue|leanquill\\.chatAboutChapter|leanquill\\.chatAboutThread|leanquill\\.chatAboutTheme" C:/Dev/LQ/package.json C:/Dev/LQ/test/storyChatExtensionCommands.test.ts` returns >=8
    - `cd C:/Dev/LQ && node -e "const p=require('./package.json'); for (const id of ['leanquill.startStoryChat','leanquill.applyMetadataAction','leanquill.saveStoryChatSummary','leanquill.openStoryMemory']) { if (!p.activationEvents.includes('onCommand:'+id)) process.exit(1); } console.log('ok');"` exits 0
  </acceptance_criteria>
  <action>
    1. Add command contributions with exact ids/titles:
       - `leanquill.startStoryChat` title `LeanQuill: Start Story Chat`, category `LeanQuill`, icon `$(comment-discussion)`.
       - `leanquill.applyMetadataAction` title `LeanQuill: Apply Metadata Action`, category `LeanQuill`, icon `$(check)`.
       - `leanquill.saveStoryChatSummary` title `LeanQuill: Save Story Chat Summary`, category `LeanQuill`, icon `$(save)`.
       - `leanquill.openStoryMemory` title `Open Story Memory`, category `LeanQuill`, icon `$(lightbulb)`.
       - `leanquill.chatAboutIssue` title `Chat about this issue`, category `LeanQuill`.
       - `leanquill.chatAboutChapter` title `Chat about this chapter`, category `LeanQuill`.
       - `leanquill.chatAboutSelection` title `Chat about selection`, category `LeanQuill`.
       - `leanquill.chatAboutCharacter` title `Chat about this character`, category `LeanQuill`.
       - `leanquill.chatAboutPlace` title `Chat about this place`, category `LeanQuill`.
       - `leanquill.chatAboutThread` title `Chat about this thread`, category `LeanQuill`.
       - `leanquill.chatAboutTheme` title `Chat about this theme`, category `LeanQuill`.
       - `leanquill.chatAboutResearch` title `Chat about this research`, category `LeanQuill`.
    2. Add `onCommand:` activation events for every command id above.
    3. Add command palette entries for `leanquill.startStoryChat`, `leanquill.applyMetadataAction`, `leanquill.saveStoryChatSummary`, and `leanquill.openStoryMemory` when `leanquill.workspaceLeanQuillReady`.
    4. Add `editor/context` entry for `leanquill.chatAboutSelection` when `editorTextFocus && leanquill.workspaceLeanQuillReady`, group `leanquill@20`.
    5. Add `view/item/context` entries:
       - `leanquill.chatAboutIssue` when `view == leanquill.openQuestions && viewItem == openQuestion && leanquill.workspaceLeanQuillReady`, group `leanquill@20`, passing the issue id/file metadata already used by open-question tree handlers where available.
       - `leanquill.chatAboutChapter` when `view == leanquill.outlineTree && viewItem == chapter && leanquill.workspaceLeanQuillReady`, group `leanquill@20`, passing the chapter path metadata already used by outline tree handlers where available.
       - `leanquill.chatAboutCharacter` when `view == leanquill.characters && viewItem == character && leanquill.workspaceLeanQuillReady`
       - `leanquill.chatAboutPlace` when `view == leanquill.places && viewItem == place && leanquill.workspaceLeanQuillReady`
       - `leanquill.chatAboutThread` when `view == leanquill.threads && viewItem == thread && leanquill.workspaceLeanQuillReady`
       - `leanquill.chatAboutTheme` when `view == leanquill.themes && viewItem == theme && leanquill.workspaceLeanQuillReady`
       - `leanquill.chatAboutResearch` when `view == leanquill.research && viewItem == research && leanquill.workspaceLeanQuillReady`
    6. Add `view/title` entry for `leanquill.startStoryChat` on `view == leanquill.outlineTree && leanquill.workspaceLeanQuillReady`, group `navigation@3`.
    7. If any exact tree view id above does not exist in the current package manifest, do not invent a new view for Phase 17. Instead rely on the command handler fallback from Task 1 and add a static test proving the command accepts explicit target metadata when invoked programmatically.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && node -e "const p=require('./package.json'); const ids=p.contributes.commands.map(c=>c.command); for (const id of ['leanquill.startStoryChat','leanquill.applyMetadataAction','leanquill.saveStoryChatSummary','leanquill.openStoryMemory','leanquill.chatAboutSelection']) { if(!ids.includes(id)) process.exit(1); } console.log('ok');" && npm run build</automated>
  </verify>
  <done>Package manifest exposes story chat, metadata action, and memory commands with activation events and target-bearing menus or tested command fallbacks for issue, chapter, thread, and theme.</done>
</task>

</tasks>

<verification>
Run `npm run build`. After all Phase 17 code plans are complete, run `npm run build:test && npm test`.
</verification>

<success_criteria>
- General and contextual chat entry points exist per D-01.
- All chat drafts are unsent and visibly scoped per D-02/D-03/UI-SPEC.
- Metadata action application happens through extension code per D-11/D-14.
- Post-chat session summaries persist through extension-owned stores per D-04/D-07.
- Story memory is discoverable through native QuickPick per UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-05-SUMMARY.md`
</output>
