---
phase: 17-ai-story-chat-memory-and-metadata-actions
plan: 04
type: execute
wave: 4
depends_on:
  - "17-01"
  - "17-03"
files_modified:
  - src/initialize.ts
  - test/storyChatWorkflow.test.ts
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - "Fresh initialization writes `.leanquill/workflows/story-chat.md` and `.leanquill/workflows/metadata-actions.md`."
    - "Copilot, Cursor, and Claude story chat harness entry points are generated idempotently and reference both workflow contracts."
    - "Workflow text states that `leanquill-story-chat` reads active `.leanquill/memory/` context before answering, produces post-chat summaries, and lets the extension validate/apply accepted actions."
  artifacts:
    - path: "src/initialize.ts"
      provides: "Story chat workflow content, metadata action workflow content, and three story chat harness files"
    - path: "test/storyChatWorkflow.test.ts"
      provides: "Static tests for generated workflow and harness content"
  key_links:
    - from: "writeHarnessEntryPoints"
      to: ".leanquill/workflows/story-chat.md"
      via: "harness bodies instruct agents to read workflow contracts first"
      pattern: "story-chat.md"
    - from: "writeHarnessEntryPoints"
      to: ".leanquill/workflows/metadata-actions.md"
      via: "harness bodies instruct agents to emit MetadataAction JSON"
      pattern: "metadata-actions.md"
---

<objective>
Publish canonical story chat and metadata action workflow contracts through initialization and generated harness entry points.

Purpose: D-12 requires a portable LeanQuill contract under `.leanquill/workflows/` with thin Cursor, Copilot, and Claude entry points. This plan follows the Phase 12/15 pattern already in `src/initialize.ts`.
Output: Updated workflow specs and generated `leanquill-story-chat` harness trio.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-CONTEXT.md
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-RESEARCH.md
@.planning/phases/15-import-claude-desktop-research/15-01-PLAN.md
@.planning/phases/15-import-claude-desktop-research/15-03-PLAN.md
@src/initialize.ts
@test/harnessChatDraft.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add story chat and metadata action workflow specs</name>
  <files>src/initialize.ts, test/storyChatWorkflow.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/initialize.ts (`RESEARCH_WORKFLOW_CONTENT`, `RESEARCH_IMPORT_WORKFLOW_CONTENT`, `LEANQUILL_WORKFLOW_SPECS`)
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-CONTEXT.md
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-RESEARCH.md
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "STORY_CHAT_WORKFLOW_CONTENT|METADATA_ACTION_WORKFLOW_CONTENT" C:/Dev/LQ/src/initialize.ts` returns >=2
    - `rg -n "story-chat\\.md|metadata-actions\\.md" C:/Dev/LQ/src/initialize.ts C:/Dev/LQ/test/storyChatWorkflow.test.ts` returns >=4
    - `rg -n "AI proposes metadata actions; LeanQuill extension code validates and applies accepted actions" C:/Dev/LQ/src/initialize.ts` returns >=1
    - `rg -n "Full manuscript context is never automatic|LeanQuill advises but never authors manuscript prose|Read active story memory from \\.leanquill/memory/|After the chat, produce a session summary" C:/Dev/LQ/src/initialize.ts C:/Dev/LQ/test/storyChatWorkflow.test.ts` returns >=4
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    1. In `src/initialize.ts`, add `STORY_CHAT_WORKFLOW_CONTENT` with frontmatter:
       - `name: LeanQuill Story Chat Workflow`
       - `version: 1`
    2. `STORY_CHAT_WORKFLOW_CONTENT` body must include these exact section headings:
       - `# LeanQuill Story Chat Workflow`
       - `## Context rules`
       - `## Conversation rules`
       - `## Active story memory`
       - `## Memory output`
       - `## Metadata action proposals`
       - `## Safety`
    3. The body must contain exact sentences:
       - `LeanQuill advises but never authors manuscript prose.`
       - `Full manuscript context is never automatic.`
       - `AI proposes metadata actions; LeanQuill extension code validates and applies accepted actions.`
       - `Do not write to manuscript chapter files.`
       - `Read active story memory from .leanquill/memory/ before answering, using only records supplied by the LeanQuill chat context or active memory index.`
       - `After the chat, produce a session summary suitable for LeanQuill to save under .leanquill/chats/ and .leanquill/memory/.`
       - `Use LeanQuill: Apply Metadata Action only after the author approves the proposed action.`
       - `Low-risk automatic memory/chat-log writes are limited to .leanquill/memory/ and .leanquill/chats/. Entity, theme, and research metadata actions require authorApproval.`
    4. In `src/initialize.ts`, add `METADATA_ACTION_WORKFLOW_CONTENT` with frontmatter:
       - `name: LeanQuill Metadata Action Contract`
       - `version: 1`
    5. `METADATA_ACTION_WORKFLOW_CONTENT` body must include a JSON example containing keys: `schemaVersion`, `actionId`, `sourceChatId`, `operation`, `targetPath`, `fieldPath`, `oldValue`, `newValue`, `rationale`, `risk`, and `authorApproval`.
    6. Metadata action workflow text must list exact allowed operations: `set`, `append`, `removeFromList`, `createMemory`, `supersedeMemory`, `createIssue`, `updateIssueStatus`, `updateThemeMetadata`, `updateResearchAssociation`, `updateChatLogProvenance`.
    7. Metadata action workflow text must list exact blocked categories: `manuscript prose writes`, `path traversal`, `unconfigured path roots`, `unknown operations`, `schema-invalid payloads`, `approval bypass`, and `destructive deletes`.
    8. Add `{ fileName: "story-chat.md", content: STORY_CHAT_WORKFLOW_CONTENT }` and `{ fileName: "metadata-actions.md", content: METADATA_ACTION_WORKFLOW_CONTENT }` to `LEANQUILL_WORKFLOW_SPECS`.
    9. Create `test/storyChatWorkflow.test.ts` that imports source text by reading `src/initialize.ts` and asserts the static strings above are present. Use `node:test`, `assert/strict`, and `fs.readFile`.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>Fresh init/backfill workflow spec list includes story-chat and metadata-actions contracts with required safety text.</done>
</task>

<task type="auto">
  <name>Task 2: Generate leanquill-story-chat harness trio</name>
  <files>src/initialize.ts, test/storyChatWorkflow.test.ts</files>
  <read_first>
    - C:/Dev/LQ/src/initialize.ts (`writeHarnessEntryPoints` and existing leanquill-researcher/import entries)
    - C:/Dev/LQ/test/storyChatWorkflow.test.ts
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "leanquill-story-chat\\.agent\\.md|skills/leanquill-story-chat|leanquill-story-chat\\.md" C:/Dev/LQ/src/initialize.ts` returns >=3
    - `rg -n "name: leanquill-story-chat" C:/Dev/LQ/src/initialize.ts` returns >=3
    - `rg -n "LeanQuill-Story-Chat" C:/Dev/LQ/src/initialize.ts` returns >=3
    - `rg -n "\\.leanquill/workflows/story-chat\\.md|\\.leanquill/workflows/metadata-actions\\.md|\\.leanquill/memory/|\\.leanquill/chats/" C:/Dev/LQ/src/initialize.ts C:/Dev/LQ/test/storyChatWorkflow.test.ts` returns >=8
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
  </acceptance_criteria>
  <action>
    1. In `writeHarnessEntryPoints`, add three new idempotent entries:
       - Copilot: `.github/agents/leanquill-story-chat.agent.md`
       - Cursor: `.cursor/skills/leanquill-story-chat/SKILL.md`
       - Claude: `.claude/agents/leanquill-story-chat.md`
    2. Each file frontmatter must use `name: leanquill-story-chat` and a description containing exact substring `LeanQuill-Story-Chat`.
    3. Each file body must instruct the harness to read both `.leanquill/workflows/story-chat.md` and `.leanquill/workflows/metadata-actions.md` before answering, then read active story memory from `.leanquill/memory/` when the chat context lists relevant memory records.
    4. Cursor `SKILL.md` body must include a `<cursor_skill_adapter>` block and state invocation when the user mentions `leanquill-story-chat` or asks for LeanQuill story chat.
    5. Copilot tools array should match the existing researcher pattern: `['read', 'write', 'search', 'web']`.
    6. Claude tools line should match existing generated Claude researcher/import pattern: `tools: Read, Write, Edit, Bash, Grep, Glob, WebSearch, WebFetch`.
    7. Harness body text must include exact sentence `Do not directly edit manuscript files or LeanQuill metadata files; propose MetadataAction JSON for extension application.`
    8. Harness body text must instruct the agent to end story chats with a concise summary block the author can pass to `LeanQuill: Save Story Chat Summary`, including session summary, memory topic/body, and any approved metadata action ids.
    9. Append the new entries to the existing `entries` array loop without changing skip-if-exists behavior for any existing researcher/import files.
    10. Extend `test/storyChatWorkflow.test.ts` to assert all three paths, `name: leanquill-story-chat`, both workflow filenames, `.leanquill/memory/`, `.leanquill/chats/`, and `LeanQuill: Save Story Chat Summary` are present.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test</automated>
  </verify>
  <done>`writeHarnessEntryPoints` creates Copilot/Cursor/Claude story chat entry points that reference both canonical workflow contracts.</done>
</task>

</tasks>

<verification>
Run `npm run build:test && npm test`. Static tests and grep checks must confirm workflow filenames, required safety text, allowed operation list, and generated harness entry paths.
</verification>

<success_criteria>
- D-04/D-07/D-12 are satisfied for workflow contracts, post-chat session summary instructions, and cross-harness entry points.
- Harness files remain thin wrappers over canonical `.leanquill/workflows/` contracts.
- Safety contract explicitly forbids manuscript file edits and direct agent metadata writes.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-04-SUMMARY.md`
</output>
