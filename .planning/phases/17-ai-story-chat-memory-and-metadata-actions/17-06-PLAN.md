---
phase: 17-ai-story-chat-memory-and-metadata-actions
plan: 06
type: execute
wave: 5
depends_on:
  - "17-04"
  - "17-05"
files_modified: []
autonomous: false
requirements:
  - TBD
must_haves:
  truths:
    - "Phase 17 execution is explicitly gated until ROADMAP dependencies Phase 9 and Phase 10 are complete."
    - "Full automated suite passes after all Phase 17 implementation plans."
    - "Extension Development Host story chat opens an unsent draft with active story memory in the expected context summary and no automatic general manuscript prose."
    - "Selection story chat shows only the selected range/excerpt plus chapter identity."
    - "Post-chat story summary save writes both `.leanquill/chats/` and `.leanquill/memory/` artifacts."
    - "Metadata action application accepts valid approved JSON, blocks manuscript writes, and records action log entries."
  artifacts:
    - path: "dist/extension.js"
      provides: "Built extension bundle for manual F5 verification"
    - path: "dist-test/"
      provides: "Built node:test bundle for automated verification"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "17-06 final checkpoint"
      via: "Phase 9 and Phase 10 dependency gate is surfaced before runnable verification"
      pattern: "Phase 9|Phase 10|dependency"
    - from: "package.json"
      to: "src/extension.ts"
      via: "registered commands activate implemented handlers"
      pattern: "leanquill.startStoryChat"
---

<objective>
Verify Phase 17 end-to-end after code and workflow plans land.

Purpose: The node:test harness covers deterministic stores/contracts, but VS Code chat opening, QuickPick, context menus, and cross-harness behavior require an Extension Development Host checkpoint.
Output: Automated test results plus human verification of core Phase 17 UX.
</objective>

<execution_context>
@C:/Dev/LQ/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LQ/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-VALIDATION.md
@.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-UI-SPEC.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@src/extension.ts
@src/storyChatContext.ts
@src/metadataActionApplier.ts
@src/storyMemoryStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated Phase 17 gate</name>
  <files>package.json, src/storyChatContext.ts, src/storyMemoryStore.ts, src/metadataActionContract.ts, src/metadataActionApplier.ts, src/extension.ts</files>
  <read_first>
    - C:/Dev/LQ/package.json
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-VALIDATION.md
    - C:/Dev/LQ/src/storyChatContext.ts
    - C:/Dev/LQ/src/storyMemoryStore.ts
    - C:/Dev/LQ/src/metadataActionContract.ts
    - C:/Dev/LQ/src/metadataActionApplier.ts
    - C:/Dev/LQ/src/extension.ts
  </read_first>
  <acceptance_criteria>
    - `cd C:/Dev/LQ && rg -n "Phase 17|depends on Phase 9|Phase 9|Phase 10" .planning/ROADMAP.md .planning/STATE.md .planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-06-PLAN.md` surfaces the dependency gate before automated/manual verification
    - `cd C:/Dev/LQ && npm run build:test && npm test` exits 0
    - `cd C:/Dev/LQ && npm run build` exits 0
    - `rg -n "leanquill\\.startStoryChat|leanquill\\.applyMetadataAction|leanquill\\.openStoryMemory" C:/Dev/LQ/package.json C:/Dev/LQ/src/extension.ts` returns >=6
    - `rg -n "leanquill\\.saveStoryChatSummary|saveStoryChatSessionSummary|Saved to story memory" C:/Dev/LQ/src C:/Dev/LQ/test C:/Dev/LQ/package.json` returns >=4
    - `rg -n "manuscriptScope.*none|Full manuscript context is never automatic|activeMemory|selectedTextExcerpt|spanHint" C:/Dev/LQ/src/storyChatContext.ts C:/Dev/LQ/test/storyChatContext.test.ts` returns >=6
    - `rg -n "metadata-actions\\.jsonl|metadata-actions\\.md|story-chat\\.md|\\.leanquill/memory/|\\.leanquill/chats/" C:/Dev/LQ/src C:/Dev/LQ/test` returns >=7
  </acceptance_criteria>
  <action>
    Phase 17 has an explicit roadmap dependency on Phase 9 and Phase 10. Before running implementation verification, inspect `.planning/ROADMAP.md`, `.planning/STATE.md`, and any Phase 9/10 summaries/status markers. If Phase 9 or Phase 10 is incomplete or not started, stop this plan as blocked and report: `Phase 17 is not runnable until Phase 9 and Phase 10 are complete.` Do not remove or bypass this dependency.

    Once the dependency gate is satisfied, run the automated gate exactly:
    1. `cd C:/Dev/LQ && npm run build:test && npm test`
    2. `cd C:/Dev/LQ && npm run build`
    3. Run the `rg` acceptance checks listed above.
    If any command fails, fix the implementation in the responsible plan files before proceeding to the human checkpoint. Do not skip tests.
  </action>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test && npm run build</automated>
  </verify>
  <done>Phase 9/10 dependency gate is satisfied or surfaced as a blocker; when satisfied, build and full node:test suite are green with static checks for story chat, memory, workflows, and metadata action wiring.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual Extension Development Host verification</name>
  <files>package.json, src/extension.ts, src/storyChatContext.ts, src/metadataActionApplier.ts</files>
  <read_first>
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-UI-SPEC.md
    - C:/Dev/LQ/.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-VALIDATION.md
    - C:/Dev/LQ/package.json
    - C:/Dev/LQ/src/extension.ts
    - C:/Dev/LQ/src/storyChatContext.ts
    - C:/Dev/LQ/src/metadataActionApplier.ts
  </read_first>
  <acceptance_criteria>
    - Human sees the final checkpoint call out that Phase 17 verification is runnable only after Phase 9 and Phase 10 are complete; if either dependency is incomplete, checkpoint is blocked before F5 verification.
    - Human reports command `LeanQuill: Start Story Chat` opens an unsent chat draft containing `Manuscript scope: none`
    - Human reports the general story chat draft lists active story memory when `.leanquill/memory/` contains active records
    - Human reports the general story chat draft does not include chapter prose
    - Human reports editor selection command `Chat about selection` opens an unsent draft containing `Manuscript scope: selection`, selected range/chapter identity, and only the selected excerpt
    - Human reports command `LeanQuill: Save Story Chat Summary` writes one `.leanquill/chats/` file and one `.leanquill/memory/` file for a story chat summary
    - Human reports `LeanQuill: Apply Metadata Action` applies a valid approved `.leanquill/memory/` action and shows `Applied LeanQuill metadata action.`
    - Human reports a proposed action targeting `manuscript/ch01.md` is blocked and shows copy containing `LeanQuill blocked this metadata action`
    - Human reports `.leanquill/metadata-actions.jsonl` contains an `applied` line and a `blocked` line after the two action tests
  </acceptance_criteria>
  <action>
    Pause for human Extension Development Host verification after Task 1 is green and the Phase 9/10 dependency gate is satisfied. Present the dependency gate plus the `<how-to-verify>` steps exactly as written, wait for the resume signal, and do not mark the plan complete until the user reports either `approved` or describes a failed step. If the user describes a failed step, route back to the implementation plan that owns the failed behavior before asking for this checkpoint again.
  </action>
  <what-built>
    Phase 17 story chat commands, story memory storage/browsing, metadata action validation/application, action log, and generated cross-harness workflow contracts.

    Execution gate: Phase 17 is not runnable until roadmap dependencies Phase 9 and Phase 10 are complete. If either dependency is incomplete, stop here and report the blocker instead of launching the Extension Development Host.
  </what-built>
  <how-to-verify>
    1. Confirm Phase 9 and Phase 10 are complete in the roadmap/state artifacts. If not, stop and report `Phase 17 is not runnable until Phase 9 and Phase 10 are complete.`
    2. Launch the extension with VS Code Run and Debug -> Run LeanQuill Extension.
    3. Open a LeanQuill-ready book workspace in the Extension Development Host.
    4. Ensure at least one active `.leanquill/memory/*.md` record exists, then run command `LeanQuill: Start Story Chat`. Expected: chat opens with unsent draft; draft includes `Manuscript scope: none`, active story memory, and no chapter prose.
    5. Open a manuscript chapter, select a short range, and run command `Chat about selection`. Expected: chat opens with unsent draft; draft includes `Manuscript scope: selection`, the chapter identity, the selected range/span, and only the selected excerpt.
    6. Run `LeanQuill: Save Story Chat Summary` with a valid story chat summary JSON. Expected: notification `Saved to story memory.`, a chat log appears under `.leanquill/chats/`, and a memory file appears under `.leanquill/memory/`.
    7. Run `LeanQuill: Apply Metadata Action` with a valid approved `createMemory` JSON targeting `.leanquill/memory/manual-test.md`. Expected: notification `Applied LeanQuill metadata action.` and a memory file appears under `.leanquill/memory/`.
    8. Run `LeanQuill: Apply Metadata Action` with any action whose `targetPath` is `manuscript/ch01.md`. Expected: blocked notification containing `LeanQuill blocked this metadata action`; manuscript file remains unchanged.
    9. Open `.leanquill/metadata-actions.jsonl`. Expected: one JSON line has `"status":"applied"` and another has `"status":"blocked"`.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe the exact failed step and observed behavior.</resume-signal>
  <verify>
    <automated>cd C:/Dev/LQ && npm run build:test && npm test && npm run build</automated>
  </verify>
  <done>Human confirms the Phase 9/10 dependency gate is satisfied, then confirms active memory in story chat drafts, selection scoping, post-chat summary persistence, metadata apply, manuscript blocking, and action logging in Extension Development Host.</done>
</task>

</tasks>

<verification>
Dependency gate: Phase 17 verification is blocked until Phase 9 and Phase 10 are complete. Automated gate after dependencies: `npm run build:test && npm test && npm run build`. Manual gate after dependencies: Extension Development Host verification steps in Task 2.
</verification>

<success_criteria>
- Phase 17 success criteria are demonstrable: story-focused chat, durable memory, and portable auditable metadata action contract.
- AI remains advisory and manuscript prose remains protected.
- Cross-environment workflow files exist, while final application of metadata writes is owned by extension code.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-story-chat-memory-and-metadata-actions/17-06-SUMMARY.md`
</output>
