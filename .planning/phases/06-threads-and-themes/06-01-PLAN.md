---
phase: 06-threads-and-themes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/projectConfig.ts
  - src/themesStore.ts
  - src/threadStore.ts
  - src/chapterPickerOptions.ts
  - src/initialize.ts
  - Imported/data-contracts/project-config-schema.md
  - test/projectConfig.test.ts
  - test/themesStore.test.ts
  - test/threadStore.test.ts
  - test/chapterPickerOptions.test.ts
autonomous: true
requirements:
  - THREAD-01
must_haves:
  truths:
    - "ProjectConfig includes folders.threads with default notes/threads/"
    - "ThemesDocument round-trips through .leanquill/themes.yaml without a new YAML npm dependency"
    - "ThreadProfile files use frontmatter key touchesChapters (manuscript-relative paths, forward slashes)"
    - "Chapter picker options include only outline nodes whose fileName is a non-empty manuscript/*.md path"
    - "Init template project.yaml includes threads: notes/threads/ under folders"
  artifacts:
    - path: "src/types.ts"
      provides: "ThemesDocument, CentralThemeEntry, ThreadProfile"
    - path: "src/themesStore.ts"
      provides: "readThemesDocument, writeThemesDocument, defaultThemesDocument, addCentralThemeEntry"
    - path: "src/threadStore.ts"
      provides: "listThreads, createThread, saveThread, deleteThread, parseThreadFile, serializeThreadFile, slugifyThreadTitle"
    - path: "src/chapterPickerOptions.ts"
      provides: "buildChapterPickerOptions(index, chapterOrder)"
    - path: "src/projectConfig.ts"
      provides: "folders.threads parsing"
  key_links:
    - from: "src/chapterPickerOptions.ts"
      to: "src/types.ts"
      via: "Walks OutlineNode tree; eligible when fileName matches /^manuscript\\/.+\\.md$/i"
    - from: "src/threadStore.ts"
      to: "src/safeFileSystem.ts"
      via: "All thread .md writes go through SafeFileSystem.writeFile"
    - from: "src/themesStore.ts"
      to: ".leanquill/themes.yaml"
      via: "read fs.readFile; write safeFs.writeFile to path.join(rootPath, '.leanquill/themes.yaml')"
---

<objective>
Establish types, `folders.threads` config, `.leanquill/themes.yaml` I/O, per-thread markdown store, and outline-derived chapter picker data — the foundation for Themes + Threads tabs (per D-01–D-04, D-09–D-14).

Purpose: Wave 2 UI and Wave 3 extension wiring import stable contracts without circular dependencies.

Output: New modules `themesStore.ts`, `threadStore.ts`, `chapterPickerOptions.ts`; extended `types.ts`, `projectConfig.ts`, `initialize.ts`; tests and schema doc update for `folders.threads`.
</objective>

<execution_context>
@C:/Dev/LeanQuill/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Dev/LeanQuill/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-threads-and-themes/06-CONTEXT.md
@.planning/phases/06-threads-and-themes/06-RESEARCH.md
@src/characterStore.ts
@src/types.ts
@src/projectConfig.ts
@src/outlineStore.ts
@src/chapterOrder.ts
@test/characterStore.test.ts
@test/projectConfig.test.ts
</context>

<interfaces>
From `src/types.ts` (append; do not remove `CharacterProfile`):

```typescript
export interface CentralThemeEntry {
  id: string;
  title: string;
  summary: string;
  notePath: string;
  linkedChapters: string[];
}

export interface ThemesDocument {
  schemaVersion: string;
  centralQuestion: string;
  bookSynopsis: string;
  bookCustomFields: Record<string, string>;
  centralThemes: CentralThemeEntry[];
  bookLinkedChapters: string[];
}

export interface ThreadProfile {
  fileName: string;
  title: string;
  touchesChapters: string[];
  customFields: Record<string, string>;
  body: string;
}
```

From `src/projectConfig.ts` after change:

```typescript
folders: { research: string; characters: string; threads: string };
// DEFAULT: threads: "notes/threads/"
```

YAML key in `themes.yaml` (file on disk uses snake_case; map to camelCase in TS): top-level `schema_version`, `central_question`, `book_synopsis`, `book_custom_fields` (map), `central_themes` (list of maps with `id`, `title`, `summary`, `note_path`, `linked_chapters`), `book_linked_chapters`. **Per D-14:** optional theme→chapter links only; store in `linked_chapters` per theme and/or `book_linked_chapters` at book level — both are manuscript-relative paths with forward slashes.

Thread frontmatter **reserved keys** (per D-11): `title`, `touchesChapters` (list); all other string keys → `customFields`; body after closing `---` like characters.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, folders.threads, init template, schema doc</name>
  <read_first>
    - src/types.ts (full file)
    - src/projectConfig.ts (full file)
    - src/initialize.ts (renderProjectYaml block, lines ~24–55)
    - test/projectConfig.test.ts (full file)
    - Imported/data-contracts/project-config-schema.md (folders section)
  </read_first>
  <files>src/types.ts, src/projectConfig.ts, src/initialize.ts, test/projectConfig.test.ts, Imported/data-contracts/project-config-schema.md</files>
  <behavior>
    - `parseProjectConfig` with `threads: notes/threads/` inside `folders:` → `folders.threads === "notes/threads/"`
    - Missing `threads:` key → default `"notes/threads/"`
    - `parseProjectConfig` with `threads: custom/t/` → `"custom/t/"`
  </behavior>
  <action>
    Append `CentralThemeEntry`, `ThemesDocument`, `ThreadProfile` to `src/types.ts` exactly as in the `<interfaces>` block (field names and types).

    Extend `ProjectConfig.folders` with `threads: string`. In `parseProjectConfig`, inside the existing `folders:` loop, add a third matcher parallel to `characters`:
    `const threadsMatch = /^\s+threads:\s*["']?(.+?)["']?\s*$/.exec(line);`
    Default `let threads = "notes/threads/";` and assign when matched. Return `folders: { research, characters, threads }`.

    Update `DEFAULT_PROJECT_CONFIG` to include `threads: "notes/threads/"`.

    In `initialize.ts` `renderProjectYaml`, add **after** the `characters:` line:
    `    "  threads: notes/threads/",`

    In `Imported/data-contracts/project-config-schema.md`, under `folders:`, document:
    `  threads: notes/threads/   # narrative thread profile markdown files`

    Add three tests in `test/projectConfig.test.ts` mirroring the existing `characters` tests for `threads` parsing and defaults.
  </action>
  <verify>
    <automated>npm run build:test && npm test -- --test-name-pattern="parseProjectConfig"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "threads" src/projectConfig.ts` shows interface field, default, parse regex, and return object key `threads`.
    - `rg -n "threads: notes/threads" src/initialize.ts` returns at least one match.
    - `rg -n "ThreadProfile|ThemesDocument|CentralThemeEntry" src/types.ts` matches all three type names.
    - `rg -n "threads:" Imported/data-contracts/project-config-schema.md` matches documentation line under folders.
  </acceptance_criteria>
  <done>
    Project builds; parseProjectConfig thread tests pass; init template emits `threads:` under `folders:`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: themesStore — read/write .leanquill/themes.yaml</name>
  <read_first>
    - src/characterStore.ts (serialize/list patterns; no npm yaml)
    - src/safeFileSystem.ts (writeFile to .leanquill)
    - src/types.ts (ThemesDocument from Task 1)
  </read_first>
  <files>src/themesStore.ts, test/themesStore.test.ts</files>
  <behavior>
    - `defaultThemesDocument()` returns schemaVersion `"1"`, empty strings, empty arrays/maps.
    - `parseThemesYaml(content)` on minimal valid YAML string produces document with `centralQuestion` and `bookSynopsis` set from file.
    - `serializeThemesYaml(doc)` then `parseThemesYaml` round-trips for a doc with two `centralThemes` entries and `bookLinkedChapters`.
    - Malformed / empty file: `readThemesDocument` returns default document (no throw).
  </behavior>
  <action>
    Create `src/themesStore.ts` exporting:
    - `THEMES_FILE_NAME = "themes.yaml"` (under `.leanquill/`)
    - `defaultThemesDocument(): ThemesDocument`
    - `parseThemesYaml(content: string): ThemesDocument` — hand-rolled line/block parser sufficient for the fixed schema; normalize CRLF; on unrecoverable parse return `defaultThemesDocument()`.
    - `serializeThemesYaml(doc: ThemesDocument): string` — stable key order: `schema_version`, `central_question`, `book_synopsis`, `book_custom_fields` (scalar map), `central_themes` (each item: `id`, `title`, `summary`, `note_path`, `linked_chapters` list), `book_linked_chapters`.
    - `async readThemesDocument(rootPath: string): Promise<ThemesDocument>` — `fs.readFile` join(rootPath, ".leanquill", "themes.yaml"); if ENOENT return default.
    - `async writeThemesDocument(rootPath: string, doc: ThemesDocument, safeFs: SafeFileSystem): Promise<void>` — `safeFs.writeFile` to `.leanquill/themes.yaml` with serialized content.
    - `addCentralThemeEntry(doc: ThemesDocument): { doc: ThemesDocument; newId: string }` — append entry with `crypto.randomUUID()` as `id`, empty other fields; **does not** write file (caller saves).

    Add `test/themesStore.test.ts` with Node test runner covering default, parse/serialize round-trip, malformed input, and `addCentralThemeEntry` adds one theme.
  </action>
  <verify>
    <automated>npm run build:test && npm test -- --test-name-pattern="themes"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export (async )?function (readThemesDocument|writeThemesDocument|parseThemesYaml|serializeThemesYaml)" src/themesStore.ts` shows all four symbols.
    - `test/themesStore.test.ts` exists and `npm test -- --test-name-pattern="themes"` exits 0.
  </acceptance_criteria>
  <done>
    themesStore reads/writes only `.leanquill/themes.yaml` via SafeFileSystem for writes; tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: threadStore + chapterPickerOptions</name>
  <read_first>
    - src/characterStore.ts (parseCharacterFile/serialize/list/create/save/delete/slugify — mirror for threads)
    - src/types.ts (ThreadProfile, OutlineNode, OutlineIndex)
    - src/chapterOrder.ts (`resolveChapterOrder`)
    - test/characterStore.test.ts (withTempDir, SafeFileSystem allowPath pattern)
  </read_first>
  <files>src/threadStore.ts, src/chapterPickerOptions.ts, test/threadStore.test.ts, test/chapterPickerOptions.test.ts</files>
  <behavior>
    - `slugifyThreadTitle("Plot A")` → `"plot-a.md"` (same rules as `slugifyCharacterName`).
    - `parseThreadFile` reads `touchesChapters` list from frontmatter; missing → `[]`.
    - `serializeThreadFile` emits `touchesChapters:` with forward slashes.
    - `buildChapterPickerOptions(index, chapterOrder)` returns only paths present in outline with `fileName` matching `manuscript/*.md`; ordering: follow `chapterOrder.chapterPaths` first, then any remaining eligible nodes by title.
    - I/O: create/list/save/delete mirror characterStore tests using `folders.threads`.
  </behavior>
  <action>
    Create `src/threadStore.ts` mirroring `characterStore.ts` structure:
    - Reserved frontmatter keys: `title`, `touchesChapters` (reuse list parsing logic pattern from character `aliases` / `referencedByNameIn`).
    - Exports: `slugifyThreadTitle`, `parseThreadFile`, `serializeThreadFile`, `listThreads`, `createThread(initialTitle: string, ...)`, `saveThread`, `deleteThread` — signatures parallel character equivalents but `ThreadProfile` and `config.folders.threads`.

    Create `src/chapterPickerOptions.ts` exporting:
    ```typescript
    export interface ChapterPickerOption { path: string; title: string; }
    export function buildChapterPickerOptions(
      index: OutlineIndex,
      chapterOrder: ChapterOrderResult,
    ): ChapterPickerOption[]
    ```
    Implementation (per D-12, D-13): recursively walk `index.nodes`; collect nodes where `node.fileName.trim() !== ""` and `node.fileName.replace(/\\/g,"/").match(/^manuscript\/.+\.md$/i)`. Build `path -> title` map. Output array: iterate `chapterOrder.chapterPaths` in order, push option if in map; then append any map keys not yet emitted sorted by title.

    Add `test/threadStore.test.ts` and `test/chapterPickerOptions.test.ts` with focused cases (≥8 thread tests, ≥3 picker tests including empty outline and nested outline).
  </action>
  <verify>
    <automated>npm run build:test && npm test -- --test-name-pattern="thread|chapterPicker|ChapterPicker"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "touchesChapters" src/threadStore.ts` shows parse + serialize handling.
    - `rg -n "buildChapterPickerOptions" src/chapterPickerOptions.ts` exists.
    - `rg -n "manuscript/" src/chapterPickerOptions.ts` shows eligibility filter for chapter files only.
  </acceptance_criteria>
  <done>
    threadStore CRUD works with SafeFileSystem; chapter picker excludes empty fileName and non-manuscript paths; all new tests pass; `npm run build` succeeds.
  </done>
</task>

</tasks>

<verification>
```bash
npm run build
npm run build:test && npm test
rg -n "folders\\.threads|threads:" src/projectConfig.ts src/initialize.ts
```
</verification>

<success_criteria>
- THREAD-01 data layer: themes YAML contract, thread markdown with `touchesChapters`, chapter-only picker helper (per CONTEXT).
- No new npm dependencies for YAML.
- All automated tests for this plan pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-threads-and-themes/06-01-SUMMARY.md`
</output>
