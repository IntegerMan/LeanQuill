// STUB: Phase 08-02 implements

/**
 * Resolve a `span_hint` fragment within manuscript text (exact, stale, fuzzy — D-13).
 * Phase 08-01: stub throws until implementation lands in 08-02.
 */
export function resolveSpanHintInDocument(
  _fullText: string,
  _spanHint: string,
  _preferredStartIndex?: number,
): never {
  throw new Error("STUB: Phase 08-02 implements");
}
